# 野球シミュレーター 技術仕様書

> このドキュメントはAI（Claude Code等）が読み取りやすい形式で記述されています。
> 機能追加・修正時にこのファイルを参照してください。

---

## 1. システム概要

```yaml
名称: 野球シミュレーター
バージョン: v25_15
技術スタック:
  - React 18
  - Tailwind CSS
  - 物理演算エンジン v2（Gemini設計）
ゲーム形式: 1球ごとのシミュレーション
対戦形式: ホームチーム vs アウェイチーム（各9人）
```

---

## 2. 球種システム

### 2.1 球種効果パラメータ

各球種は以下の4パラメータで定義される：

| パラメータ | 説明 | 値の範囲 |
|-----------|------|---------|
| `whiffBonus` | 空振り率への加算 | -0.05 ~ +0.10 |
| `groundballBonus` | ゴロ率への加算 | 0 ~ +0.15 |
| `weakBonus` | 弱い打球率への加算 | -0.04 ~ +0.23 |
| `velocityMinus` | ストレートからの球速低下(km/h) | 0 ~ 30 |

### 2.2 球種一覧

```javascript
ballEffects = {
  straight:  { whiffBonus: 0,     groundballBonus: 0,    weakBonus: -0.04, velocityMinus: 0  },
  twoSeam:   { whiffBonus: -0.05, groundballBonus: 0.12, weakBonus: 0.12,  velocityMinus: 5  },
  slider:    { whiffBonus: 0.09,  groundballBonus: 0.05, weakBonus: -0.02, velocityMinus: 12 },
  curve:     { whiffBonus: 0.04,  groundballBonus: 0.06, weakBonus: 0,     velocityMinus: 23 },
  fork:      { whiffBonus: 0.09,  groundballBonus: 0.06, weakBonus: 0.13,  velocityMinus: 17 },
  changeup:  { whiffBonus: 0.03,  groundballBonus: 0.14, weakBonus: 0.02,  velocityMinus: 21 },
  sinker:    { whiffBonus: 0,     groundballBonus: 0.15, weakBonus: 0.23,  velocityMinus: 8  },
  shoot:     { whiffBonus: 0,     groundballBonus: 0.12, weakBonus: 0.23,  velocityMinus: 5  },
  cutter:    { whiffBonus: 0,     groundballBonus: 0.09, weakBonus: 0.20,  velocityMinus: 5  },
  splitter:  { whiffBonus: 0.09,  groundballBonus: 0.05, weakBonus: 0.05,  velocityMinus: 7  },
  palm:      { whiffBonus: 0.05,  groundballBonus: 0.07, weakBonus: 0.11,  velocityMinus: 22 },
  knuckle:   { whiffBonus: 0.10,  groundballBonus: 0.02, weakBonus: 0.15,  velocityMinus: 30 }
}
```

### 2.3 球種特性の解釈

```yaml
空振り系（whiffBonus高）:
  - knuckle (+0.10), fork/slider/splitter (+0.09)

ゴロ系（groundballBonus高）:
  - sinker (+0.15), changeup (+0.14), twoSeam/shoot (+0.12)

弱い打球系（weakBonus高）:
  - sinker/shoot (+0.23), cutter (+0.20), knuckle (+0.15), fork (+0.13)

速球系（velocityMinus低）:
  - straight (0), twoSeam/shoot/cutter (5), splitter (7), sinker (8)

緩急系（velocityMinus高）:
  - knuckle (30), curve (23), palm (22), changeup (21)
```

### 2.4 球種レベルによる完成度と効果

各投手の変化球には **レベル（0〜100）** があり、レベルが高いほど効果的になる。

#### レベルの影響一覧

| 要素 | 計算式 | Lv0 | Lv25 | Lv50 | Lv75 | Lv100 | 備考 |
|------|--------|-----|------|------|------|-------|------|
| **制球ペナルティ** | `(100 - level) × 0.05` | -5.0% | -3.75% | -2.5% | -1.25% | 0% | ストライク率が低下 |
| **コンタクトペナルティ** | `level × 0.12` | 0% | -3.0% | -6.0% | -9.0% | -12.0% | 打者のコンタクト率が低下 |
| **ボール球追い率** | `level × 0.05` | 0% | +1.25% | +2.5% | +3.75% | +5.0% | 打者がボール球を振る率が上昇 |
| **タイミング窓縮小** | `level/100 × 0.08` | 0% | -2.0% | -4.0% | -6.0% | -8.0% | 打者のタイミング窓が縮小 |
| **球速減速幅** | `8 + (level/100) × 15` | -8km | -11.75km | -15.5km | -19.25km | -23km | ストレートとの球速差（緩急効果） |

#### レベル帯の評価目安

```yaml
Lv 0〜19:  未完成。制球が大きく乱れ、変化も小さい。実戦では使い物にならない。
Lv 20〜39: 発展途上。一応投げられるが制球が不安定。カウント球としては不安。
Lv 40〜59: 実用レベル。制球・変化ともに一定水準。ローテ投手の持ち球として最低限。
Lv 60〜74: 武器級。高い空振り・ゴロ誘発力を発揮。決め球として信頼できる。
Lv 75〜89: 一級品。制球ペナルティがほぼ消え、打者を翻弄する変化。
Lv 90〜100: 超一流。制球・キレ・緩急すべてが最高峰。打者にとって脅威。
```

#### レベルが影響しない要素

- `whiffBonus` / `groundballBonus` / `weakBonus` / `velocityMinus` の基本値は球種固定（レベルに依存しない）
- レベルはこれらの基本効果を「活かせるかどうか」（制球・タイミング・追い率）に影響する

#### 球種習得時の初期レベル

```yaml
キャンプ・メイン練習「新球種習得」:
  大成功 (25%): Lv 65〜75（即戦力）
  成功   (50%): Lv 10〜19（要育成）
  失敗   (25%): 習得不可

キャンプ・サブ練習「新球種習得」:
  成功   (12%): Lv 20〜39（球種ランダム）
  失敗   (88%): 習得不可

派遣（大学野球留学/プロ研修）:
  変化球成長は既存球種のレベルアップのみ（新球種習得なし）
```

### 2.5 投球フォームと球種の相性

投球フォームによって特定の変化球に相性ボーナスがある。

| フォーム | 球速補正 | 縦変化補正 | 横変化補正 | 相性の良い球種 |
|----------|---------|-----------|-----------|--------------|
| オーバースロー | +3km | +15% | 0% | カーブ, フォーク, スプリッター, ナックル |
| スリークォーター | 0km | +5% | +5% | （すべて平均的） |
| サイドスロー | -2km | -5% | +15% | スライダー, シュート, カッター, ツーシーム |
| アンダースロー | -5km | +10% | +5% | シンカー, カーブ, パーム |

※サイドスロー・アンダースローは同じ利き腕の打者に対して空振り率ボーナスあり（+3%/+5%）

---

## 3. 選手パラメータ

### 3.1 打撃パラメータ

| パラメータ | 説明 | 影響 | 値の範囲 |
|-----------|------|------|---------|
| `meet` | ミート力 | タイミング誤差の最大値を減少 | 0-100 |
| `power` | パワー | スイングスピード・打球初速に影響 | 0-100 |
| `eye` | 選球眼 | ボール球のスイング率を低下 | 0-100 |
| `bats` | 打席 | 左右相性に影響 | 'left' / 'right' / 'switch' |
| `steal` | 盗塁能力 | （将来拡張用） | 0-100 |

#### 計算式

```javascript
// スイングスピード（km/h）
v_swing = 100 + (power * 0.4)  // 100-140km/h

// タイミング誤差の最大値（ms）
maxError = 8 - (meet * 0.06)  // 8-2ms
```

### 3.2 投手パラメータ

| パラメータ | 説明 | 影響 | 値の範囲 |
|-----------|------|------|---------|
| `velocity` | 球速 | 最高球速(km/h)、タイミング窓に影響 | 120-165 |
| `control` | 制球力 | ストライクゾーン率に影響 | 0-100 |
| `stamina` | スタミナ | 投球可能数、疲労時のペナルティ | 50-150 |
| `throws` | 投球腕 | 左右相性に影響 | 'left' / 'right' |
| `arsenal` | 持ち球 | 投球可能な球種とレベル | Array |

#### 計算式

```javascript
// ストライクゾーン確率
strikeZoneProb = 0.25 + (control / 100) * 0.65  // 25-90%

// 球種レベルによる制球ペナルティ（ストレート以外）
controlPenalty = 30 - (ballLevel / 100) * 30  // 0-30
```

### 3.3 捕手パラメータ

| パラメータ | 説明 | 影響 | 値の範囲 |
|-----------|------|------|---------|
| `lead` | リード力 | 配球最適化、トンネリング効果増大、予測率低下 | 0-100 |
| `arm` | 肩力 | 盗塁阻止率（将来拡張用） | 0-100 |

### 3.4 守備パラメータ

| パラメータ | 説明 | 影響 | 値の範囲 |
|-----------|------|------|---------|
| `defense` | 守備力 | 捕球成功率に加算 | 0-100 |
| `speed` | 走力 | 守備範囲、打球到達に影響 | 0-100 |
| `arm` | 肩力 | 送球・タッチアップ阻止 | 0-100 |
| `throws` | 投球腕 | 内野守備ペナルティ（左投げ二遊三：-20） | 'left' / 'right' |

---

## 4. 物理演算エンジン

### 4.1 定数

```javascript
BABIP_CALIBRATION = 0.32  // BABIP校正係数（参考値）
COR = 0.45                // 反発係数（バットとボールの衝突）
```

### 4.2 タイミングウィンドウ計算

打者が「芯」で捉えられる許容時間（ms）：

```javascript
// 基本計算（球速に反比例）
timingWindow = (1000 / (pitchVelocity / 3.6)) * 0.20

// 修正要素
if (読みが的中) timingWindow *= 1.2          // +20%
timingWindow *= (1 - tunnelingEffect * 0.2)  // トンネリングで縮小
if (変化球) timingWindow *= (1 - ballLevel/100 * 0.08)  // 変化球で縮小

// 目安
// 160km/h → 約45ms
// 140km/h → 約51ms
// 120km/h → 約60ms
```

### 4.3 空振り判定

```javascript
// タイミング誤差を計算
timingError = random(-maxError, +maxError)

// ミート品質（0-1）
meetQuality = max(0, 1 - abs(timingError) / timingWindow)

// 空振り判定
isContact = meetQuality > 0.005  // 99.5%以上でコンタクト
```

### 4.4 打球初速計算

```javascript
if (isContact) {
  baseVelocity = 80 + (power * 0.2)           // 80-100km/h
  qualityBonus = meetQuality * 50              // 0-50km/h
  pitchBonus = (pitchVelocity - 130) * 0.25    // 反発ボーナス
  exitVelocity = baseVelocity + qualityBonus + pitchBonus + random(-5, +5)
  exitVelocity = clamp(70, 175)                // 70-175km/h
}

// MLB参考値
// 平均Exit Velocity: 約140km/h (88mph)
// 最大Exit Velocity: 約190km/h (120mph)
```

### 4.5 打出し角度計算

```javascript
// ミート品質による基本角度
if (meetQuality > 0.8) {
  angle = 25 + random(10)      // バレルゾーン: 25-35度
} else if (meetQuality > 0.6) {
  angle = 10 + random(30)      // 高品質: 10-40度
} else if (meetQuality > 0.4) {
  angle = -5 + random(55)      // 中品質: -5-50度
} else {
  // 低品質: ボテボテかポップフライ
  angle = random() < 0.5 ? (-15 + random(20)) : (55 + random(30))
}

// パワー補正
angle += (power - 60) / 100 * 5
```

### 4.6 飛距離計算

```javascript
rad = launchAngle * PI / 180
v = exitVelocity / 3.6  // m/s変換

// 滞空時間
hangTime = max(0.5, (2 * v * sin(max(0, rad))) / 9.8)

// 飛距離
if (launchAngle <= 0) {
  // ゴロ
  distance = 15 + exitVelocity * 0.15 + random(20)
} else {
  // フライ/ライナー
  optimalAngleFactor = 1 - abs(launchAngle - 30) / 60  // 30度が最適
  distance = v * cos(rad) * hangTime * (0.8 + optimalAngleFactor * 0.4)
  distance *= (0.9 + power / 500)
}
```

---

## 5. 守備判定システム

### 5.1 担当野手決定

```yaml
距離 < 40m（内野）:
  - 距離 < 15m → pitcher
  - 方向 < -15度 → third
  - 方向 < 0度 → short
  - 方向 < 15度 → second
  - それ以外 → first

距離 >= 40m（外野）:
  - 方向 < -10度 → left
  - 方向 <= 10度 → center
  - それ以外 → right
```

### 5.2 結果判定ルール

#### ホームラン判定

```javascript
// 条件: 距離>100m AND 角度20-40度 AND 初速>=150km/h
if (distance > 100 && launchAngle >= 20 && launchAngle <= 40 && exitVelocity >= 150) {
  return 'homerun'
}

// 長打圏フライ: 距離>95m AND 角度25-45度 AND 初速>=140km/h → 70%HR
if (distance > 95 && launchAngle >= 25 && launchAngle <= 45 && exitVelocity >= 140) {
  if (random() < 0.70) return 'homerun'
}
```

#### アウト率テーブル

| 打球タイプ | 条件 | 基本アウト率 |
|-----------|------|-------------|
| ポップフライ | 角度 >= 60度 | 95-97% |
| 内野ゴロ | 角度 < 10度, 距離 < 40m | 98.5% |
| 外野ゴロ | 角度 < 10度, 距離 >= 40m | 30% |
| 内野ライナー | 角度 10-25度, 距離 < 40m | 90% |
| 外野ライナー | 角度 10-25度, 距離 >= 40m | 82% |
| 内野フライ | 角度 >= 25度, 距離 < 40m | 97% |
| 浅い外野フライ | 距離 < 70m | 99.5% |
| 中堅フライ | 距離 70-90m | 95% |
| 深いフライ | 距離 > 90m | 80% |

#### 守備力による補正

```javascript
defenseBonus = (fielder.defense - 70) / 100 * 補正係数
speedBonus = (fielder.speed - 65) / 100 * 補正係数
catchProb = min(上限, baseOutRate + defenseBonus + speedBonus)
```

---

## 6. ゲームルール

### 6.1 カウント補正

```javascript
getCountAdjustment(balls, strikes) {
  if (balls === 3 && strikes <= 1) {
    // 3ボール: 打者有利
    return { strikeZone: 0.7, swingRate: 0.6 }
  }
  if (strikes === 2) {
    // 2ストライク: 投手有利
    return { strikeZone: 1.2, swingRate: 1.3 }
  }
  return { strikeZone: 1.0, swingRate: 1.0 }
}
```

### 6.2 スタミナシステム

```javascript
// 1球ごとに1消費
currentStamina -= 1

// スタミナ残量によるペナルティ
getStaminaPenalty(current, max) {
  rate = current / max

  if (rate >= 0.50) return { velocity: 0, control: 0 }
  if (rate >= 0.40) return { velocity: -5, control: -5 }
  if (rate >= 0.30) return { velocity: -10, control: -10 }
  if (rate >= 0.20) return { velocity: -15, control: -15 }
  if (rate >= 0.10) return { velocity: -20, control: -20 }
  return { velocity: -25, control: -25 }
}
```

### 6.3 左右相性

```javascript
getHandednessEffect(pitcherThrows, batterBats) {
  // スイッチヒッターは常に逆側に立つ
  if (batterBats === 'switch') {
    effectiveBats = pitcherThrows === 'right' ? 'left' : 'right'
  }

  isOppositeHand = (右投げ-左打ち) or (左投げ-右打ち)

  if (isOppositeHand) {
    // 打者有利
    return {
      meetBonus: +5,
      powerBonus: (右打者のみ) +2,
      contactBonus: +0.03,
      infieldHitBonus: (左打者のみ) +0.05
    }
  } else {
    // 投手有利
    return {
      meetPenalty: -5,
      powerBonus: (右打者のみ) +2,
      contactBonus: -0.03,
      infieldHitBonus: (左打者のみ) +0.05
    }
  }
}
```

### 6.4 球種予測システム

```javascript
// 球種数による予測成功率
predictionRates = {
  1: 100%,
  2: 50%,
  3: 31.3%,
  4: 21%,
  5: 16%,
  6: 10.6%
}

// キャッチャーリードによる追加削減
leadReduction = (catcher.lead / 100) * 0.02 * (totalPitchTypes - 1)
finalRate = baseRate - leadReduction

// 予測成功時: タイミングウィンドウ +20%
```

### 6.5 トンネリング効果

```javascript
getTunnelingEffect(lastPitch, currentPitch, catcherLead) {
  // 球速差による錯覚（比率ベース: 遅い投手ほど体感の緩急が大きい）
  veloDiff = abs(lastPitch.velocity - currentPitch.velocity)
  baseVelocity = max(lastPitch.velocity, currentPitch.velocity)
  veloDiffRatio = veloDiff / baseVelocity

  // 軌道近似性
  if (前球ストレート → 今球変化球) orbitCloseness = 0.8
  else if (前球変化球 → 今球ストレート) orbitCloseness = 0.6
  else orbitCloseness = 0.2

  // リード力で効果増大
  leadMultiplier = 1 + (catcherLead / 100)

  return min(0.25, (veloDiffRatio * 0.45 + orbitCloseness * 0.025) * leadMultiplier)
}

// 効果: タイミングウィンドウを縮小（最大25%）
```

### 6.5.1 緩急ペナルティ（コンタクト率への影響）

```javascript
// 比率ベース: 遅い投手ほど同じ球速差で大きな緩急効果を得る
speedDiffRatio = veloDiff / effectiveVelocity
speedDiffPenalty = speedDiffRatio * 22

// 例: カーブ（-23km）を投げた場合
// 155km投手: 23/155 = 14.8% → penalty = 3.27
// 130km投手: 23/130 = 17.7% → penalty = 3.89（+19%有利）
```

### 6.6 ファウル判定

```javascript
// 基本ファウル率
foulBaseRate = 0.35

// ミート品質によるペナルティ
qualityPenalty = (1 - meetQuality) * 0.25

// 2ストライク時は粘り
if (strikes === 2) foulRate += 0.10

// 打球方向によるファウル（|方向| > 30度で70%）
if (abs(direction) > 30 && random() < 0.70) return 'foul'
```

---

## 7. スイング判定

### 7.1 スイング確率

```javascript
// ストライクゾーン内
if (isInStrikeZone) {
  if (strikes === 2) {
    swingProb = 0.90 + ((100 - eye) / 100) * 0.08  // 見逃し三振回避
  } else if (balls >= 2) {
    swingProb = 0.60 + ((100 - eye) / 100) * 0.30  // 有利カウント
  } else {
    swingProb = 0.75 + ((100 - eye) / 100) * 0.20  // 通常
  }
  swingProb *= (1 - catcherLeadEffect)
}

// ボールゾーン
else {
  if (strikes === 2) {
    swingProb = 0.20 + ((100 - eye) / 100) * 0.30
  } else {
    swingProb = 0.15 + ((100 - eye) / 100) * 0.30
  }
}
```

---

## 8. 走者進塁ルール

```javascript
advanceRunners(hitType) {
  advancement = {
    'single': 1,
    'double': 2,
    'triple': 3,
    'homerun': 4  // 全員生還
  }

  // 各走者を進塁させる
  for (runner on base) {
    newBase = currentBase + advancement
    if (newBase >= 3) runsScored++
    else newBases[newBase] = true
  }

  // 打者も進塁
  if (advancement < 3) {
    newBases[advancement - 1] = true
  } else {
    runsScored++
  }
}
```

---

## 9. データ構造

### 9.1 選手データ構造

```javascript
player = {
  id: number,
  name: string,
  position: 'pitcher' | 'catcher' | 'first' | 'second' | 'short' | 'third' | 'left' | 'center' | 'right',
  battingOrder: 1-9,

  batting: {
    meet: 0-100,
    power: 0-100,
    eye: 0-100,
    bats: 'left' | 'right' | 'switch',
    steal: 0-100
  },

  physical: {
    speed: 0-100,
    arm: 0-100,
    throws: 'left' | 'right'
  },

  fielding: {
    defense: 0-100
  },

  catching: {
    lead: 0-100
  },

  pitching: {
    velocity: 120-165,
    control: 0-100,
    stamina: 50-150,
    arsenal: [
      { id: number, type: string, level: 0-100 }
    ]
  },

  stats: {
    batting: { atBats, hits, homeruns, rbis, walks, strikeouts },
    pitching: { outs, runsAllowed, strikeouts, walks, pitches }
  }
}
```

### 9.2 ゲーム状態

```javascript
gameState = {
  inning: 1-12,
  isTopInning: boolean,
  outs: 0-2,
  count: { balls: 0-3, strikes: 0-2 },
  bases: [boolean, boolean, boolean],  // 1塁, 2塁, 3塁
  score: { home: number, away: number },
  currentBatterIndex: { home: 0-8, away: 0-8 },
  currentStamina: number,
  gameOver: boolean
}
```

---

## 10. 統計記録

### 10.1 打球タイプ統計

```javascript
battedBallTypes = {
  grounder: number,  // 角度 < 10度
  liner: number,     // 角度 10-25度
  fly: number,       // 角度 25-50度
  popup: number      // 角度 >= 50度
}
```

### 10.2 打球方向統計

```javascript
directions = {
  left: number,        // 方向 < -20度
  leftCenter: number,  // 方向 -20 ~ -5度
  center: number,      // 方向 -5 ~ 5度
  rightCenter: number, // 方向 5 ~ 20度
  right: number        // 方向 > 20度
}
```

---

## 11. 拡張ポイント

### 11.1 現在未実装の機能

```yaml
盗塁システム:
  - steal パラメータは定義済み
  - catcher.arm は定義済み
  - 実装待ち

犠牲フライ:
  - isOutfieldFly フラグは出力済み
  - tagupThrowbackChance は計算済み
  - タッチアップ進塁の実装待ち

投手交代:
  - 複数投手のデータ構造は対応済み
  - 交代UIの実装待ち

代打・代走:
  - 選手交代の仕組み未実装
```

### 11.2 調整可能なパラメータ

```yaml
難易度調整:
  - BABIP_CALIBRATION: BABIP全体の調整
  - 空振り閾値: 0.005（現在）を変更
  - ファウル基本率: 0.35（現在）を変更

バランス調整:
  - 各球種のwhiffBonus/groundballBonus/weakBonus
  - アウト率テーブルの各値
  - スタミナペナルティの閾値
```

---

## 12. シーズン成長システム（キャンプ & 派遣）

選手はシーズンオフの**キャンプ練習**と**派遣**の2つの経路で成長する。
実装は `src/season/yearProgressionSystem.js`。

### 12.1 概要

```yaml
キャンプ練習:
  対象: 全選手（派遣中の選手を除く）
  期間: 4クール制（MAX_CAMP_ROUNDS = 4）
  選択: 各クール メイン練習1つ + サブ練習1つ
  特徴: 小刻みな成長、能力値70以上で減衰、稀に覚醒で飛躍

派遣:
  対象: 若手低能力値の選手（年齢/総合力制限あり）
  期間: キャンプ全期間（通常練習には参加しない）
  枠: 各チーム各派遣先1人、リーグ合計8人
  特徴: 一気に大幅成長、減衰なし、失敗なし、稀に追加覚醒
```

### 12.2 キャンプ練習の基礎成長公式

```javascript
// 1クールあたりの基礎成長量（メイン練習の1ターゲット能力ごと）
rawBase  = (random 1-3) × ageMultiplier × posBonus × boBonus
rawFocus = (random 1-4) × ageMultiplier × posBonus × boBonus
baseGrowth = round((rawBase + rawFocus) × 0.11)   // 0.11で抑制

// 覚醒（減衰を受けない大幅ボーナス）
awakeningChance = floor(experience / 15)   // 単位: %
if (random < awakeningChance) {
  awakeningGrowth = floor(random × 6) + 3   // +3〜8
}

// 減衰後の最終成長量
totalGrowth = max(0, adjustedBaseGrowth + awakeningGrowth)
```

0.11の抑制係数は「キャンプだけでプロ級(80+)に到達しない」ための調整値（元は0.167）。

### 12.3 年齢補正（getAgeGrowthBase）

`ageMultiplier = max(0.3, 1.0 + ageBase × 0.15)` で成長倍率を算出。

**フィジカル系**（speed / arm / stamina / velocity / bodyStamina / recovery）

| 年齢 | base | ageMultiplier |
|------|------|---------------|
| ≤20 | +2.5 | 1.375 |
| 21-22 | +1.8 | 1.27 |
| 23-25 | +0.8 | 1.12 |
| 26-28 | +0.1 | 1.015 |
| 29-31 | -0.5 | 0.925 |
| 32-34 | -1.2 | 0.82 |
| 35-37 | -2.0 | 0.70 |
| 38+ | -3.0 | 0.55 |

**技術系**（meet / power / eye / control / defense / steal）

| 年齢 | base | ageMultiplier |
|------|------|---------------|
| ≤21 | +1.0 | 1.15 |
| 22-24 | +2.2 | 1.33 |
| 25-27 | +1.5 | 1.225 |
| 28-30 | +0.3 | 1.045 |
| 31-33 | -0.3 | 0.955 |
| 34-36 | -1.0 | 0.85 |
| 37+ | -2.0 | 0.70 |

### 12.4 ポジション・打順補正

前年のポジション別出場試合数 / 打順別出場試合数の**加重平均**で倍率を算出。

**ポジション補正**（getPositionGrowthBonus, 抜粋）

| ポジション | 伸びやすい能力 |
|------------|----------------|
| 遊撃 / 二塁 | speed 1.4, defense 1.4, steal 1.3 |
| 三塁 | power 1.4, meet 1.3, arm 1.2 |
| 一塁 | power 1.5, meet 1.4, eye 1.2 |
| 捕手 | defense 1.5, arm 1.4 |
| 中堅 | speed 1.3, defense 1.2, steal 1.2 |
| 右翼 | arm 1.3, power 1.2 |
| 左翼 | バランス（各1.1前後） |

**打順補正**（getBattingOrderGrowthBonus, 抜粋）

| 打順 | 伸びやすい能力 |
|------|----------------|
| 1番 | speed 1.4, steal 1.4, meet/eye 1.2 |
| 2番 | meet 1.3, speed/eye 1.2 |
| 3番 | meet 1.3, power 1.2 |
| 4番 | power 1.5, meet 1.2 |
| 5番 | power 1.3, meet 1.2 |
| 6-7番 | defense 1.2, arm/meet/power 1.1 |
| 8番 | defense 1.3, arm 1.2, speed 1.1 |
| 9番（投手枠） | 補正なし |

### 12.5 高能力値の成長減衰（重要）

| 系統 | 対象能力 | 閾値 | 減衰率 | 下限 |
|------|----------|------|--------|------|
| **技術系** | meet/power/eye/control/defense/steal | **70**(プロ級) | **5%/pt** | **10%** |
| **フィジカル系** | speed/arm/stamina/bodyStamina/recovery | 80 | 3%/pt | 10% |
| **球速** | velocity | 155km | 20%/km | 10% |

**技術系のdampFactorカーブ**

| 能力値 | dampFactor | 実効成長量（baseGrowth=1想定） |
|--------|------------|--------------------------------|
| ≤69 | 100% | 1 |
| 70 | 100% | 1 |
| 75 | 75% | 0.75 → 1 |
| 80 | 50% | 0.50 → 1 |
| 85 | 25% | 0.25 → 0 |
| 88+ | 10%（下限） | 0.10 → 0 |

- 69以下は減衰なしで通常成長
- 70以降はキャンプ基礎成長だけでは伸ばしにくい
- **覚醒分は減衰を受けない**ため、プロ級からの飛躍は覚醒が主要経路

### 12.6 メイン練習メニュー（TRAINING_MENUS）

| メニュー | 対象能力 | 主な対象選手 |
|----------|----------|--------------|
| 打撃練習 | meet, power | 野手 |
| 走塁練習 | speed, steal | 野手 |
| 守備練習 | defense, arm | 全選手 |
| スタミナ練習 | stamina | 投手 |
| 制球練習 | control | 投手 |
| 球速練習 | velocity | 投手 |
| 新球種習得 | 新変化球 | 投手 |

**新球種習得（メイン）の結果**

```yaml
大成功 (25%): Lv 65〜75（即戦力の武器級）
成功   (50%): Lv 10〜19（要育成）
失敗   (25%): 習得不可
```

### 12.7 サブ練習メニュー（SUB_TRAINING_MENUS）

メイン練習より効果は小さいが、特殊効果を持つものが多い。共通成長量は
`Math.random() < 0.4 ? (Math.random() < 0.3 ? 2 : 1) : 0` （40%で1, そのうち30%で2）。

| メニュー | 主効果 | 備考 |
|----------|--------|------|
| ランニング | speed +0〜2、体力 +1〜4（確定）、投手スタミナ +1（20%） | 安定枠 |
| 筋トレ | power/arm/speed のいずれか1つ +0〜2（ランダム選択） | パワー特化を緩和 |
| ストレッチ | 各能力 +1（各10%）、recovery +1（30%） | 広く薄く |
| 守備補強 | defense +0〜2、弱ポジ適正 +3（30%） | |
| 選球眼練習 | eye +0〜2 | 減衰適用 |
| 変化球練習 | 全変化球Lv +1〜/クール | 投手のみ、年齢補正あり |
| 新球種習得（サブ） | 成功率12%、成功時 Lv 20〜39 | 投手のみ |
| Cリード学習 | catcher.lead +1〜3 | 捕手向け |
| サブポジ練習 | 指定ポジ適正 +9〜15 | ポジション選択可 |
| フォーム改造 | **成功20%**: フォーム変更＋control +3〜5<br>**失敗**: control -1〜3 | 投手、ハイリスク |
| 打席変更 | **switch化15%** / **反対20%** / **switch→片30%**<br>失敗時ミート -1〜2 | ハイリスク |

### 12.8 派遣システム

#### 派遣先と適格条件

| 派遣先 | アイコン | 年齢上限 | 総合力上限 | 成長プロフィール |
|--------|----------|----------|------------|------------------|
| 🎓 大学野球留学 | university | **22歳以下** | **55以下** | **physical**（フィジカル系メイン） |
| 🏟️ プロ研修 | proCamp | **24歳以下** | **50以下** | **technical**（技術系メイン） |

**総合力計算**

```javascript
// 投手
overall = ((velocity - 115) × 1.5 + control + stamina / 3) / 3

// 野手
overall = (meet + power + speed + defense) / 4
```

#### 派遣枠

```yaml
DISPATCH_LIMITS:
  perTeamPerDest: 1   # 各チームは各派遣先に1人まで
  leagueTotal:    8   # リーグ全体で合計8人まで
```

#### 結果判定

```javascript
baseLeapChance = 0.40                          // 基本40%
expBonus       = min(0.25, experience / 200)   // 経験値ボーナス
leapChance     = baseLeapChance + expBonus     // 40% 〜 65%

if (random < leapChance) outcome = 'great_success'  // 飛躍 1.5倍
else                     outcome = 'success'        // 成長 1.0倍
// 失敗なし
```

#### 成長内容（派遣は減衰を受けない）

**大学野球留学**（フィジカル系 / 22歳以下 / 総合力55以下）

| 対象 | 能力 | 成長 (1.0倍) | 飛躍 (1.5倍) | 備考 |
|------|------|--------------|--------------|------|
| 投手 | 球速 | +4〜8 | +6〜12 | 上限158km |
| 投手 | スタミナ | +10〜24 | +15〜36 | 上限200 |
| 投手 | 制球 | +2〜5 | +3〜7 | 上限99 |
| 野手 | パワー | +8〜17 | +12〜25 | 上限99 |
| 野手 | 走力 | +6〜13 | +9〜19 | 上限99 |
| 野手 | 肩力 | +4〜9 | +6〜13 | 上限99 |
| 野手 | ミート | +2〜5 | +3〜7 | 上限99 |

**プロ研修**（技術系 / 24歳以下 / 総合力50以下）

| 対象 | 能力 | 成長 (1.0倍) | 飛躍 (1.5倍) | 備考 |
|------|------|--------------|--------------|------|
| 投手 | 制球 | +8〜17 | +12〜25 | 上限99 |
| 投手 | 球速 | +1〜3 | +1〜4 | 上限155km |
| 投手 | 変化球Lv | +5〜12（各球種） | +7〜18（各球種） | 各変化球ごとに成長 |
| 投手 | スタミナ | +5〜14 | +7〜21 | 上限200 |
| 野手 | ミート | +8〜17 | +12〜25 | 上限99 |
| 野手 | 選球眼 | +6〜13 | +9〜19 | 上限99 |
| 野手 | 守備 | +5〜10 | +7〜15 | 上限99 |
| 野手 | パワー | +2〜5 | +3〜7 | 上限99 |

#### 派遣中の追加覚醒

成長適用後に追加で覚醒チャンスが発生する。

```yaml
成長時(1.0倍): 20% の確率で覚醒
飛躍時(1.5倍): 30% の確率で覚醒
成長量:
  投手: 球速 or 制球 が +5〜12（multiplier適用後: 成功時5〜12 / 飛躍時7〜18）
  野手: ミート/パワー/走力 のどれかが +5〜12（同上）
上限: 球速160km、その他99
```

飛躍＋覚醒のコンボが発生すると、能力値を一気に20近く伸ばすことも可能。

#### 派遣は減衰を受けない

`resolveDispatchTraining` は `applyGrowth = (base) => floor(base * multiplier)` をそのまま加算し、
**成長減衰システムを適用しない**。既にプロ級(70+)の選手でも能力をさらに伸ばせる貴重な経路。
ただし年齢/総合力の適格条件により、**伸びしろのある若手しか派遣できない**。

### 12.9 経験値（experience）

```yaml
獲得:
  投手: 登板数 + 投球回数（1試合あたり約1〜7pt）
  野手: 試合数 + 打席数/3（1試合あたり約1〜2pt）
  フル稼働年: 約250pt

消費:
  キャンプのメイン練習実行後: experience × 0.3 を残して70%消費
  （練習経験値としてリセット）

用途:
  - 覚醒発生率: experience / 15 （%）
  - 派遣の飛躍率ボーナス: min(0.25, experience / 200)
```

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2025-01-05 | 初版作成（v25_15ベース） |
| 2026-04-10 | §12 シーズン成長システム（キャンプ & 派遣）追記 |

---

## 関連ファイル

```
REFACTORING_GUIDE.md  - リファクタリング手順書
DEVELOPMENT_GUIDE.md  - 開発ガイド
.claude/plans/        - 技術プランファイル
```

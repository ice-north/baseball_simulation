# 野球シミュレーター

## 技術スタック
Vite + React (JSX, no TypeScript), Tailwind CSS

## UIデザイン原則（必ず守ること）

### 1. 文字サイズ
- **最小サイズは `text-xs`（12px）**。`text-[9px]`・`text-[10px]`・`text-[11px]` は使用禁止
- データ表・ラベル類は `text-xs` 以上、本文は `text-sm` 以上を基本とする
- 新規UIを書くときは必ず `text-xs` を下限として意識すること

### 2. 文字色・コントラスト
- **`text-gray-400`（低コントラスト）は原則禁止**。最低でも `text-gray-300` を使う
- 補足・無効テキストは `text-gray-400` まで許容するが、読ませたい情報には使わない
- `bg-gray-900` 背景には `text-gray-100`〜`text-gray-200` を基本とする
- `bg-gray-800` 背景には `text-gray-100` または `text-white` を使う
- 薄い文字（`text-gray-500` 以下）は「意図的に読ませない装飾」以外に使わない

#### 一括是正済み（`text-gray-500`/`600` は暗い背景から全廃）
このルールは長らく守られておらず、実際には `text-gray-400`〜`600` が **1270箇所**あった。
（例: チーム選択の「戦力45〜85 主力級20% 予算100」が `text-gray-500` で事実上読めなかった）
階層を保ったまま一段ずつ引き上げてある。**順序が重要**——逆にすると 500/600 が 300 まで飛ぶ。

    1. text-gray-400 → text-gray-300
    2. text-gray-500 → text-gray-400
    3. text-gray-600 → text-gray-400   ← 400 が下限なので 500 と統合される

- ⚠ **`DraftResultScreen.jsx` だけは新聞風の明るいカード（`bg-white`/`bg-gray-100/200`）を
  持つ**ので一括対象から外してある。この画面は暗い部分と明るい部分が同居していて、
  明るいカードの上では **逆に濃くする**（`text-gray-400` → `500`）のが正解。
  行単位で振り分けてある。**新しく明るい背景の画面を作るならここと同じ扱いにすること**
- ⚠ `hover:` / `disabled:` 付きの指定は一括置換で**関係が潰れる**
  （`text-gray-600 hover:text-gray-400` が両方 400 になる等）。置換後に必ず確認する

### 3. スクロールバー抑制
- コンテナに `overflow-y-auto` や `overflow-auto` を安易に付けない
- 画面全体のレイアウトは `h-screen` + flexboxで高さを分配し、**画面外にはみ出さない**設計にする
- スクロールが必要な領域は必ず **高さを明示**（`h-64`、`flex-1 min-h-0` 等）してから `overflow-y-auto` を付ける
- `min-h-0` を忘れると flex子要素が親をはみ出してスクロールバーが出るため、flex内スクロール領域には必ず付ける

#### 想定ディスプレイは「Full HD の125%スケーリング」＝ 可視高 **730px**
一番多いのは 1920×1080（世界23% / 日本27%）だが、**高さ1080が使えるわけではない**。
2位の 1536×864 は別のディスプレイではなく **同じFHDパネルの125%表示**で、
13〜14インチのノートでは既定になっている。さらにブラウザから使えるのは
タスクバー（約48px）とタブ・アドレスバー（約87px）を引いた残り。

| パネル | 倍率 | 論理解像度 | **可視高** |
|---|---|---|---|
| 1920×1080 | 100% | 1920×1080 | 約950 |
| **1920×1080** | **125%** | **1536×864** | **約730** ← ここに合わせる |
| 1366×768 | 100% | 1366×768 | 約640 |

- **試合画面は 1536×730 に収まること**。以前は必要高834pxで、125%の人は
  「1球」ボタンが画面外に出ていた。余白を詰めて766pxまで削ってある
- ⚠ **高さは打者によって変わる**（`describeZoneProfile` が2行に折り返すと +36px）。
  余白調整だけでは固定できないので、下記の自動フィットと**セットで**成立している

#### 自動フィット（`App.jsx` の `applyFit`）
- ⚠ **かつては横幅しか見ておらず、一度も発動していなかった**。レイアウトが flex/% の
  流動幅なので `root.scrollWidth` は常にビューポート幅と一致し、条件が成立しない。
  実測で1024px幅でも zoom は1のままだった。**溢れるのは縦**
- 縦のフィットは **`data-fit-height` を持つ画面だけ**。能力ランキング（自然高1939px）の
  ように縦に読み進める画面まで縮めると、文字が小さくなるだけで何の得もない
- ⚠ **依存配列を持たせないこと**。以前は `[screenMode, managementView, gameFlowState]`
  しか見ておらず、**画面内で高さが変わっても再計算されなかった**
  （「試合開始」で掲示板と采配パネルが増えても再フィットせず57px切れていた）。
  実測は rAF で束ねてあるので1フレーム1回
- 下限 **0.7**。これ以上縮めると `text-xs`(12px) が8px台になって読めない
- 実測: 1920×950 → 1.000 / 1536×730 → 0.95 / 1366×640 → 0.87 / 1280×600 → 0.79

#### コンテンツ幅は3段だけ（`max-w-*`）
`3xl`〜`7xl` が10箇所ずつ散っていて、画面を移動するたび本文の幅が変わっていた。

| 段 | 幅 | 用途 |
|---|---|---|
| `max-w-7xl` | 1280 | 表・一覧が主役（ロスター・日程・成績・資料室・チーム運営） |
| `max-w-5xl` | 1024 | 設定・選択・カード一覧（モード選択・レギュレーション・マニュアル） |
| `max-w-3xl` | 768 | フォーム・確認（セーブ/ロード・オフシーズン） |

- `4xl`/`6xl`/`2xl` は使わない。SVGの寸法指定（`RenderBases` 等）は対象外

### 4. デザイントークン（`src/index.css` の `:root` ＋ `tailwind.config.js`）
- **アクセント色**: `var(--accent)`(cyan-400) / `var(--accent-strong)` / `var(--accent-soft)`。UIクローム・チャートに使う。**ランク色（pink/red/orange/yellow/green/blue/gray）とは役割を分離**すること
- **サーフェス**: `var(--surface-0/1/2)`（最深部→gray-900→gray-800相当）
- ⚠ **トークンは Tailwind のユーティリティとして生やしてある**（`bg-surface-2` / `text-accent`）。
  CSS変数を定義しただけの頃は実使用が1〜2箇所しかなく（対して素の `bg-gray-800/900` が392箇所）、
  トークンが有名無実だった。`tailwind.config.js` の `theme.extend.colors` で解決してある。
  **ここに色を増やさないこと**——足すのはアクセントとサーフェスだけ
- ⚠ 透明度付き（`bg-gray-800/30` 等）はCSS変数色では alpha が効かないので**素の色のまま**。
  ベタ塗りだけ `bg-surface-*` に寄せてある
- **チャート**: `var(--chart-grid)`（グリッド線・控えめ） / `var(--chart-axis)`（軸線）
- **日本語フォント**: bodyでHiragino/Yu Gothic/Noto Sans JP等のシステムJPスタックを優先（webフォント不使用）
- **数字揃え**: 桁を揃えたい表・スタッツには `.tnum`（`font-variant-numeric: tabular-nums`）または Tailwind の `tabular-nums`
  - **セルに1つずつ付けるのではなく `<table>` 自身に付ける**。継承するので全セルに効くうえ、
    列を足すたびに付け忘れる事故が起きない。日本語の選手名は数字を含まないので影響しない
  - 適用済み: 順位表・成績ランキング（`ScheduleScreen`）/ 個人成績（`PlayerStatsScreen`）/
    能力ランキング（`AbilityRankingScreen`）/ チーム運営（`CorporateManagementScreen`）/
    日程進行（`DateProgressScreen`）
  - **1個ずつのカード（「プロ輩出数 12名」等）には要らない**。縦に比較する列でないと意味がない

### 5. ボタン・タブの語彙（`.btn-*` / `.seg`。`src/index.css`）
素のTailwind色を各画面が好きに使った結果、ボタンの背景色が **13色・480箇所**に散っていた。
とくに深刻だったのが**「このタブが選択されている」状態の色が画面ごとに違う**こと
（日程=青 / 大学リーグ=緑 / 資料室=赤 / トレード=黄 / 成績=紫）。色が意味を運んでいなかった。

| クラス | 役割 | 例 |
|---|---|---|
| `.btn-primary` | 前へ進む・確定・実行（**1画面に1つ**） | シーズン開始 / 試合采配 / 保存 |
| `.btn-secondary` | 補助・切替・戻る | AIオーダー編成 / 1アウトまで / 選手交代 |
| `.btn-danger` | 破壊的（削除・解雇・戦力外） | 削除 / 解雇 |
| `.btn-warn` | 不可逆だが破壊ではない | 緊急セーブの復元 |
| `.seg` / `.seg-on` | タブ・セグメントの選択状態 | 画面上部のタブ / フィルタ |

- **余白・文字サイズは持たない**（既存の `px-4 py-2 text-sm` と併用する前提）
- **無効化は `disabled` 属性に任せる**。`cond ? 'btn-primary' : '...cursor-not-allowed'` と
  書くと無効時に背景が消えるので、**常に `btn-primary` を付けて `:disabled` に任せる**
- ⚠ **ランク色・チーム色・結果の意味色とは別語彙**。そちらは素のTailwind色のままにする
- ⚠ **`DraftResultScreen` は対象外**。抽選の赤い演出を持つ「見せ場」の画面で、
  明るいカードと同居している（文字色の一括是正で外したのと同じ理由）

#### 試合画面は 守備=amber / 攻撃=cyan（この語彙とは別）
`.seg` を持ち込まないこと。ただし**パネル内で色が逸れていた3箇所は揃えた**:
- 攻撃パネルの2行目「コース」だけ **teal** → cyan
- 守備パネルの2行目「狙い」だけ **rose** → amber、「敬遠」の **indigo** → amber
- 一発采配（盗塁・エンドラン・バント・スクイズ）の **緑と黄** → cyanの濃淡。
  緑=ボール / 黄=ストライク という**結果の意味色と衝突**していたため。
  走塁=`cyan-700/800` / 犠打=`cyan-900/950` で群は保つ
- 場面バッジ（併殺狙い / 三振狙い）の emerald・sky も同じ理由で amber の濃淡へ

#### サイドバーの選択色は1色
`SidebarButton` は `color` prop で green/yellow/blue を出し分けていたが、実際に指定していたのは
資料室の yellow 1箇所だけだった。**項目ごとに色を変えると「色＝現在地」の意味が壊れる**ので
prop ごと削除し、選択中はアクセント（`seg-on` ＋ 左のアクセントバー）に統一した。

### 6. 能力の可視化
- **配色統一**: 能力値の色は共有 `AbilityValue`（球速=(v-115)×2.5・投手スタミナ=v/2 で正規化）に集約。生の `getAbilityColor(velocity/stamina)` は常に最大色になるので**使わない**
- **レーダー**: `AbilityRadar`（`playerRadarAxes`/`teamRadarAxes`）で選手・チームの能力バランスを多角形表示。単一系列＝アクセント1色

## アーキテクチャ要点
- **TEAMS_DATA** はグローバルミュータブルオブジェクト（React stateではない）
- 変更後 `setUpdateTrigger(prev => prev + 1)` で再レンダリング
- lineup配列は **splice()で直接変更**すること（filter()で新配列を作るとスタメン増殖バグが再発する）
- 守備位置適正: `fitnessMult = 0.5 + (fitness / 100) * 0.5`（適正100=100%, 適正0=50%）

## 監督移籍システム (`src/game/managerTransfer.js`)
- **前提**: どのモードで開始しても TEAMS_DATA には全カテゴリ（独立26・社会人300・大学234）が生成され、
  選手プール（高校生/大学/リリース）も共有された「1つの日本球界」になっている。
  モードの違いは実質「どのチームを操作し、どのカレンダーで進行するか」だけ
- **移籍＝以下4点の差し替え**（新しいシミュレーション基盤は不要）
  1. `leagueConfig`（App.jsx が `userTeamName = allTeams[0]` として参照）
  2. `seasonData.settings`（カテゴリ別のカレンダー・対戦相手・試合数）
  3. `WORLD_DATA.mode` / `userLeagueId`（独立リーグは並行世界の入替も実施）
  4. `gameMode`（画面ルーティング）
- **独立リーグの入替**: 旧・自リーグを `WORLD_DATA.independentLeagues` へ戻して背景シミュ対象にし、
  新・自リーグを除外する（自分で試合を消化するため）
- **完了シーズンのデータは消さない**: standings/results/大会結果はオフシーズン画面の表示と年鑑記録に必要。
  翌年のスケジュール・順位表は `advanceToNextYear`／レギュレーション確認画面が `settings` から再生成する
- **UI**: オフシーズン画面の「🧳 監督移籍」。カテゴリ別タブ＋チーム名検索から就任先を選ぶ
- **キャリア履歴**: `WORLD_DATA.managerCareer`（year/from/to/category）に記録・セーブされる

## 試合画面のデザイン方針（`App.jsx` RENDER）
**ベースは「現地で試合を見ている感じ」**。その上で情報はスポーツナビのように
目に入りやすくする。

- **電光掲示板は世界の中の物体**として扱う。オレンジ単色＋`textShadow` のグローは
  意図的な演出なので**落とさない**。ただしヘッダー(`TEAM/安/失`)と消灯セグメントは
  `text-orange-600` だとコントラストが足りないので `400`/`500` にしてある
- 掲示板の下段は実物に倣って **BSO / 塁 / 投手(球数) / 打者(打順・打率) / 球種・球速**
- **チーム色分けは使わない**。以前は アウェイ=赤 / ホーム=青 と 投手=青 / 打者=赤 が
  併存しており、**表と裏で色の意味が反転**していた（裏の攻撃ではホーム=青のチームの
  打者が赤になる）。掲示板の外は **守備=amber / 攻撃=cyan** に統一し、
  采配パネルの色と同じ語彙にしてある。選手名は白(`text-gray-100`)
- **結果の意味色は1系統**: 緑=ボール / 黄=ストライク / 赤=アウト / 白=安打。
  コース図・BSOランプ・打席結果バッジで共有する
- **数字は `tabular-nums`**。打率・球速・EVなど桁を揃えたいものは全て

### 左右のメンバー表は「列」を固定する
打順を縦に読んで比較する表なので、桁数や文字数で位置がずれてはいけない。
- **打席結果は3文字幅に固定し、2文字は均等割り付けで埋める**
  （`formatAtBatResult` + `textAlignLast: 'justify'`）。`addAtBatResult` に渡る
  文字列は 安打/二塁打/投ゴロ/ライナー … と2〜4文字ありバッジの幅が揃わず、
  数が増えると折り返して行がガタつく。**1文字まで削ると何のことか分からない**ので、
  読める長さを保ったまま幅だけ揃える。元の表記は `title` に出す。直近5打席まで
- **凡打はスコアブックと同じ表記にする**（遊ゴロ / 中飛 / 右直）。
  従来は `description` から「アウト」を削るだけで、ゴロは3文字・ライナーは4文字と
  バラバラだった。`judgeFielderReach` の `fieldingPosition` を out の結果に通して
  守備位置を頭に付ける
- **打席結果は2行目に置く**。1行目に並べると選手名が `truncate` で切れる。
  1行目＝打順・守備位置・名前・調子・左右 / 2行目＝成績＋打席結果、と分ける。
- **打席結果は左から右へ増やす**。右寄せ(`justify-end`)にすると打席が増えるたびに
  既存のバッジが左へずれて落ち着かない。先頭から並べれば N打席目は常に同じ位置に出る。
  `slice` も**先頭から**にすること（`slice(-6)` だと6打席目で全部1つ左へずれる）
- **成績は打率・本塁打・打点の3つだけ**を固定幅の右寄せで詰めて置く
  （`w-8/w-9/w-10 text-right` + `tabular-nums`）。等分グリッドで横いっぱいに
  広げると3つが離れて1かたまりに見えない。安打数は打率と重複するので出さない
- **チーム名・選手名は `truncate min-w-0`**。長い名前が2行になると行の高さが揃わない
- ⚠ **`players.sort()` は state配列を破壊する**。必ず `[...players].sort()` にすること
  （試合終了サマリーで実際にやってしまっていた）

### 対戦カードに配球の材料を載せる
コース図を置いても、それを読むための情報が周りに無かった。以下を試合画面に出す。
- **打者の型**（A型直球対応 / B型コース狙い …）と `describeZoneProfile` の
  「内角に弱い」。どちらも文字なので対戦カードに置く
- **コース適性は投球コース図に重ねる**（`PitchZonePlot` の `heat` prop、赤=得意/青=苦手）。
  一度は対戦カードに5×5のミニヒートマップを別に置いたが、**2枚の図を見比べることになって
  読みにくかった**。投球位置と同じ図の背景に敷くのが正解
- ⚠ `zoneHeatmap` は**打者基準**（col 4 = その打者の内角）を返すので、
  投球位置と同じ規則で**左打者だけ列を反転**してから描く
- ⚠ **重ねる塗りはマーカーより必ず薄く**（`HEAT_MAX_ALPHA = 0.30`）。
  濃くすると結果の意味色（緑/黄/赤/白）が読めなくなる
- **捕手の名前・リード・守備**。このゲームで配球を決めているのは捕手で、
  リードは防御率 -0.32 相当の価値があるのに試合画面のどこにも出ていなかった

### カウントはコース図の上にも置く
掲示板のBSOはカードの上（約200px上）にあり、配球を決めるたびに目線が往復していた。
コース図の直上に小さな B/S/O ランプを重複して置いてある。掲示板側は現地感のために残す。

## 采配モードの画面構成（`App.jsx` RENDER）
- **3カラムの比率は試合前後で変える**。試合前は選手を並べ替えるので選手欄重視の
  `5fr 3fr 5fr`、試合中は中央（掲示板・対戦カード・コース図・ログ・采配）が主役なので
  `3fr 5fr 3fr`。左右のメンバー表は試合中は参照用
- 対戦カード: 守備側（投手＋捕手） / VS / 攻撃側（打者＋型＋コース適性）
  → **投球コース（168px・上にBSO）｜ 投球ログ（残り幅いっぱい）** を横一列
  → **投球結果**（ログの下。1行に畳む。球種・球速は掲示板とログにあるので繰り返さない）
  → **采配（攻撃 or 守備）** → **進行ボタン** の順で一番下
  （ログは高さ150pxを明示してから `overflow-y-auto`。凡例は場所を食うので撤去）
- 采配は**いま操作できる側だけを出す**（従来は両方並べて使えない側を薄く表示していた）
  - 攻撃中: 打撃方針・盗塁/エンドラン/バント/スクイズ・狙い球（球種）・コース
  - 守備中: 配球（球種）・狙い（勝負/際どく/誘い）・敬遠・場面バッジ
- 進行ボタン（1球 / 1アウトまで / 試合終了まで / AI / 選手交代）は区切り線の下に分離
- **守備シフトは廃止**。段階5で打球方向モデルを作った後も、シフトは
  「単打の35%をアウトに書き換える」後付け処理のままで整合していなかった。
  守備は全て基本配置とする（`defenseShift` は state ごと除去）

## ⚠ 采配モードはハーネスで実行されない（`npm run check:scope`）
`App.jsx` の `simulateSinglePitch` は React コンポーネントの中に閉じているため、
**sim-harness では一度も実行されない**。自動シミュレーションだけがテストされている
状態なので、采配モードのスコープ由来のバグはブラウザで踏むまで分からない。

実際に起きた事故: `simulateSinglePitch` から `currentBatter`
（`simulatePitch` のローカル）を参照し、投球のたびに ReferenceError で
試合が進まなくなった。**ビルドは通り、season-check も全項目PASSしていた**。

`tools/sim-harness/scope-check.mjs` が `@babel/parser` で全 `src/` を走査し、
未定義の識別子を検出する（`npm run check` に組み込み済み）。
- 別の関数のローカル変数を参照したら即FAILする
- `typeof x === 'function'` のような未宣言ガードは警告（到達しない分岐）
- **采配モードに手を入れたら必ず走らせること**

この検査で見つかった既存バグ2件:
- `dateProgression.js` が `getNextGame` を import しておらず、
  「次の試合まで進む」が ReferenceError でクラッシュしていた
- `teams-data.js` の `typeof createDefaultPlayers === 'function'` 分岐は
  ESモジュールでは必ず false。一度も実行されていない到達しないコードだった（除去）

## 主要ファイル
- `src/App.jsx` (~5130行) - メインアプリ、試合シミュレーション、画面遷移（下記セクション参照）
- `src/game/autoSimulation.js` (~2430行) - 自動シミュレーション・buildDefense
- `src/game/aiManager.js` (~640行) - 監督AI（自動投手交代・代打・守備固め・盗塁判定）
- `src/game/lineupGenerator.js` (~625行) - AIオーダー編成・投手ローテーション生成
- `src/game/gameControls.js` (~185行) - resetGame・multiPitch・simMode
- `src/game/gameSetup.js` (~750行) - setupManagedGame・handleManagedGameEnd
- `src/game/saveSystem.js` (~470行) - セーブ/ロード/エクスポート/インポート
- `src/game/seasonProgress.js` (~420行) - 日程進行ハンドラー
- `src/simulation-logic.js` (~675行) - 物理演算（打球・投球）
- `src/components/ScheduleScreen.jsx` (~760行) - 日程/順位表/成績ランキング
- `src/components/LineupSettingScreen.jsx` (~2720行) - スタメン/投手起用/守備分析の3タブ
- `src/components/DateProgressScreen.jsx` (~4510行) - 日程進行画面
- `src/components/ManagementScreen.jsx` (~635行) - 管理画面ルーター
- `src/components/GameFlowScreens.jsx` (~570行) - ゲームフロー画面群
- `src/components/UniversityScoutScreen.jsx` (~730行) - 大学スポーツ推薦スカウト画面
- `src/components/GameUIComponents.jsx` (~505行) - Sidebar・RenderBases・AccordionSection
- `src/components/` - 各画面コンポーネント（Camp, Tryout, OffSeason, Draft等）
- `src/season/` - シーズン管理（スケジュール生成, 日付進行, トライアウト, 年間進行）
- `src/season/universityPool.js` (~1425行) - 大学プール（高卒世代生成・進路振分・ランク別成長・4年間成長・卒業）
- `src/season/yearProgressionSystem.js` (~2780行) - 年間進行・キャンプ・オフシーズン処理
- `src/corporate/corporateInit.js` (~1700行) - 社会人/独立リーグ初期化・チームランク変動
- `src/corporate/scoutingSystem.js` (~2005行) - 社会人モード入退団（退団処理・スカウト候補生成・AI自動処理）
- `src/university/universityTeamsData.js` (~455行) - 大学チームデータ（27リーグ234校（2部制12リーグ×12校＋1部制15リーグ×6校）、ランク別成長倍率定義）
- `src/university/universityLeagueManager.js` (~495行) - 大学リーグ戦シミュレーション（27リーグ春季・秋季、スケジュール生成・試合シミュレーション・順位表管理）
- `src/data/playerNames.js` (210KB) - 姓3200件+名3000件の重み付き名前DB
- `src/players.js` - 初期選手データ
- `src/teams-data.js` - チームデータ

## App.jsx セクション構成（トークン節約のため必要なセクションだけ読むこと）

App.jsxは大きいファイルなので、作業に関連するセクションだけを `offset` + `limit` で読むこと。
**※分割作業は完了しました。以降は通常の開発（機能追加・バグ修正）を行うこと。**

| セクション | 行範囲 | 内容 |
|---|---|---|
| IMPORTS | L1-57 | import文 |
| APP_STATE | L58-340 | アプリ全体のstate定義（試合状態、チーム状態、セーブ等） |
| GAME_HANDLERS | L341-595 | 成績更新・打順変更・選手交代ハンドラー |
| AI_MANAGER | L596-610 | → `aiManager.js` に抽出済み（ラッパーのみ） |
| THROW_PITCH | L611-2030 | throwPitch（投球シミュレーション本体、最大セクション） |
| GAME_CONTROLS | L2031-2080 | → `gameControls.js` に抽出済み（ラッパーのみ） |
| GAME_SETUP | L2081-2100 | → `gameSetup.js` に抽出済み（ラッパーのみ） |
| SEASON_PROGRESS | L2101-2110 | → `seasonProgress.js` に抽出済み（ラッパーのみ） |
| MANAGEMENT | L2111-2112 | → `ManagementScreen.jsx` に抽出済み |
| GAME_FLOW | L2113-2130 | → `GameFlowScreens.jsx` に抽出済み |
| RENDER | L2131-END | メインreturn（試合画面UI） |

### 編集作業ガイド
- **試合ロジック修正**: `src/App.jsx` THROW_PITCH (L611-2030) + `src/game/aiManager.js`
- **画面遷移修正**: `ManagementScreen.jsx` + `GameFlowScreens.jsx`
- **UI修正**: `src/App.jsx` RENDER (L2131-END) + `GameUIComponents.jsx`
- **日程進行修正**: `src/game/seasonProgress.js`
- **選手交代修正**: `src/App.jsx` GAME_HANDLERS (L341-595)
- **試合セットアップ**: `src/game/gameSetup.js`
- **監督AI修正**: `src/game/aiManager.js`

## ゲームフロー
```
【独立リーグモード】
NEW GAME → レギュレーション設定 → トライアウト(24人ドラフト) → キャンプ
→ 4月(高校3年生5000人生成) → レギュラーシーズン(日付進行で自動消化)
→ プレーオフ → ドラフト(10月:チーム選手+高校生からNPB指名)
→ 契約更改 → トライアウト → オフシーズン(表彰/引退/高校生ランク別振り分け/大学プール処理)
→ Year 2+(キャンプ...)

【社会人モード】
NEW GAME → 企業チーム選択 → キャンプ
→ 4月(高校3年生5000人生成+スカウト対象に) → レギュラーシーズン(日付進行)
→ 都市対抗予選(6月) → 都市対抗本戦(8月)
→ ドラフト(10月:高校生+チーム選手からNPB指名) → 日本選手権(11月)
→ 退団(11/9) → スカウト入団(11/10) → オフシーズン(高校生ランク別振り分け)
→ Year 2+(キャンプ...)

【大学モード】
NEW GAME → 大学チーム選択 → キャンプ
→ 4月(高校3年生5000人生成) → 春季リーグ(4-6月) → 全日本大学選手権(6月)
→ 秋季リーグ(9-11月) → 明治神宮大会(11月)
→ ドラフト(10月) → スポーツ推薦スカウト(11/10) → オフシーズン(卒業+入部+振り分け)
→ Year 2+(キャンプ...)
```

## 高校生・大学プールシステム (`src/season/universityPool.js`)

### 高校データ（`src/data/highSchoolData.js`）
**996校**・S〜Fの7ランク・47都道府県（1県あたり 最少13 / 中央18 / 最多41）。

- ⚠ **校名は実在のものだけを置くこと**。一度「市郡名 × 接尾辞（工業・商業・東…）」で
  機械生成したが、「高槻工業」「富田林工業」のような**実在しない校名**が混ざった。
  この手の合成はやらない
- ⚠ ネットワークは egress プロキシで塞がれており、`ja.wikipedia.org` などの
  一覧ページは WebFetch でも取得できない（`EGRESS_BLOCKED`）。校名を足すときは
  手で書き出すことになるので、**WebSearch で抜き取り検証すること**
- **足すのは D/E/F だけ**。上位ランク（S〜C）は元の334校で足りている
  （1校あたり2〜4人）。プレイヤーが名前を見る有望株はそちらから出る。
  ランクは県ごとに野球での知名度順で D → E → F

#### 才能と校ランクは上下3ランクまで混ざる
以前は各行に「才能の上限」があり、**才能Cは C校より上へ行けなかった**。
つまり S校の選手は必ず A才能以上で、**校名を見れば才能の下限が確定で分かって**いた。
1ランク上（既存の 2〜3）に加えて **2ランク上=1.2 / 3ランク上=0.4** の裾を足してある。

- **下向き（弱小校の逸材）は制限しない**。S才能の1%はF校から出る。
  甲子園の知名度システムはこれを前提にしている
- 4ランク以上は0のまま。S校に才能D・Eは出ない

**校ランクごとの才能構成（才能の出現率で重み付け）**

| 校 | 才S | 才A | 才B | 才C | 才D | 才E |
|---|---|---|---|---|---|---|
| **S** | 47.0% | 31.7% | 12.7% | **8.6%** | 0% | 0% |
| A | 15.9% | 51.0% | 13.6% | 11.1% | 8.6% | 0% |
| B | 5.1% | 23.7% | 44.9% | 8.9% | 13.9% | 3.6% |
| C | 1.5% | 8.4% | 34.6% | 37.0% | 12.4% | 6.1% |
| D | 0.4% | 2.0% | 16.6% | 34.4% | 41.1% | 5.4% |
| E | 0.2% | 0.9% | 6.0% | 23.3% | 51.0% | 18.7% |
| F | 0.0% | 0.2% | 1.5% | 8.5% | 42.6% | 47.2% |

⚠ B校で 才D(13.9%) が 才C(8.9%) より多いのは**才能の出現率の差**（D 40% 対 C 16%）。
裾の重みは C の方が大きいので、式としては正しい。
**能力値そのものは一切変わらない**（校名が変わるだけ）。甲子園の優勝校は
40年で S校21回 / A校17回 / B校2回 と、強豪校の優位は保たれている。

#### ⚠ `assignHighSchool` の重みは1ランク1回だけ
`B`〜`E` 行の末尾が `['D', n]` と**重複していた**（`S`/`A` 行は S〜F を1回ずつ）。
重みが 4→15→37→66 と才能が下がるほど増える形なので「最も弱い校」の枠のはずで、
ランク文字の誤記。**才能E層の68%がDランク校**へ流れており、Dだけ1校あたり
40.6人・Fが26.9人という非単調な分布になっていた。`F` に統合済み。

**1校あたりの所属人数（高校生5000人）**

| | S | A | B | C | D | E | F | 最多の校 |
|---|---|---|---|---|---|---|---|---|
| 修正前（335校） | 2.9 | 2.0 | 2.3 | 3.6 | **40.6** | 19.8 | 26.9 | **57人** |
| **修正後（996校）** | 1.9 | 1.9 | 2.4 | 3.7 | 5.3 | 5.3 | **5.7** | **14人** |

上位300人（ドラフト候補相当）の出身校は **165校 → 258校**、同じ校から最多
**7人 → 3人**。知名度は「球速上位50人 9.3 / 投手全体 5.1」で、
出場が希少になったぶん**能力のある選手がより際立つ**（修正前は 11.2 / 5.8）。

- **高校3年生生成（4月）**: `generateAprilHighSchoolClass(year)` で5000人を `highSchoolPool` に生成（`HIGH_SCHOOL_CLASS_SIZE = 5000`）
  - **体格**: large(25%)/medium(50%)/small(25%)。体格がパワー・肩・走力・守備等に影響
    - 大柄: パワー+6, 肩+5, 走力-5, 守備-3 / 小柄: 走力+5, 守備+3, 盗塁+4, パワー-5
  - **能力ベースの投手決定**: 肩力が高い子ほど投手になる確率が上がる（全体~40%）
    - `pitcherChance = clamp(0.12 + baseArm * 0.007, 0.10, 0.75)`、左投げは+0.10
  - **ポジション決定**: 体格・肩力・走力で重み付き確率選択
    - 大柄+鈍足→一塁/左翼、小柄+俊足→遊撃/二塁/中堅、強肩→捕手/右翼
  - **確率分布ベース**: 全能力を正規分布で生成、ハードキャップなし
    - 球速: N(126,9)+ランクボーナス、左投手-3km。160km≈10年1人、155km≈年2-3人。最低110km
    - **球速上限=肩力連動**: `cap = 130 + (arm-50)×0.7` → 肩50=130km, 肩80=151km, 肩100=165km。生成・成長すべてに適用
    - **球速キャッチアップ補正**: 肩力capと現在球速のギャップが大きいほど球速成長にボーナス。`mult = min(3.0, 1.0 + max(0, gap-5)×0.05)`。肩90/球速125(gap33)→×2.4倍。投げ方改善の表現
    - パワー: N(15,12)+ランク+体格+ポジション補正。75前後が高卒の最高値
    - 守備: N(38,13)+ランク×0.6+体格+ポジション補正。フィジカル系のため高校時点でプロ級到達可能
    - 肩力/走力: N(38/35,14)+体格補正。σ拡大で突出した素材が出やすい
  - **ポジション別能力補正**: 一塁(パワー+5,守備-4)、遊撃(守備+5,パワー-3)、中堅(守備+4,盗塁+3,パワー-3)等
- **NPBドラフト（10月）**: `processNPBDraft(allTeams, gameYear)` で全ソースを統一評価、グローバルTop-N指名
  - 高校/大学3-4年生/社会人/独立の全候補をスコア順ソート → 上位100〜140名を指名
  - 比率は選手の質から自然に決まる（目標: 高校30%, 大学35%, 社会人20%, 独立14%）
  - スコア = 能力値×将来性倍率 + 年齢ボーナス(18歳+33→22歳+5→28歳-50) + 成長力ボーナス(能力比例) + 知名度ボーナス(fame×0.3)
  - **年齢別評価傾向**: 高校生(≤19)=素材型(球速/パワー/足/肩)重視、社会人(≥22)=技術型(制球/ミート/守備)重視
  - 投手(高校): 球速×1.5, 140km+×4.0, 150km+×5.0, 制球×0.7, 変化球×0.5, 肩力×0.3。アンダー-20/サイド-10
  - **変化球は種類込みで評価する**（`pitchOwnValue`）。以前は「最高レベル1つ×重み
    ＋本数ボーナス」で**球種の違いを見ていなかった**。実測では同じLv100でも
    カーブ1.16 対 ツーシーム0.72 と 0.44 違う。捕手の球種スコアと**同じ回帰係数**
    （type × level のグリッド実測）を流用し、物差しを二重に作らない。
    2球種目以降は逓減（1.0 / 0.55 / 0.30 / 0.18 / 0.10）。
    スケール29.4 は**プール平均が従来と一致する**よう合わせてあり、
    指名の構成比は動かさず投手間の順位だけが変わる
    - 実測: 同一投手（18歳/142km/制球55）で Lv70 の持ち球だけ替えると
      カーブ222 / フォーク220 / シンカー216 / スライダー215 / ツーシーム213pt
  - **捕手のリードを守備と同じ重みで評価する**。以前は捕手も
    meet/power/eye/speed/defense/arm でしか見ておらず、**リードが完全に盲点**だった
    （ドラフト評価との相関 **-0.074**、上位30人の平均リード43.4 が全体49.2 を
    下回る逆転が起きていた）。実測の1点あたりの価値は リード -0.0044 /
    捕手守備 -0.0041 とほぼ同じなので同じ重みで足す。修正後の相関 **+0.186**
    - ⚠ **平均50からの差で足すこと**。絶対値だと全捕手に一律 +34pt 乗り、
      上位120人に入る捕手が 14→19人（実NPBは約8%）に膨らむ
  - ⚠ **出どころ(`deception`)と回転(`spinRate`)は意図的に評価に入れない**。
    ラジャーガンで測れない資質はスカウトにも見えない、という切り分け。
    無相関のまま（0.00 前後）にしてあるので、出どころの良い投手は
    **下位指名から出てくる掘り出し物**になる
  - ⚠ **既知の偏り: 投手が指名されにくい**。評価点の平均が 投手139 対 野手184 で、
    実際の指名は 投手23% / 捕手9% / 野手67%（実NPBは概ね 投手55% / 捕手8% / 野手37%）。
    2つの分岐でスケールが揃っていないのが原因で、**今回の変更より前からある**（未修正）
  - 投手(社会人): 球速×0.9, 140km+×2.5, 150km+×3.0, 制球×1.4, 変化球×1.0, スタミナ×0.35。アンダー+8/サイド+5
  - 投手(大学): 球速×1.1, 140km+×3.0, 150km+×3.5, 制球×1.1, 変化球×0.8（中間値）
  - 野手(高校): ミート×0.6, パワー×1.4, 選球眼×0.2, 走力×0.8, 守備×0.2, 肩力×0.6
  - 野手(社会人): ミート×1.3, パワー×0.8, 選球眼×0.8, 走力×0.3, 守備×0.7, 肩力×0.2
  - 野手(大学): ミート×1.0, パワー×1.0, 選球眼×0.5, 走力×0.4, 守備×0.4, 肩力×0.3（中間値）
  - 成長力ボーナス: 現在能力に比例するabilityFactorを乗算（素材がなければ成長力だけでは評価されない）
  - 将来性倍率: 18歳×1.22→22歳×1.0（若い選手の能力を伸びしろ込みで評価）
  - 大学生が指名されたらuniversityPoolから除去（残りは卒業後にreleasedPoolへ）
- **ランク別進路振り分け（オフシーズン）**: `distributeHighSchoolGraduates(enrollYear)` でドラフト漏れをS→A→B→C→Dの順に配分
  - 各大学ランクの1チームあたり受け入れ数: S=15名, A=13名, B=11名, C=9名, D=7名
  - 大学チーム数×受け入れ数で各ランクの大学スロットを決定（S:150, A:286, B:517, C:864, D:413）合計2230名
  - 社会人: 社会人127名程度、独立: スロットベース83名+即戦力志向型40名程度
  - **5000人の進路内訳（概算）**: NPB指名20名(0.4%)、大学2230名(44.6%)、社会人127名(2.5%)、独立123名(2.5%)、引退2500名(50%)
  - **即戦力志向型独立直行**: スコアランキング18〜58%帯でgrowthPotential≥1.05の選手が確率で独立を選択（最大50名）
- **大学在学**: `universityPool` にグローバル保持。4年間（or 22歳）で卒業。在学中は毎年ランク別成長処理
  - 入学ブーストなし（4年間の年次成長に統合済み）
  - 各エントリに `universityRank` を保持。成長倍率: S=1.55, A=1.35, B=1.15, C=1.00, D=0.85
- **卒業後**: `releasedPlayersPool` に追加され、トライアウト/スカウト候補として供給
- **スカウト対象**: 高校生プール（4月〜）、大学3-4年生、リリースプールすべてがスカウト候補
- **セーブ/ロード**: `serializeUniversityPool()` / `deserializeUniversityPool()` で大学+高校生プール両方を保存・復元
- **新規ゲーム時**: `clearUniversityPool()` + `clearHighSchoolPool()` でクリア

### 投手と野手の評価スケール（`src/game/playerValue.js`）
**トレードとドラフトで別々の評価式を持っていた**（`TradeScreen.jsx` にローカルな
`getPlayerValue`）。式が違えば等価交換にならないので、能力点を
`draftAbilityScore` に一本化し、両方がそれを使う。**独自の評価式をもう1つ書かないこと**。

#### メイン／サブに分けて、群ごとの偏差値で比べる
| | メイン（スカウトが最初に見る） | サブ（支えるフィジカル・付随） |
|---|---|---|
| 投手 | 球速・制球・**変化球**（`pitchOwnValue`） | スタミナ・体力・素材としての肩・フォーム |
| 野手 | 打撃（ミート/パワー/選球眼）・守備・走塁（捕手は**リード**も守備の一部） | 肩・体力・守備の幅 |

`raw = main × 1.0 + sub × 0.6` を **ポジション群（投手/捕手/野手）ごとの偏差値**に
直してから 3倍する（平均的な選手が150）。将来性は `ageBonus`/`potentialMult` が別に見る。

**なぜ偏差値か** — 素点のスケールを倍率で揃えようとして2通り失敗した:
1. 実測の 1pt あたりの価値（野手0.0354 / 投手0.0206 ＝ 野手が1.72倍）を
   **絶対値に掛ける**と、ゼロ点が投打で対応しておらず上位120人が**投手94%**
2. **平均を引いてから掛ける**と、`draftAbilityScore` が年齢帯で別ウェイトを使う
   （球速×1.5 対 0.9 …）ため素点のスケールが年齢帯で違い、
   ソース構成が壊れる（**高校62%→4%**）

偏差値ならゼロ点も年齢帯のスケール差も関係なくなる。

#### ⚠ 平均は群共通・σは年齢帯ごと
- **平均を年齢帯別にしてはいけない**。各帯の平均が50になり、ソース構成が
  `ageBonus` だけで決まって高校生に偏る（実測 **高校84%**）
- **σは年齢帯別にすること**。高校生投手のσは19、高校生野手は25と幅が違い、
  群共通のσだと高校生投手だけ偏差値が伸びない（上位120人の投手が
  プール比率42%に対して**20%**しか入らなかった）
- 成長力ボーナスの倍率（`abilityFactor`）も**偏差値で見る**こと。素点で割ると
  投手(平均81)と野手(平均113)で倍率が変わり、揃えたスケールがまたずれる

**結果**

| | 変更前 | 変更後 | 実NPB |
|---|---|---|---|
| 指名の投手シェア | 23% | **41%** | 約55% |
| 　　捕手 | 9% | 7% | 約8% |
| 　　野手 | 67% | 53% | 約37% |
| 指名ソース（高校/大学/社会人/独立） | 61/13/12/14% | **66/13/10/11%** | — |
| トレード評価の平均（投手/捕手/野手） | 投打で別スケール | **237.6 / 234.9 / 236.6** | — |

#### ロスター需要は別レイヤーで表現する（`DRAFT_DEMAND`）
純粋な価値で並べると 投手39% / 捕手3% / 野手58% になる。
実NPBが投手を多く獲るのは1人あたりの価値が高いからではなく、
**1軍に投手が11〜13人必要**で、**野手転向を前提にした指名**もあり、
**捕手は2人必要だが母数が少ない**という**ロスター需要**の話。
**価値の物差しを歪めて寄せてはいけない**ので、需要は加点として別に持つ。

- `DRAFT_DEMAND = { P: 18, C: 17, F: 0 }`。実測 **投手48% / 捕手7% / 野手45%**
  （6回の指名・計583名。実NPBは概ね 投手55% / 捕手8% / 野手37%）
- ⚠ **ドラフト（と将来性を見る画面）にだけ掛けること**。トレードの等価交換は
  `deviationValue` をそのまま使う——需要を混ぜると「同じ数値なら等価」が崩れる

#### 球速の重みは140km未満だけが足りていなかった
実測 1km = 0.0512 防御率、投手ブランチ平均 1pt = 0.0206 なので、
価値に見合う重みは **2.5 pt/km**。140km以上は閾値ボーナスで 3.4〜5.5 と
足りていたが、**140km未満の傾きが 0.9〜1.5** しかなかった。
独立リーグの投手は大半が120〜135kmなので、**実際に効くのはそこ**。
`velBase` を 高校3.4 / 大学2.8 / 社会人2.5 に引き上げた。
評価と球速の相関 **0.754 → 0.907**、上位50人の平均球速 138.8（全体120.9）。

#### 左投手はプロ候補の「線」が低い（`LEFTY_VELOCITY_EDGE = 3`）
実際のスカウトは「右なら145km、左なら140km」を目安にする。
幅の決め方を3通り試した（高校生投手の上位100人に占める左の割合。プール約30%）:

| | 上位100人の左投手 |
|---|---|
| +5km を全球速帯に | **52%**（効きすぎ） |
| +5km を閾値だけに | **18%**（足りない。左は生成時点で-3kmされている） |
| **+3km を全体に** | **27%** ≒ プール比率 |

+3 は生成時の左投手ペナルティ(-3km)をちょうど打ち消す値でもある。
実測で上位に入る左投手は右より **2.3km 遅い**（134.1 対 136.4）ので、
「左は少し遅くても候補に挙がる」という関係は出ている。

⚠ 球速の重みを変えたら `VALUE_DIST` / `BAND_SD` を**必ず測り直すこと**
（投手のσが 34 → 46 に変わった）。

## 選手のステップアップを追う3階層（甲子園 / 注目リスト / プロ）
「アマチュア野球のゲーム」という位置づけを保ったまま、選手が階層を上がる物語を
見えるようにするための仕組み。**いずれもプレイヤーは操作しない**。

### 夏の甲子園（`src/season/koshien.js`）
- 高校生プールは既に `highSchool = { name, rank, pref }` を持ち**996校**・47都道府県に分布する。
  この構造をそのまま使い、**都道府県予選 → 甲子園本戦**を8月に消化する
- 校の戦力 = 校ランクの地力 + エースの出来 + 打線の厚み。
  **校ランクの幅は意図的に狭い（S52〜F24）**。広げると結果が校ランクだけで決まり、
  エースや主砲の能力が勝ち上がりに反映されなくなる
- 勝ち上がりに応じて所属選手の `fame` が上がる（優勝+12 / 出場+3、エース・主砲は上乗せ）
  → ドラフト評価(`fame × 0.3`)とスカウト発見率に直結する
- 実測: 球速上位50人の平均知名度11.2 に対し投手全体5.8（能力のある選手が有名になる）
- センバツは対象外。高校生プールの生成が4月なので同学年が存在するのは夏だけ

### 注目選手リスト（`src/game/watchList.js`）
- `WORLD_DATA.watchList` に **playerId だけ**を持ち、表示時に全プールを横断検索して現在地を解決する
  - 選手オブジェクトはプール間を移る際に参照が張り替わるため、**実体を握ると必ず壊れる**
  - 探索順: 高校生プール → 大学プール → TEAMS_DATA各チーム → npbAlumni → リリースプール → 消息不明
- 甲子園の結果画面と推薦スカウト画面の ☆ から登録。**スカウトの「注目」と連動**させてある
  （交渉率を上げる `_watching` と別概念にすると「注目」が2つ存在して混乱するため）
- 登録時のスナップショットを保持し、「球速 128→145」のように伸びを表示する

### 教え子のプロキャリア（`src/game/npbCareer.js`）
- ドラフトで送り出した選手(`team.npbAlumni`)は能力スナップショットだけが残り、
  その後が記録されていなかった。指名が「巣立ち」ではなく「消滅」になっていた
- `advanceToNextYear` の冒頭で毎年1シーズン進める: 一軍/二軍判定 → 成績生成 → 加齢 → 引退判定
- **総合力の基準は本編と同じ能力値**: 投手 球速146/制球60/スタミナ85 → 約68、
  野手 ミート58/パワー55/選球眼52 → 約69。一軍定着58 / レギュラー68
- 成績も実データ基準: 総合70で防御率3.60・打率.265。総合80で2.80・.300
- **投手の役割は初回で固定する**。毎年抽選すると「通算151勝106セーブ」が生まれる
- **二軍が3年続くと戦力外**。これが無いと8年以内の脱落が2%しか起きない
- 実測（ドラフト上位相当100人）: 一軍到達41〜49% / 5年以上一軍40〜48% / 8年以内引退42%
- 表示は資料室の「OB名鑑」タブ（現況＋プロ通算）。**プロは操作させない**——
  遊べるようにするとドラフトが単なる移籍になり、送り出す重みが失われる

## 知名度システム (`player.fame`)
- **概要**: 0〜100の通算蓄積値。活躍するほど上がり、NPBドラフト評価とスカウト発見率に影響
- **蓄積タイミング**:
  - `recordAwardsToPlayers()`: シーズン終了時 — タイトル1位: +15、タイトル2位: +8、規定到達: +2
  - `applyUniversityFame()`: 大学年次更新時 — リーグ順位・大学ランク・学年・能力で毎年加算
    - 大学ランク基礎: S=+4, A=+3, B=+2, C=+1, D=+0
    - リーグ順位(1部): 1位+5, 2位+3, 3位+1。2部所属は-2
    - 学年補正: 3年生以上×1.5倍、2年生×1.2倍
    - 能力ボーナス: 150km+投手+3, 145km++1, パワー60++2, ミート60++1, 俊足70++1
- **NPBドラフトへの影響**: `checkNPBDraftEligibility()` で `fameBonus = fame × 0.3`（最大+30pt）
  - 能力があっても無名(fame=0)だとドラフト漏れ → 大学/社会人/独立でブレイク → 翌年以降に指名
- **スカウト発見への影響**: `generateScoutCandidates()` で知名度が低い選手は発見されにくい
  - `discoveryPenalty = -(100-fame)/100 × (100-scoutEye)×0.3`
  - 無名の原石(fame=0)はscoutingEyeが高いスカウトだけが見つけられる
  - 有名選手(fame=100)はどのチームでも候補に挙がる

## チームランク変動システム (`src/corporate/corporateInit.js`)
- **対象**: 社会人(300チーム: 企業93+クラブ208、初期化は179チーム)・独立リーグ(5リーグ26チーム)・大学(234校) すべてのチーム
- **注目度(reputation)**: 0〜100の値。成績に応じて毎年変動。ランク昇降格の判定基準
  - 初期値: S=85, A=65, B=40, C=20, D=5
  - 年間減衰: -3pt（実績なしなら自然低下）
- **昇格閾値**: S≥75, A≥55, B≥32, C≥15
- **降格閾値(ヒステリシス)**: S<60, A<40, B<22, C<8（急激な変動を防止）
- **注目度の獲得源（社会人）**:
  - リーグ戦: 1勝+0.5, 優勝+3
  - 全国大会: 1勝+2（順位ボーナスなし）
  - プロ輩出: 1名+4
- **注目度の獲得源（独立リーグ・大学）**:
  - リーグ順位: 1位+5, 2位+3, 3位+1, 4位以下+0（勝利数ボーナスなし）
  - プロ輩出: 1名+4
- **更新タイミング**: シーズン中は2ヶ月ごと（6月/8月/10月）に成績差分を反映。年度末に自然減衰(-3pt)を適用してからランク判定
- **WORLD_DATA内の他リーグ大学**: 順位表から簡易計算してランク変動を適用
- **ランク判定は Eloスコア(`rankingScore`) の全体パーセンタイル**（S=上位5%, A=6-20%, B=21-45%, C=46-75%, D=下位25%）。
  注目度(reputation)とは別系統で、`updateAllRanks()` が年度末に一括更新する
- **Eloスコアの変動源** (`ELO_I`):
  - リーグ戦: 勝率と期待勝率の差（社会人50 / 大学・独立40）
  - **最終順位ボーナス: 1位+30, 2位+15, 3位+6**（弱小リーグでも優勝すれば昇格できるように）
  - 全国大会: ブラケットの勝敗ごとに変動（社会人・独立GC=40 / 大学=35、後半戦ほど重み増）
  - **独立グランドCS: 優勝+40, 準優勝+18**
  - **プロ輩出: 1名あたり+15**（`proDraftCountSeason` を年度末に消費してリセット）

## 大学プールの初期化（既知の落とし穴）
- `seedInitialUniversityClasses(gameYear)` は **セーブのロード時にしか呼ばれていなかった**ため、
  新規ゲームでは在学生が1人も存在せず、新聞「ドラフト戦線」の大学欄が空のままだった
  → `DateProgressScreen.checkAndTriggerEvents` の冒頭で毎回呼ぶ（中身があれば即returnする冪等な関数）
  - 初回のみ約800ms（8920人生成）。2回目以降は実質ゼロ
  - セーブは gzip+base64 で約1.46MB。IndexedDBに格納されるので問題ない
- **`enrollYear` はゲーム内年度（1,2,3…）**。`seasonData.currentDate.year` はカレンダー年（2024等）。
  学年判定でこの2つを混ぜると `2024 - 2` となり、全学年が3〜4年生として通ってしまう。
  学年を出すときは必ず `seasonData.settings.year` を使うこと

## 大学リーグシステム (`src/university/`)
- **チームデータ** (`universityTeamsData.js`): `UNIVERSITY_TEAMS` に234校定義（27リーグ、2部制12リーグ×12校＋1部制15リーグ×6校）
  - ランク分布: S=10校, A=22校, B=47校, C=96校, D=59校
  - 2部制12リーグ: 札幌, 北東北, 関甲新, 東都, 首都, 東京新, 千葉県, 愛知, 阪神, 近畿, 中国地区, ＋他
  - 1部制15リーグ: 北海道学生, 仙台六大学, 南東北大学, 東京六大学, 神奈川, 北陸, 東海地区, 京滋, 関西学生, 関西六大学, 広島六大学, 四国地区, 福岡六大学, 九州六大学, 九州地区, 南部九州
- **リーグ戦** (`universityLeagueManager.js`): 27リーグの春季(4/5〜6/10)・秋季(9/6〜11/5)リーグ戦
  - ランク別戦力値 `{ S: 78, A: 65, B: 50, C: 38, D: 25 }` で簡易シミュレーション
  - 部制はUNIVERSITY_REGIONSの`divisions`フィールドで汎用管理（12リーグが2部制、他1部制）。将来の3部制も対応可
  - 試合日: 火・水・土・日。`WORLD_DATA.universityLeagues` に格納
  - DateProgressScreenで折りたたみ表示（全27リーグ個別展開可）。**全モード（独立・大学・社会人）で表示**
- **ランク別成長**: `UNIVERSITY_RANK_GROWTH` — S=1.55倍, A=1.35倍, B=1.15倍, C=1.00倍, D=0.85倍
  - `applyUniversityGrowth()` で自動適用。universityPool の各エントリに `universityRank` を保持
  - 旧入学ブーストを廃止し、4年間の年次成長に統合。S校とD校の差がより明確に
- **大学specialties連動成長**: 各大学の得意分野が成長に影響
  - 7分野: technique(技巧), power(パワー), stamina(体力), defense(守備), versatility(適応), athletic(身体), mental(精神)
  - specialty該当: ×1.2倍、非該当: ×0.85倍。能力→分野マッピング: 制球=technique, 球速=power, スタミナ=stamina, ミート=technique, パワー=power, 選球眼=mental, 走力=athletic, 守備=defense
  - **新球種習得**: 投手に毎年判定。technique/versatility持ち=15%, それ以外=5%。フォーム適性球種を優先
  - **サブポジ成長**: 野手に毎年判定。defense/versatility持ち=35%, それ以外=12%。適正+4〜14
- **社会人トーナメント表示**: 独立・大学モードでも地域トーナメント/都市対抗/日本選手権/クラブ選手権のトーナメント表を折りたたみ式で表示（`!isCorporate` 条件）
  - セーブロード後の再生成時は、現在月が大会終了月を過ぎている場合に自動キャッチアップ完了
- **将来のゲームモード**: `gameMode = 'university'` として社会人モードと並行実装可能

## 社会人モード入退団 (`src/corporate/scoutingSystem.js`)
- **退団（11/9）**: 自動引退判定 + ユーザー/AI戦力外通告。戦力外選手(35歳未満)はリリースプールへ
- **入団（11/10）**: スカウトシステム。`scoutingEye` でスカウト精度が変化（能力値のぼかし表示）
  - `reputation` で候補者の基礎能力にボーナス、候補数はスカウト能力で6〜12人
  - ユーザーは最大3名に交渉可能。成功率は注目度・ランク・交渉力・選手の質で決定
  - AIチームも交渉成功率に基づいて獲得（ランクが高いほど成功しやすい）
- **交渉成功率**: `calculateRecruitSuccessRate(player, teamData)` → 5〜95%
  - ベース: 注目度0→30%, 50→60%, 100→90%
  - 選手の質: 有力な選手ほど-最大30%
  - 交渉力: スタッフの交渉能力で+0〜15%
  - ランク補正: S=+10%, A=+5%, B=0%, C=-5%, D=-10%
  - パイプボーナス: OBの繋がりで入団しやすい（強度1→+5%, 2→+8%, 3→+12%）
- **パイプスカウト効果**: OBがいる大学出身の候補は発見されやすく（スコア+10〜20）交渉も有利
- **チーム運営画面**: サイドバー「チーム運営」（社会人モード限定）でスタッフ・スカウト状況・パイプ・財務を確認
- **画面遷移**: `corporate_departure` → `corporate_scout` → `dateprogress`（独立リーグの `contract` → `tryout` に対応）

## 大学モード スポーツ推薦スカウト (`src/corporate/scoutingSystem.js`)
- **概要**: 4月の高校生プール生成と同時にスカウト候補を初期化し、シーズン通年でスカウト活動
- **推薦枠数**: ランク別 S=8, A=7, B=5, C=4, D=3
- **候補生成**: `initUniversityScoutList(teamData, rank)` — 4月に高校生プールから候補を選出。reputation依存で8〜20名
- **能力表示**: `_revealLevel` 0=primary(精度50-65%), 1=secondary(精度65-80%), 2=full(精度80-95%)
- **調査システム**: `startUniversityInvestigation(candidate, date)` — 5日間で`_revealLevel`が1段階上昇
- **注目システム**: `toggleUniversityWatch(candidate)` — 注目中は週+4%ずつ`_watchBonus`が蓄積
- **日次処理**: `processUniversityScoutDay(candidates, date, rank, rep)` — 調査完了判定+注目ボーナス加算
- **交渉成功率**: `calculateUniversityRecruitRate()` = baseRate(30%+rep*50%) - qualityPenalty + rankBonus(S+12%〜D-12%) + invBonus(8%/段階) + watchBonus
- **交渉**: `attemptUniversityRecruit(player, rank, rep)` — 成功時`_universityReserved`フラグ
- **設計思想**: 低ランク大学は無名の逸材を早期に発掘・注目して交渉率を上げてから確保する醍醐味
- **フロー**: 4月初期化 → シーズン中サイドバーからスカウト画面 → 11/10に強制完了イベント → オフシーズン卒業処理で入部
- **画面遷移**: サイドバー「推薦スカウト」で随時アクセス / `university_scout`（11/10トリガー時はonComplete付き）
- **新入生構成**: スカウト推薦(scouted) + AI推薦(recommended) + 一般入部(general)
- **UI**: `src/components/UniversityScoutScreen.jsx`
- **セーブ/ロード**: `WORLD_DATA._universityScout` に候補・獲得済みリストを保持。年度末にnullリセット

## クラブチームのプロ意識駆動成長 (`src/season/yearProgressionSystem.js`)
- **設計思想**: クラブチームはキャンプも無く実践経験も少ないため、人工的な突出選手ブーストではなく、選手のプロ意識(discipline)×成長率(growthPotential)で数年かけてドラフト候補に成長する設計
- **クラブ除外**: `corporateInit.js` の `proChance` / `standout` はクラブチーム(`type === 'club'`)を除外。初期能力は低い(RANK_SCALE D=0.58, C=0.70)
- **disciplineMult（クラブ）**: `1.0 + max(0, (discipline - 40) × 0.045)` — discipline 40→1.0x, 60→1.9x, 80→2.8x
- **disciplineMult（企業/独立）**: `1.0 + max(0, (discipline - 50) × 0.015)` — 環境が整っているため控えめ
- **知名度蓄積**: クラブでdiscipline≥65の選手は `fameGain += (discipline-50)×0.08` で地域の評判が蓄積
- **ドラフト到達条件**: discipline 80+（上位5%）× growthPotential 1.2+ で3-4年かけて到達。年間約2名がドラフト指名

## 守備位置と適正のズレ（`src/game/lineupGenerator.js`）
- **AIオーダー編成は `p.position` を打順の守備位置で恒久的に上書きする**（`positionFitness` は変えない）。
  そのため「守備位置＝三塁／適正は一塁が100」という選手が生まれる。転向自体は正しい表現だが、
  適性30の選手を遊撃や捕手に置くような編成が出来ていた
- **原因1: 候補プールに本職が入らない**。難守備ポジションの補充条件が
  「適性50以上の選手が1人でも居れば良し」だったため、遊撃手(二塁適性77)が1人居るだけで
  条件を満たし、その選手が遊撃に入った結果、二塁に適性30の選手が回っていた
  → **全8ポジションについて本職をプールに入れることを保証**する
- **原因2: 試行ごとに割り当て順を入れ替えている**（`trialSeed % 3 === 1` で反転）。
  捕手が最後に回った試行では捕手を守れる選手が既に取られており、適性30の右翼手が捕手に入る。
  他8人が最適に収まるため `calcPositionValue` の適性ペナルティ(√)だけでは総合値で勝ててしまう
  → **適性55未満の割り当てに総合値から減点**（捕手×2.0 / 遊撃・二塁×1.2 / 中堅×1.0 / 他×0.6）
- 実測（トライアウトで能力順に24人指名 → AI編成、40チーム960人）:
  適正不一致 **87人 → 22人**。最悪ケースも「左翼→遊撃 適正30」から「左翼→一塁 適正30」へ
  （一塁は守備の弱い選手を置く定位置なので妥当）
- 残る22人はロスターにそのポジションの選手が居ない場合で、転向は避けられない

## 既知の未修正（エキスパンションチーム）
`generateExpansionRoster` は候補30人（うち投手14人前後）から24人を取るため、
野手が10人ほどしか残らず8ポジションを埋められない。さらに既存の独立リーグチームより
戦力が高い（スタメン野手のミート+パワー平均 93.5 vs 68.7）。
候補プールを広げると欠落は解消するが戦力差が116.5まで開くため、**別途の較正が必要**。

## 投手起用ロール
- 先発: complete(完投型), short(ショートスターター), quality(勝ち権利交代), ace(エース), opener(オープナー:初回〜2回で継投しロングへ繋ぐ)
- リリーフ: long(ロング), onepoint(ワンポイント), setup(セットアッパー), closer(守護神), ace_relief(中継ぎエース), behind(ビハインド), mopup(敗戦処理)
- `pitchingRotation.pitcherRoles` マップ + レガシー配列の両方を更新すること

## リリーフの起用分散（`src/game/autoSimulation.js`）
- **登板疲労 `pitchingRotation.reliefFatigue` が唯一の登板間隔の制御**。ゲートは `< 50`
- **+30/登板 では回復（25/日）に負けて永久に50に届かず**、同じリリーフが100試合中97登板していた
  → **+50** にすると2連投で頭打ちになる（最多登板が中央値62→52登板）
  - **これ以上（+70等）にしてはいけない**。全員がゲートに掛かり、疲労を見ない緊急経路へ
    流れて（22%→55%）かえって偏りが悪化する
- **役割別の選択は疲労順にすること**。`ワンポイント` `セットアッパー` `中継ぎエース` は
  配列の先頭を `break` で取っていたため、左のワンポイントが1人のチームでは
  左打者のたびにその1人が呼ばれ69登板していた
- **緊急フォールバックは `player.fatigue` ではなく `reliefFatigue` で並べる**。
  前者はシーズン疲労でリリーフにはほとんど溜まらず、実質ロスター順になる
- **ゲートは相対にすること**。絶対値50だけで判定すると、全員が50を超えた時点で
  ロール別の経路が総崩れになり、疲労を見ない緊急経路へ流れる（実測24%）。
  「50未満」か「今日まだ投げていない中で最も疲労の少ない投手」なら通す形にすると
  緊急経路が **24% → 6%** に下がる
- **`reliefFatigue` には上限150を設ける**。相対ゲート導入後は疲労が溜まり続けても
  登板できるため、無制限だと4210のような値になり、左ワンポイント投入判定などの
  絶対値 `< 50` が永久に無効化される
- **加算箇所は2つある**（`autoSimulation.js` と `aiManager.js`）。後者だけ +30・上限なしで
  残っていたため、采配モードで投げたリリーフは管理値がズレていた。両方 +50/上限150 に揃える
- **計測時は日次回復を必ず呼ぶこと**。`recoverAllPitcherFatigue()` を呼ばない自作ループで
  測ると疲労が上限に張り付き、実ゲームには存在しない「95登板」が観測される
- **総量は元々正しい**（リリーフ3.23人・3.40回/試合。実NPB 3.0-3.5人・3.0-3.5回）。
  問題は常に分配であって、投手を増やしても直らない
- 実測（6チーム×100試合×3シード）: 最多登板 **97 → 68登板**
  - ロール別平均: 中継ぎエース49 / ビハインド47 / ロング40 / ワンポイント37 / 敗戦処理32 / セットアップ32 / 守護神30
  - 勝利は先発に戻った（先発10-14勝、リリーフ0-2勝）。報告された「リリーフが20勝」は解消

## 降板ルール（3条件のいずれかで降板）
1. **球数制限**: ロール別上限（complete:120, ace:110, quality:100, short:65, opener:40, closer:40, setup:35, ace_relief:40, long:60, onepoint:15, behind/mopup:50）
   - **オープナー**: 2イニング(6アウト)投球で役割完了→ロングリリーフへ継投（球数上限40も併用）
2. **スタミナ25%以下**: 先発・リリーフ共通。残りスタミナが最大の25%を切ったら降板
3. **ダメージポイント制（先発のみ）**: 単打/四球=4pt, 長打=6pt, 失点=10pt。閾値は1回=45→9回=5（5刻みで減少）。イニングまたぎで-10pt回復（最低0）
- `gameState.starterDamagePoints` で積算を追跡

## 守備力と失策・自責点（`src/simulation-logic.js` getErrorRate）
- **守備力の水準**: 20=小学生 / 30=中学生 / 40=高校生 / 50=大学生 / **60=プロの及第点**
- **失策率（1守備機会）**: 60を基準に、下回るほど急激・上回るほど緩やかに変化
  - 守備20→12.2% / 30→9.6% / 40→7.0% / 50→4.4% / 60→1.8% / 70→1.2% / 80→0.6%
  - 難しい打球（横っ飛び・深いフライ等）は difficulty で最大2倍
  - 肩が弱いと送球ミスが増える（`armPenalty`）
- **失策の発生箇所（捕球ミス）**: 内野ゴロ／内野ライナー／外野ライナー／外野フライ（落球）
  ※以前は内野ゴロのみで、外野守備が失策に無関係だった
- **送球エラー** (`getThrowErrorRate`): 捕球とは独立した判定。**送り手の肩＋受け手の守備**の両方が効く
  - 一塁への悪送球（一塁手の守備が受け手）／外野からの中継ミス（遊撃・二塁がカットマン）
    ／盗塁阻止の捕手悪送球（二塁・三塁のカバーが受け手）
  - 肩60・受け手60で約1.2%、肩40・受け手40で約4.6%
  - 悪送球・中継ミスは走者が**余分に1つ進塁**する（`extraAdvance`）
- **捕殺（`assists`）**: 単打時の積極進塁（1塁→3塁 / 2塁→本塁）を走者の足と外野の肩で判定し、
  狙って失敗すれば刺殺。成功率=肩60・カット60・走力55で約22%
  - 試行率: 2塁→本塁55% / 1塁→3塁22%（走力で増、外野の肩で減）
  - 強肩ほど走者が自重するため捕殺数は伸びないが**失点が明確に減る**（肩40→7.0失点/試合, 肩80→6.2）
- **失策出塁は安打ではない**: 打数のみ加算し、安打・打点・被安打には計上しない
  （安打扱いにすると失策増加でリーグ打率が.251→.271に上振れする）
- **実測（守備・肩を揃えた場合／両チーム計）**: 40→3.42失策/試合, 50→2.13, 60→1.02, 75→0.48
  - 防御率と失点率の差: 高校生4.69→3.62 / プロ及第点2.73→2.48
- **自責点**: 失策絡みの得点は防御率から除外される（下記）
  - (a) 失策で出塁した走者の生還 (b) 失策が無ければ3アウトだった後の得点 は非自責
  - 自動試合は走者に `_reachedOnError` を付けて追跡、采配モードはイニング単位の近似
  - `earnedRuns` のフォールバックは `??` を使うこと（自責点0が失点で上書きされるため `||` は不可）

## 投げ手・打席の左右比率（`src/utils/handedness.js`）
- **生成される選手の左右はここに一元化**。以前は universityPool / tryoutSystem /
  rosterProgression がそれぞれ別の比率を持ち、生成元によって左打者が35%〜51%までバラついていた
- 目標分布 — **打席: 右56% / 左41% / 両3%**、**投げ手: 右75% / 左25%**
- 投げ手と打席は**同時分布**（`HANDEDNESS_TABLE`）で持つ。左投げはほぼ左打ちという
  現実の相関があるため、独立に振ってはいけない
  - R投R打55% / R投L打17% / R投両打3% / L投L打24% / L投R打1%
- `generateHandedness()` = 両方まとめて決定 / `generateBats(throws)` = 投げ手が先に
  決まっている場合（投手は左30%・野手は左15%など）の条件付き打席
- **打球方向は打席で左右反転する**（`simulation-logic.js` の `velShift` / `pullTendency`）。
  この比率を変えると担当野手分布・守備の重要度が丸ごと変わるので、変更時は必ず再測定すること

## 球種の効果（`BALL_EFFECTS` → `simulation-logic.js`）
`BALL_EFFECTS` の `whiffBonus` / `groundballBonus` / `weakBonus` は長らく
**捕手の球種選択スコアでしか使われておらず、物理エンジンが一度も読んでいなかった**。
そのため「スライダーは空振りが取れる」「シンカーはゴロを打たせる」という設定が
結果に一切反映されず、球種の違いは実質 `level`（制球ペナルティ）と
`velocityMinus`（緩急）だけだった。以下の3経路で接続してある。

| 効果 | 接続先 | 効き方 |
|---|---|---|
| `whiffBonus` | `calculatePhysicsContact` のタイミング窓 | `窓 *= (1 - whiff × level × (1-ミート耐性))` |
| `weakBonus` | 打球初速 | `初速 -= weak × level × (1 - 芯品質×0.5) × 26` |
| `groundballBonus` | 打出し角度 | `角度 -= gb × level × 34`（0.15/Lv100 で -5.1度） |

**球種ごとの性格（ストレート＋1球種Lv50・同一ロスター反復・6シード×110試合。すべて有意）**

| 持ち球 | 防御率差 | 三振率 | ゴロ率 | BB/9 | 本塁打 | 性格 |
|---|---|---|---|---|---|---|
| ストレートのみ | — | 16.0% | 43.5% | 3.56 | 0.874 | |
| **カーブ** | **-0.844** | **20.4%** | 46.7% | 3.78 | 0.592 | 緩急 |
| **ナックル** | -0.721 | **20.4%** | 46.0% | **4.29** | 0.587 | 空振りだが決まらない |
| チェンジアップ | -0.711 | **20.2%** | 47.5% | 3.75 | 0.622 | 緩急＋ゴロ |
| **シンカー** | -0.701 | 19.0% | **47.4%** | 3.67 | 0.626 | ゴロ＋凡打の両方 |
| **フォーク** | -0.699 | **20.2%** | 46.3% | **4.03** | 0.624 | 空振り |
| スライダー | -0.669 | 19.9% | 46.1% | 3.74 | 0.658 | 空振り |
| **シュート** | -0.494 | 18.7% | **47.9%** | 3.86 | 0.643 | 詰まらせる |
| **カッター** | -0.493 | 18.6% | 46.7% | 3.86 | 0.653 | 詰まらせる |
| **ツーシーム** | -0.397 | **18.4%** | 47.4% | 3.87 | 0.682 | 詰まらせる（whiff は負） |

**「何を持っているか」で防御率が 0.40〜0.84 動く。**
上位（カーブ〜スライダー）は三振で、下位（シュート〜ツーシーム）はゴロで抑える。
**シンカーが単独首位だった状態は解消した**（-0.827 → -0.701 の4位）。詰まらせる球の
whiffBonus を負にして、三振と凡打の役割を分けたため（下記「空振りの順位」参照）。

### キャンプの変化球練習は1球種に集中できる
`executeSubTraining(player, 'breaking', { targetPitch })`。指定するとプールを
分配せず1球種に注ぎ込み、さらに +2（フォーム適性があれば +1）が乗る。

**実測（4球種持ちの投手・1クールあたり）**

| | スライダー | カーブ | フォーク |
|---|---|---|---|
| 分配（従来） | +3.01 | +2.02 | +1.95 |
| **スライダーに集中** | **+8.96** | 0 | 0 |

封印と組み合わせると「使えない球を封印したまま3年磨いて、Lv40を超えたら
解禁する」という育成が成立する。UIはキャンプ画面のサブ練習の右の選択で
「全部（分配）」か球種名を選ぶ。

### 変化球の封印（`activeArsenal` / `arsenal.sealed`）
覚えている球でも**試合では使わない**という選択。スタメン設定→投手起用の
選手詳細で、変化球バッジをクリックして封印／解禁する。

- 封印した球は**試合で投げないだけ**。練習・成長・表示からは消さない
- 打者の読み合い（`effectiveArsenalSize`）からも外れる。
  投げない球は引き出しとして数えられないので**読まれやすくなる代償**がある
- 全部は封印できない（投げる球が無くなると試合が成立しない）
- `arsenal[i].sealed` に持つのでセーブは選手オブジェクトごと自動で乗る

**実測（同一ロスター反復・5シード×110試合）**

| 設定 | 防御率 | BB/9 | 三振率 |
|---|---|---|---|
| ストレート＋スライダーLv10（投げる） | 3.592 | **4.35** | 17.7% |
| ストレート＋スライダーLv10（**封印**） | **3.749** | **3.51** | 16.2% |
| ストレート＋スライダーLv40（投げる） | 3.273 | 3.93 | 19.2% |
| ナックルLv70のみ（**ストレート封印**） | 3.307 | 4.00 | 18.4% |
| ストレート＋ナックルLv70（両方） | **2.960** | 3.86 | 20.9% |

- 未熟な球の封印は **防御率 +0.16 と引き換えに BB/9 -0.84** のトレードオフ。
  「四球を出せない場面の投手は封印しておく」が意味を持つ
- **ストレート封印のナックルボーラーは成立するが最適ではない**。
  実効持ち球数が1になり `guessSuccessRate` が0.95に張り付くため
  （ナックル自体が強いので、それでも変化球なしの3.75よりは良い）

### 変化球の習熟度（`LEVEL_SIGMA_W = 0.27`）
**未熟な変化球は決まらない**。レベル0で σ に +0.27セル、レベル100で0。
`whiff/gb/weak` はレベル比例だが、**球速減(`velocityMinus`)はレベル非依存**なので
未熟でも緩急の価値だけは残る、という非対称がある。

| スライダーの習熟度 | 防御率差（持たない場合との差） | 三振率 | BB/9 |
|---|---|---|---|
| （持たない） | — | 16.8% | 3.58 |
| Lv10 | **-0.108（有意でない）** | 18.5% | **4.48** |
| Lv30 | -0.474 | 19.7% | 4.03 |
| Lv50 | -0.685 | 20.8% | 3.77 |
| Lv70 | -0.806 | 22.2% | 3.49 |
| Lv90 | **-0.993** | 23.4% | **3.14** |

**Lv10 は「防御率はほぼ変わらず四球だけ +0.9 増える」**＝覚えたてはむしろ損。
Lv20〜30 から明確に得になる。「とりあえず覚える」が通用しない形になっている。

- ⚠ 0.32 まで上げるとリーグの四球率が 10.3%→11.3% に戻る（実NPB 8.5%。
  この作品の長年の課題なので悪化させたくない）。0.27 が落としどころ
- **ナックルは Lv10 で -0.010（誤差）/ BB/9 5.30** と、`TYPE_SIGMA` の代償が
  重なって完全に無価値になる。Lv60 まで磨いて初めて他と並ぶ

### 変化球の球速減は2エンジンで共有する（`pitchVelocityDrop`）
⚠ 以前は**自動シミュだけ別式**だった。
  自動シミュ: `8 + level/100 × 15` → Lv100なら**全球種が一律 -23km/h**
  采配モード: `BALL_EFFECTS.velocityMinus` → ナックル-30 / ツーシーム-5
スキップだとツーシームもナックルも同じ速さで来ていた。緩急は打球方向
（`SEQ_DIR`）とタイミングの両方に効くので `constants.js` に一本化した。
レベルで少し伸びる（`velocityMinus × (0.72 + level/100 × 0.28)`）。

### ナックルは読み合いが成立しない（`UNREADABLE_PITCHES`）
回転を殺して不規則に揺れる球で、投手・捕手・打者の誰にもどこへ来るか
分からない。**球種を張り当てても効果が出ない**ようにしてある
（`guessRight` を常に false にする。采配モードのプレイヤーの狙い球も同様）。

**実測（同一ロスター反復・5シード×110試合）**

| 設定 | 防御率 | 被打率 | 三振率 | BB/9 | 本塁打 |
|---|---|---|---|---|---|
| ストレートのみ | 3.625 | .2538 | 16.8% | 3.55 | 0.747 |
| **ナックルLv100のみ（ストレート封印）** | **2.565** | .2196 | 22.6% | 3.34 | 0.509 |
| ナックルLv100のみ（球速100kmの投手） | 2.865 | .2423 | 17.4% | 3.09 | 0.488 |
| ストレート＋ナックルLv100 | 2.503 | .2137 | 24.4% | 3.40 | 0.494 |
| ストレート＋スライダーLv100（比較） | 2.600 | .2185 | 24.7% | 2.97 | 0.586 |

**1球種のナックルボーラーが成立する**（読まれない前は2.864で、併用型に0.29
負けていた）。読み合い無効化で差が 0.06 まで縮み、専門職として立つようになった。
- ⚠ ただし **球速を落とすと弱くなる**（100km/hで2.865）。球速がタイミング窓を
  決める一方、ナックルの揺れは `whiffBonus` という別チャンネルなので、
  「遅いから転向する」という現実の因果は表現できていない（既知の限界）

### 球種ごとの代償（`TYPE_SIGMA` / `pitchShape.js`）
以前は**同じレベルなら全球種が同じ精度で決まった**ので、効果の大きい球
（シンカー）を選ばない理由が無かった。「効くが投げ切れない球」を σ で表現する。

- `knuckle: 0.13` … 握りが特殊で回転を殺す球。投手自身も落ちる場所が分からない。
  実測で BB/9 が +0.71、順位が5位→7位に落ちる
- `fork` / `splitter: 0.06` / `palm: 0.05` … 抜けやすい
- レベルを上げても半減までしか消えない（名手でも制御しきれない）
- ⚠ `knuckle: 0.30` にすると BB/9 が **+1.3** になって球種として成立しなくなった。
  実際のナックルボーラーもリーグ平均より 0.4〜0.5 多い程度

### 詰まらせる球（ツーシーム・カッター・シュート）
三振ではなく**内野ゴロを打たせる**のが目的の球種なので `groundballBonus` を
0.09〜0.12 → 0.17〜0.20 に上げた。実測でゴロ率が 43.3%（ストレートのみ）に対し
**47.5〜48.2%** になり、三振率は逆に最も低い（18.6〜19.3%）。
シンカーはゴロ(0.15)と凡打(0.23)の両方を持つので最も頼りになる
（`FORM_PITCH_SYNERGY` でサイド・アンダースローの適性球でもある）。

### 持ち球の「幅」（`src/game/arsenal.js`）
打者の球種予測は `1 / 持ち球数` で、**何を持っているかを一切見ていなかった**。
スライダーとカットボールを持つ投手も、スライダーとシュートを持つ投手も
同じ「3球種」として扱われていた。

球種を**既存の3軸**に置き、近い球どうしは互いを打ち消して
「実効的な持ち球数」を減らす。実効数を `guessSuccessRate` に渡す。
- 奥行き … `BALL_EFFECTS.velocityMinus`（速球系 / 遅球系）
- 横 … `PITCH_AXIS_SIDE`（グラブ側 / アーム側。**最も読み分けにくいので重い**）
- 縦 … `PITCH_AXIS_VERTICAL`（伸びる / 落ちる）

**新しい表を作らないこと**。`pitchShape.js` の定義をそのまま軸に流用している。

| ストレート＋2球種（Lv60） | 実効数 | 類似度 |
|---|---|---|
| スライダー＋シュート（左右反対） | **2.64** | 0.00 |
| カーブ＋シュート（遅球と横） | **2.64** | 0.00 |
| スライダー＋フォーク（横と縦） | 2.41 | 0.16 |
| フォーク＋カーブ | 2.04 | 0.57 |
| フォーク＋パーム（似ている） | 1.97 | 0.69 |
| スライダー＋カット（似ている） | **1.83** | 0.48 |

**実測（同一ロスター反復・6シード×110試合）**: スライダー＋カットは
スライダー＋シュートより **防御率 +0.141 ± 0.048（有意）**。
反対の性格を組み合わせるほど読まれにくく、似た球を並べても球種を
増やした意味が薄い、という関係が出ている。

⚠ 球種そのものの強さ（whiff/gb/weak）と混ざるので、**組み合わせの効果だけを
見たいときは同じ球種数・同じレベルで揃えて比較すること**。
フォーク＋パーム（実効1.97）がスライダー＋シュート（2.64）と大差ないのは、
フォークとパームが単体で強い球だから。

### 速球の空振りは球速そのもので決まる（`FASTBALL_WHIFF_*`）
`BALL_EFFECTS.whiffBonus` は「**変化**による欺き」なのでストレートは 0。
しかし実際のフォーシームは球速で空振りを取る。旧実装は球速→タイミング窓の
経路しかなく、実測でストレートの空振り率が **12.6%（実MLB 22%）**しか
出ていなかった。138km/h を境に、速ければ空振りが増え遅ければ当てられる
（`(球速 - 138) × 0.0060` をタイミング窓に掛ける）。

**実測（投手の球速帯ごと・スイングした球のうち空振り）**

| 投手の球速 | 130 | 135 | 140 | 145 |
|---|---|---|---|---|
| 本作 | 7.6% | 9.9% | 14.2% | **17.3%** |
| 実MLB | 11% | 13% | 16% | **18%** |

**傾きは実データとほぼ一致**した。自然ロスター全体では 9.3% と低いが、
これは独立リーグ相当で120〜130km/hの投手が大半だから
（実MLBの平均22%は平均150km/hの母集団の値。145km同士なら 17.3% 対 18%）。

### 空振りの順位を実データに合わせる（`OFFSPEED_WHIFF_K` / 変化球一律ペナルティ）
`whiffBonus` を設定しても、**空振り率の順位が到達球速でほぼ決まっていた**。
同じ whiffBonus 0.09 のスプリッター(到達134km) 21.5% / スライダー(129) 18.8% /
フォーク(125) 15.8%、そしてカーブ(120) 6.6% / チェンジアップ(121) 7.0% ——
**遅さが武器の球ほど損をする**逆転が起きていた（実MLBはどちらも31%）。
チェンジアップは全球種で最下位で、詰まらせる球であるツーシームやシュートより
空振りが取れていなかった。原因は `whiffBonus` を打ち消す2つの項:

| 打ち消していたもの | 症状 | 対処 |
|---|---|---|
| 基準の窓が `1/球速` | **遅い球ほど窓が広い＝当てやすい** | 落差そのものを欺きとして窓に効かせる（`OFFSPEED_WHIFF_K = 1.0`） |
| 変化球一律ペナルティ `level × 0.18` | 球種を問わず窓を狭めるので、**詰まらせる球まで三振が増える** | 0.06 に下げ、性格は `whiffBonus` に持たせる |

- ⚠ **落差は「km/h」ではなく「その投手の速球に対する比」で効かせること**。
  窓は 1/球速 に比例するので必要な打ち消し量は球速に依存する。絶対値の係数
  （0.007＝1/140）にすると独立リーグ（速球122km）で打ち消し切れず、
  カーブ・チェンジアップだけ空振りが取れないままだった
- ⚠ **落差は `pitcher.velocity - pitch.velocity` で引き算してはいけない**。
  2エンジンで `pitcher.velocity` の意味が違う（自動シミュ=スタミナ補正後 /
  采配モード=素の値）ため、采配モードでは**疲れたアンダースローのストレート**が
  大きな緩急に化ける。両エンジンが実際の球速を出すのに使っている
  `pitchVelocityDrop(type, level)` を直接読む
- `OFFSPEED_WHIFF_K = 1.0` は**ちょうど相殺**＝「タイミングの持ち時間は実際の
  球速ではなく、打者が身構えている速球で決まる」。速度差の有利不利を消して
  順位を `whiffBonus` に委ねる位置

**空振り率（速球140km・変化球Lv60・ミート58の打者・振った球のみ）**

| 球種 | 修正前 | **修正後** | 実MLB |
|---|---|---|---|
| スプリッター | 21.5% | **17.8%** | 36% |
| **フォーク** | 15.8% | **17.5%** | 36% |
| スライダー | 18.8% | 16.8% | 35% |
| **カーブ** | 6.6% | **15.3%** | 31% |
| **チェンジアップ** | 7.0% | **15.0%** | 31% |
| パーム | 8.4% | 14.8% | 31% |
| ナックル | 7.7% | 12.8% | 25% |
| カッター | 13.9% | 12.4% | 25% |
| ストレート | 10.3% | 10.5% | 22% |
| シュート | 13.9% | **7.3%** | 15% |
| シンカー | 12.1% | **6.7%** | 15% |
| ツーシーム | 9.2% | 6.3% | 15% |

**順位が実MLBと完全に一致した**（splitter=fork > slider > curve=changeup=palm >
cutter=knuckle > straight > shoot=sinker=twoSeam）。

**リーグ全体は動かない（同一ロスター反復・8シード×120試合）**

| | 修正前 | 修正後 | 差 ± 標準誤差 |
|---|---|---|---|
| 打率 | .2354 | .2341 | -0.0014 ± 0.0014 |
| 防御率 | 3.234 | 3.218 | **-0.016 ± 0.056** |
| 三振率 | 20.33% | 20.59% | **+0.26pt ± 0.10** |
| 四球率 | 10.45% | 10.35% | -0.10pt ± 0.08 |

**再配分であって総量ではない**ので `windowCoef` の再較正は不要だった。
- ⚠ **season-check はシードを固定していない**（`Math.random` そのまま）。
  3シードの単発比較では 打率 .232〜.245 と振れるので、
  前後比較は必ず同一ロスター反復で取ること。一度これで
  「打率 .242→.232 / 防御率 3.50→3.12」という**存在しない差**を読んでしまった

⚠ **絶対値は依然として実データより低い**（全球種で11〜18pt不足）。ただし
**順位と相対関係は合っている**ので、残るのは母集団の差（本作の統制条件は
速球140km、実MLBの平均は150km）と `meetDeceptionResistance`（ミート高打者は
最大50%軽減）による圧縮。リーグ全体の三振率は19-21%で実データ帯に収まっており、
これ以上上げるとリーグが壊れる。直すなら打撃側の再較正とセットで行うこと（未着手）。

### フォーム適性は試合の効きにも掛かる（`formPitchBonus`）
⚠ `FORM_PITCH_SYNERGY` は長らく**キャンプの成長倍率と習得成功率にしか
効いていなかった**。同じレベルで揃えて測ると適性が結果に一切出ず、
「サイド・アンダースローはシンカーが武器」という設計が
**球速ペナルティ（アンダー×0.92）だけ受けて損をする**形になっていた。

適性球 ×1.30 / 適性外 ×0.88 を `whiff/gb/weak` の3つに掛ける
（スリークォーターは適性リストが空＝すべて 1.0）。
あわせて `sidearm` の適性リストに **`sinker` を追加**した
（サブマリンにしか入っておらず、実態と合っていなかった）。

**実測（素の球速145で統一・同一ロスター反復・5シード×110試合）**

| フォーム × 持ち球 | 実効球速 | 防御率 | 三振率 |
|---|---|---|---|
| オーバー ＋ フォーク（適性） | 145km | **2.417** | 29.9% |
| オーバー ＋ シンカー（適性外） | 145km | 2.475 | 28.3% |
| サイド ＋ **シンカー**（適性） | 138km | **2.717** | 24.4% |
| サイド ＋ フォーク（適性外） | 138km | 2.751 | 25.2% |
| アンダー ＋ **シンカー**（適性） | 133km | **2.873** | 22.1% |
| アンダー ＋ フォーク（適性外） | 133km | 2.900 | 22.8% |

**フォーム内では適性球が最善になった**（サイド -0.034 / アンダー -0.027 /
オーバー -0.058）。ただし **`velocityMult` の球速ペナルティの方がはるかに大きい**
（オーバー 2.417 対 アンダー 2.873 で 0.46 差）ので、
「腕を下げると総合的には不利」という関係は変わらない。

### 曲がりの効きは130km/h付近が頂点（`breakEfficiency`）
物理的には変化量は **回転数 ÷ 球速** に比例するので**遅いほど曲がる**。
しかし遅すぎると打者に見極める時間ができるので、打者にとっての実効的な
効きは中間で頂点になる。`whiffBonus` / `groundballBonus` / `weakBonus` の
3つすべてに掛ける。

| 到達球速 | 80 | 110 | 120 | **130** | 140 | 150 | 160 | 170 |
|---|---|---|---|---|---|---|---|---|
| 効き | 0.60 | 1.09 | 1.16 | **1.18** | 1.16 | 1.09 | 0.97 | 0.81 |

- カーブ(120前後)・ナックル(115前後)が有利になり、速球系のツーシーム・
  カッター(140前後)はわずかに不利になる
- ⚠ **超スローボールの投手は逆に不利**。球速100km/hの投手のナックルは
  到達70km/hになり効きが0.60まで落ちる。`velocityMinus` が絶対値なので
  「遅い投手ほど変化球も極端に遅くなる」ためで、実際のナックルボーラーの
  ナックルは直球より10km/h程度しか遅くない。ここは未解決

### ⚠ 接続係数は打球分布を変えたら必ず測り直す
`BALL_WHIFF_W` / `BALL_WEAK_W` / `BALL_GB_W`（simulation-logic.js）。
段階②で EV のσが約1.7倍・打出し角の範囲も広がったため、同じ絶対値の補正では
相対的な効きが薄まり、**球種ごとの性格がほぼ消えていた**。
実測（旧係数）: whiffBonus が -0.05〜+0.10 も違うのに三振率 19.2〜20.0%、
本塁打 0.638〜0.691 と差が出ず、防御率の差も 0.14 しかなかった。
係数を 2.2 / 46 / 58 に上げて性格を復活させてある。

- **接続により捕手のリードが初めて意味を持った**。球種選択だけを分離した実測で
  制球70の投手は「常に最良を選ぶ」と「ランダム」で防御率 2.833 → 2.580（-0.253 ± 0.096、有意）
- リーグ全体の成績は変わっていない（NPB相当で 打率.236 / 失点3.77 / 本塁打0.78）

## 死球（`hitByPitchChance` / `src/game/pitchZone.js`）
**死球はこれまで1つも発生していなかった**（結果種別もスタッツも存在しなかった）。
25分割グリッドは col が 5 以上まで外れるので、そこを「打者の体の側」として使う。

- **col 5 以上が体に向かう球**。col 4 は「内角ボール」でまだ当たらない。
  6以上は背中側なので避けやすい（重み 5:1.0 / 6:0.55 / 7:0.20 / 8:0.10）
- **高さは胸〜膝がまともに当たる**。頭上を抜ける球とワンバウンドは減らす
- **振らなかった球にだけ問う**。振って当たればストライクなので当然
- **制球の項を足してはいけない**。σ が大きい投手ほど col≥5 が増えるので、
  制球の効果は既に入っている（足すと二重評価）
- 実測: col≥5 は全投球の 3.83%。そこに 8.5% を掛けて 死球は打席の 0.96%
  （実NPB 約0.9%）、0.36/チーム/試合（実NPB 約0.36）
- 制球別の与死球/9: 制球30→0.40 / 50→0.42 / 70→0.29。**ノーコンほど当てる**
- 塁の詰め方は四球と同じ（押し出しあり）。打数にも四球にも計上しない。
  出塁率の式（表示側6箇所）にも死球を足してある

### 死球は疲労が大きく溜まる（故障システムは作らない）
`hitByPitchFatigue(velocity, bodyStamina)`。**痛みを故障ではなく疲労で表現する**。

| | 体力30 | 体力50 | 体力70 | 体力90 |
|---|---|---|---|---|
| 125km | 14 | 12 | 11 | 9 |
| 140km | 18 | **16** | 14 | 12 |
| 150km | 21 | 19 | 16 | 14 |
| 160km | **24** | 21 | 18 | 16 |

- **スタメン出場1試合ぶんの疲労が 7〜13** なので、1球で1.5〜2.5試合ぶん。
  日次回復が約7/日なので「数日は本調子でない」になる
- **打席数に関わらず乗る**（代打の1打席で当たっても痛い）。
  通常の野手疲労は「3打席以上のスタメン出場」が条件だが、死球は別枠
- 采配モードは投球ログに `死球（疲労+18）` と出す。隠れたコストにしないため
- **リーグ成績には出ない**（同一ロスター反復10シード: 打率 -0.0001 / 防御率 -0.022、
  いずれも誤差内）。規定級野手のシーズン終了時の平均疲労が 27.4 → 30.1 になるだけ。
  1件ごとの痛みは大きいが、日次回復で吸収されるので蓄積はしない

### ⚠ 同時に見つかった集計バグ: 打撃成績が「0打数」の試合で丸ごと落ちていた
`autoSimulation` / `gameSetup` のシーズン集計が `if (atBats > 0)` で門番されており、
**「0打数2四球」のような試合の四球・死球がシーズン成績に一切入っていなかった**。
実測で死球の9%、四球の8%が消えていた。投手側は正しく積算されていたため、
打撃側の四球率(10.8%)と投手側のBB/9(12.4%相当)が食い違う形で表面化していた。
条件を `atBats > 0 || walks > 0 || hitByPitch > 0` に修正済み。

## 投球コースの25分割グリッド（`src/game/pitchZone.js`）
ストライクゾーンを3×3、その外側に1マスのボールゾーンを回して 5×5=25セル。

```
       col 0    1    2    3    4
  row 0  ▫    ▫    ▫    ▫    ▫    ← 高めのボール
  row 1  ▫   ┌─┬─┬─┐  ▫
  row 2  ▫   │ │ ● │ │  ▫       ● = ど真ん中 (2,2)
  row 3  ▫   └─┴─┴─┘  ▫
  row 4  ▫    ▫    ▫    ▫    ▫    ← 低めのボール
         ↑外角                ↑内角（右打者から見て）
```

- **なぜ必要か**: 従来の位置モデルは「ゾーン内/外」の1次元しかなく、
  捕手のリードを位置に効かせようとすると「際どく要求する＝ボールのリスクを上げる」
  としか表現できなかった。**7通り試して全て四球のコストが利得を上回った**。
  内外角と高低の2次元が入って初めて「この打者には外角低めが有効」が表現できる
- **捕手が狙うセルを決め、投手の制球でそこからのばらつきが決まる**
  - `pickTargetCell(aim)`: 勝負=中央の上下左右 / 際どく=ゾーンの四隅 / 誘い=ゾーンのすぐ外
  - `resolvePitchCell(target, control)`: σ = 1.38 − 制球/100 × 1.00（セル単位）
- **勝負球でど真ん中(2,2)を狙ってはいけない**。目標に含めると制球の良い投手ほど
  狙い通り真ん中へ集まって失投が増える逆転が起きる（実測: 制球20で失投5.6% / 100で8.5%）。
  中央の上下左右を狙い、制球が悪いと真ん中へ流れる＝失投、とする
- **ゾーンの隅は `corner`、ゾーン外の際どい球は `edge` で別扱い**。
  隅を `edge` と同じ -10/-8 にすると全投球の56%にペナルティが掛かり、
  打率 .234→.224 / 防御率 3.62→3.02 まで振れる。隅は -7/-5
- **5×5の四隅も `edge`**。waste にすると大外れが29%まで膨らみ実データ(0〜14%)から外れる
- **較正の基準はラベルの周辺分布ではなくリーグ成績**。ラベルは内部表現にすぎない

**段階1完了時点の実測（NPB相当・3回×3シード）**

| | 25ゾーン | 従来 |
|---|---|---|
| 打率 / 出塁 / 長打 | .236 / .305 / .353 | .234 / .302 / .352 |
| BABIP / 四球率 | .289 / 9.1% | .289 / 8.9% |
| 失点 / 本塁打 / 二塁打 | 3.74 / 0.74 / 1.47 | 3.62 / 0.77 / 1.46 |
| 三振率 | 21.5% | 22.6% |

三振率だけ約1.1pt低い（既知の乖離）。他は誤差の範囲。

**打者の見極め（1球ごと。制球60・ミート58/選球眼52・捕手守備60）**

| | 25ゾーン | 従来 | 実データ |
|---|---|---|---|
| ゾーン率 | 49.7% | 48.0% | 44-55% |
| ゾーン内スイング | 70.4% | 69.7% | 65-68% |
| **chase（ボール球スイング）** | **28.5%** | **30.2%** | **28-31%** |
| 大外れ(waste)を振る率 | 10.5% | 10.3% | — |
| 大外れが全投球に占める率 | 12.8% | 10.8% | 0-14% |
| ストライク率 | 64.0% | 63.7% | 62-63% |

chase の -1.7pt は**打者の見極めが変わったのではなく、ボール球の内訳が変わった**もの。
グリッド化で大外れが 10.8%→12.8% に増え、大外れはほとんど振ってもらえない(10%)ため
ボール球全体の平均が下がる。**枠のすぐ外(edge)だけのスイング率は 35.4%→34.6%** で不変。

- **段階3**: 捕手が打者の弱点を突く（下記「捕手の弱点狙い」）
- **試合画面の表示**（`src/components/PitchZonePlot.jsx`）: 投手と打者の情報の下に
  168px（3倍）で置く。この打席の到達点を**投手から見た向き**で、何球目かの数字つきで打つ。
  内部は5×5セルなので、セル中心＋セル内の揺らぎで連続的な位置に落とす
  - **形＝球種**。**三角は実際に曲がっていく向きを指す**:
    〇ストレート / △カーブ / ▽落ちる球（フォーク・チェンジアップ・パーム・
    ナックル・スプリッター）/ ◁▷横に逃げる球 / ◣◢逃げながら落ちる球（シンカー）
  - **横変化の向きは投手の利き腕で反転する**（打者の左右とは無関係）。
    画面左=一塁側 / 画面右=三塁側なので、
    右投手はグラブ側が一塁側 → スライダー系◁ / シュート系▷、
    左投手はその逆
  - ⚠ **どちら側に逃げるかを手書きの表で持たないこと**。`SHAPE_BY_PITCH` という
    独自の表を置いていたため `PITCH_AXIS_SIDE` と食い違い、**腕側(+0.8)の
    シンカーがグラブ側のスライダーと同じ ◁ になっていた**（右投手のシンカーは
    三塁側へ逃げながら落ちるので ◢ が正しい）。向きは `pitchShape.js` の
    `PITCH_AXIS_SIDE` を正とし、プロット側は「縦に落ちるか」の分類
    （`SHAPE_FAMILY`）だけを持つ。持ち球の「幅」(`arsenal.js`) と同じ理由で、
    **軸の表を二重に作らないこと**
  - **色＝結果**: 緑=ボール / 黄=ストライク / 赤=アウト / 白=安打
  - **塗りつぶし＝スイングした**（黄の中で見逃しと空振り・ファウルが分かれる）
  - **白い輪＝最新の1球**。赤はアウトに使うので色では示せない
  - **何球目かはマーカーの中に書く**（スポーツナビの投球図と同じ）。
    右上に添えると点が増えたときに数字どうしが重なって読めない。
    中に入れるぶんマーカーを大きく取る（基準半径6.5。168px表示で直径15.6px）
  - **マーカーの実寸を決めるのは基準半径だけ**。`mk` が枠の大きさを打ち消すので
    （`mk = max(0.6, min(1, 120/size))`）、**枠を広げてもマーカーは大きくならず
    ゾーンだけが広がる**。点が密集して狭く見えるときは枠を広げる手も使える
    （size 200 で `mk` が下限0.6に当たるので、そこまでは実寸が変わらない）
  - **三角は〇より少しだけ大きく（×1.10）**。三角は同じ外接円でも面積が小さいので
    数字を入れるには広げる必要があるが、広げすぎると〇のストレートより目立って
    球種ごとの重みが揃わない。×1.18 では2桁（10球目以降）が輪郭に触れていたので、
    **枠を詰めて2桁のときだけ数字を小さくする**（1桁は従来どおり）
  - **捕手のシルエットを中央に敷き、打者は枠の外に立たせる**。
    「捕手の後ろから見た画」にすると投球位置が読みやすい。
    以前は打者が枠に重なっていてマーカーが埋もれていた
  - **捕手は等倍だと膝が5×5の外枠からはみ出し、ゾーンより捕手が主役に見える**。
    重心(50,63)まわりで ×0.85 に縮めてある。図の主役は投球位置
  - 外枠（5×5の範囲）は薄く、内枠（ストライクゾーン）を強調する
  - 形は `pitchLoc.type`（球種の生キー）から引く。`gameLog.pitchType` は
    日本語の表示名なので逆引きできない
  - viewBox は固定なので、大きく表示するときは `mk` でマーカーを相対的に縮める
  - **打者の左右で左右反転する**。内部の col は打者基準（col 4 = その打者の内角）
    なので、投手視点に統一するには左打者だけ反転が要る。
    投手から見て 画面右=三塁側 / 画面左=一塁側 なので、
    **右打者の内角は画面右、左打者の内角は画面左**
  - ⚠ 以前は打者基準の col をそのまま x に使い「捕手側から見た向き」と書いていたが、
    実際は右打者では投手視点・左打者では捕手視点という混在だった
  - **打者のシルエットとホームベースを描く**。左右どちらの打席かを文字を読まずに
    分かるようにするため。スイッチヒッターは投手の利き腕から実際に立つ側を解決する
    （投手と逆の打席）ので `pitcherThrows` も渡す
  - **打者にバットと腕は描かない**。役目は「どちらの打席か」を伝えることだけ。
    バットはゾーン側へ張り出してマーカーと重なり、バットだけ消して腕を残すと
    何かを指しているように見える。そのぶん身体を ×0.95 と一回り大きくして、
    遠目でも左右が分かるようにしてある
  - ⚠ 右打者は `translate(100,0) scale(-s,s)` の**鏡映**（x' = 100 - s·x）。
    scale を変えたら translate も変わる。以前は s=0.82 に対して 103 が
    直書きされており、純粋な鏡映から3ずれていた
  - **揺らぎ(`jx`/`jy`)は投球時に一度だけ決めて `pitchLoc` に保存する**。
    描画のたびに乱数を引くと、再レンダリングごとに点が動く
  - `gameLog` の各エントリに `pitchLoc` / `resultType` / `paKey`（打席の識別子）を持たせ、
    表示時は現在の打席ぶんだけを抽出する

## 打者のコース適性（`src/game/batterZone.js` / 25分割グリッド 段階2）
段階1の時点では、どの打者も内角と外角を同じように打っていた。段階2で打者に
**内外角と高低の得手不得手**を持たせる。

- **選手ごとに25セルを持たせてはいけない**。生成も較正も不可能でセーブも膨らむ。
  `inside`（内角の苦手さ）・`low`（低めの苦手さ）・`middle`（ど真ん中の苦手さ）の
  **3数値だけ**を持ち、25セル分の補正はそこから導出する
  - `+1.0`=そのコースに極端に弱い（＝反対側に強い） / `-1.0`=その逆
  - `weakness = clamp(inside × colAxis + low × rowAxis + middle × (RADIAL_MID - radial), -1, 1)`
  - 式は **`pitchZone.js` の `cellWeakness` に一本化**してある。以前は
    `zoneWeaknessAt` / `zoneHeatmap` / `pickTargetCell` の3箇所に複製されており、
    項を1つ足すたびに3箇所直す必要があった

### ⚠ `middle` が無いと「ど真ん中に強い打者」が1人も居ない
`inside × colAxis + low × rowAxis` は**線形の勾配しか作れない**ので、
中心 (2,2) は colAxis も rowAxis も 0 になり、**どの打者も必ず 0**になっていた。
中心からの距離 `radial = hypot(colAxis, rowAxis)/√2` を使って半径方向の項を足す。

- `middle > 0` … 甘い球を打ち損じる / `middle < 0` … 失投を逃さない
- **捕手はど真ん中(2,2)を目標にしない**（`ZONE_CELLS`）ので、この項が効くのは主に
  「投手が狙いを外して真ん中へ流れた球＝失投」。**「失投を仕留める能力」**そのもの
- 効きは中心が最大で、真ん中高め等の十字は 29%、隅は 0

#### 四隅が苦手な打者が多数派（`MIDDLE_BIAS`）
`middle` は当初 平均0 だった。つまり**リーグの半分は甘い球を打ち損じ、
真ん中と四隅の得手不得手が五分**という母集団になっていた。実測でも
セルごとの母集団平均は「ゾーン中央 -0.005 / 四隅 0.000」と完全にフラットで、
「隅は誰でも苦手」という当たり前が一切表現されていなかった。
平均を **-0.35**（＝真ん中に強い＝相対的に四隅が苦手）へずらす。

- σ0.344 なので「真ん中より隅が得意」な打者は約14%しか残らない
- ⚠ **平均をずらすなら `cellWeakness` の centering が要る**。`(1 - radial)` の
  ままだと全打者に一律のボーナスが乗ってリーグが動く。実際に投げられた球の
  radial の平均（43万球で **0.82**）を引いて `(RADIAL_MID - radial)` にしてある
- ⚠ 0.82 と高いのは colAxis/rowAxis が3段階に丸められるため。25セル中16セルが
  radial=1 になる。**「四隅」は4セルではなく対角の象限全体**を指す粒度
- ⚠ **`describeZoneProfile` は平均からのズレで言うこと**。生の値で判定すると
  多数派である「失投を逃さない」が全打者に付いて情報量が消える
- 実測（165人）: 平均 -0.345 / σ0.305。セルごとの母集団平均は
  **ゾーン中央 +0.283 / 四隅 -0.055（差 0.338）**
- リーグは打者側へわずかに動く（同一ロスター反復10シード）:
  打率 **+0.0029 ± 0.0010** / 長打率 +0.0053 ± 0.0014 / 防御率 +0.089 ± 0.035。
  centering で平均は0なのに動くのは、**打者が得意セルをより振る**
  （`swingProbability` の `zoneWeakness`）ため甘い球のスイングが増えるから。
  実NPBの打率.247・失点3.70へ近づく向きなので戻していない

**ど真ん中の失投を打った結果（ミート58/パワー55固定・`middle` だけ変える）**

| `middle` | 実効ミート/パワー | 空振り | 安打率 | 本塁打率 | 塁打/打球 |
|---|---|---|---|---|---|
| -0.7（失投を逃さない） | 65/74 | 9.2% | 66.9% | **12.49%** | **1.120** |
| 0 | 61/61 | 16.1% | 64.3% | 7.29% | 0.941 |
| +0.7（打ち損じる） | 56/48 | 22.4% | 61.4% | **3.02%** | **0.785** |

同じ能力値でも失投の仕留め方が**本塁打率で4倍**違う。
リーグ成績は動かない（同一ロスター反復10シード: 打率 +0.0008 / 防御率 -0.007、誤差内）。
  - **軸はストライクゾーンの3列が -1 / 0 / +1**（`colAxis = clamp(col-2, -1, 1)`）。
    `(col-2)/2` にすると枠内が -0.5〜+0.5 しか取れず、フル強度のセルが全部ボール球になる。
    打者の内外角の得手不得手は「ゾーンの内寄り／外寄り」の話なので枠内で振り切ること
  - 実効ミート `-weakness × 13` / パワー `-weakness × 10`。
    `corner`(-7/-5) と同程度に留めると平均的な打者で実効ミートが±3しか動かず、
    弱点という言葉に見合わなかった（実データの打者はゾーン内の得意／苦手で打率が倍近く違う）
- **グリッドは打者から見た向き**。col 0 が外角・col 4 が内角で、左打者でも同じ。
  捕手も打者を見て要求するので、この向きなら左右で場合分けする必要がない
- **セーブに持たず、選手の名前+IDから決定的に導出する**（FNV-1a ハッシュ → Irwin-Hall で釣鐘型）
  - 生成箇所は高校生プール・大学プール・トライアウト・社会人初期化…と散在しており、
    左右の比率(`handedness.js`)で「生成元ごとに値がバラつく」事故を既に一度起こしている。
    ハッシュ導出なら**生成側を1行も触らずに全プールの全選手が持て、既存セーブもそのまま動く**
  - `player.zoneProfile` を明示的に置けばそちらが優先される（将来の矯正・検証用）
- **`inside` にはミート／パワーのバランスから来る成分を足す**
  （`tendencyInside`。詳細は下の「詰まる・泳ぐ」）
- **高低の幅は内外角と揃える**（`LOW_W = 1.19`）。`inside` はハッシュ(σ0.344)に
  `tendencyInside` が乗って σ0.408 になるのに、`low` はハッシュだけで 0.344 しか
  なかった。さらに inside の上乗せは**選手のタイプと相関する**ので、打者を並べると
  内外角の傾きだけが systematic に見え、「得意コースが内か外かしか無い」
  という印象になっていた。上下対称なので平均は0のままでリーグ成績は動かない
- **実測の分布（165人）**: inside 平均-0.011 / σ0.418 / low σ0.433。
  `describeZoneProfile` が何か言う割合は 内外角56% / 高低62% / 真ん中54% で拮抗
  （以前は高低の言及が明確に少なかった）

### 「詰まる」「泳ぐ」をミート／パワーから導く
`inside += ((meet - power) - 10) / 100`

- **パワーが無い技巧派 → 内角を捌けない（詰まる）** / **ミートが無い長距離砲 → 外角で泳ぐ**
- これが無いと、内角は「引っ張り方向に飛ばす球」でしかなくなり、
  **パワー35の打者でも引っ張りの飛距離ボーナスを受け取ってしまう**。
  実測でミート75/パワー35の打者は 内角の塁打/打球 0.451 対 外角0.394 と
  **内角の方が長打になっていた**（現実の「技巧派は内角で詰まらせる」と逆）。
  打者のタイプに関わらず「常に外角が正解」で、内外角の使い分けが成立していなかった
- **中心を10ずらす**。生成される野手はミートがパワーより平均11高いので、
  素の差を使うと全員が「内角に弱い」側に寄る（実測 平均+0.108）。
  実データの打者は平均するとむしろ外角が苦手なので向きとしても逆になる。
  これは絶対値ではなく**打者間の相対差**を表す項なので平均を引く
- **`inside` に足すだけ**なので、段階2の打撃補正・段階3の捕手の弱点狙い・
  選手詳細のヒートマップに追加の配線なしで反映される
- 成長でミート・パワーが動いたらキャッシュを作り直す

**実測（3リーグ×100試合・全員右打者・ハッシュ由来は0に固定）**

| 打者 | 来た球 | 投球数 | 空振り率 | 安打率(打球) | 塁打/打球 | 本塁打率 |
|---|---|---|---|---|---|---|
| ミート75/パワー35 | 内角 | 90571 | **12.4%** | 33.2% | **0.407** | 1.03% |
| （詰まる） | 外角 | 67263 | 10.2% | 34.4% | 0.432 | 0.74% |
| ミート40/パワー75 | 内角 | 68598 | 32.4% | 38.4% | **0.728** | 9.23% |
| （泳ぐ） | 外角 | 98329 | **41.8%** | 29.5% | **0.417** | 2.02% |

**技巧派には内角、長距離砲には外角**という型別の答えが初めて成立した。
投球数の偏り（技巧派に内角90571対67263 / 長距離砲に外角98329対68598）は
段階3の捕手が実際にタイプを突いている証拠。
- 長距離砲を外角に逃がす効果が圧倒的（塁打/打球 0.728→0.417、本塁打 9.23%→2.02%）。
  技巧派に内角の効果は小さい（0.432→0.407）が、向きは正しくなった
- 技巧派は内角でも**本塁打だけは増える**（0.74%→1.03%）。
  詰まっても引っ張り方向はフェンスが近い（ポール際96m）ため。総合的な被害は減る
- リーグ成績は動かない（打率.2388 / 長打.3507 / 四球8.7% / 三振22.6% / 失点3.58）
- **UI**: 選手詳細の能力タブに5×5のヒートマップ（赤=得意 / 青=苦手、枠内がストライクゾーン）
  - ⚠ **`zoneHeatmap` の `withLocation` で2つの意味を出し分ける**。用途が違う:
    選手詳細は `true` で**コースそのものの難しさ**（`getPitchQualityEffect` の
    失投+5 / 隅-7 / 枠外-10）を混ぜる。混ぜないと平均的な打者の四隅が中立に見えて
    「隅は誰でも苦手」が絵に出ない。試合画面の重ね塗りは `false` で
    **この打者固有の偏りだけ**——共通の地形を混ぜると全打者が同じ絵になって
    段階3の意味が消えるうえ、塗りが濃くなってマーカーが読めなくなる
- **打者は自分の得意コースをより振る**（`swingProbability` の `zoneWeakness`）。
  「得意コースは強く打てる」だけがあって「得意コースだから振る」が無く、
  打者はどのコースにも同じ確率でバットを出していた。
  `p *= 1 - weakness × 0.20` で ホット×1.20 / コールド×0.80。
  実測のスイング率は **得意52.8% 対 苦手41.6%（11.2pt差）** で実データの約10ptに一致。
  母集団の平均は0なので設計上リーグ平均は動かないが、捕手が弱点を突くぶん
  見送りが増え、四球・見逃し三振がそれぞれ0.4〜0.6pt増える（下記）

**段階2だけならリーグ成績は動かない（12リーグ×120試合、旧係数での実測）**

| | 段階2あり | なし |
|---|---|---|
| 打率 / 出塁 / 長打 | .2397 / .3081 / .3597 | .2394 / .3082 / .3587 |
| 四球率 / 三振率 | 9.00% / 21.2% | 9.04% / 21.5% |
| BABIP / 防御率 | .2919 / 3.434 | .2929 / 3.412 |

母集団の平均が0で、捕手がまだ弱点を狙わないので投球位置は適性と独立。
**設計上リーグ平均は動かない**。実測もすべて標準誤差内だった。

## 捕手の弱点狙い（25分割グリッド 段階3）
`pickTargetCell(aim, { profile, lead })`。**同じ狙い(aim)の中で「どのセルを要求するか」
だけ**を打者の弱点で変える（softmax、`TARGET_BETA = 2.2`）。リード0で完全に一様＝従来と同じ。

- **`zone` / `edge` / `chase` の配分には一切手を付けない**。ここが最大の設計点。
  配分を動かす（＝際どく要求する）案は過去に7通り試して**全て四球のコストが利得を上回った**。
  同じ狙いの中での選び直しならボールになる確率が変わらないので、四球のコストが発生しない
- **誘い球(`chase`)は対象外**。誘い球のセルはどれも「枠のすぐ外」だが
  ストライクになる確率が揃っていない（`[0,2]`は列を1つ戻せばストライク、
  `[0,3]`は行が4へ流れてボールになりやすい）。弱点狙いは後者のような角寄りのセルを
  選びがちなので、配分を変えていないつもりでも四球が増える。
  **実測で BB/9 が +0.34 悪化し、防御率の利得を丸ごと食い潰した**
- **制球によるゲートは要らない**。制球が低い投手は目標セルからのばらつきが大きく
  狙いが洗い流されるので、「投げ切れる投手にだけ効く」関係が自然に出る

**⚠ 計測は「同じロスターのリーグを2回走らせる」設計で行うこと**（`structuredClone`で
ロスターを退避→リードだけ変えて再実行）。良い捕手3チーム対悪い捕手3チームの分割だと
**ロスターの引きの差がノイズの主成分**になり、30シードでも標準誤差0.065止まりで
符号が run ごとに反転した。同一ロスター反復なら16シードで標準誤差0.034 になる。

**リード18→81（実在する範囲・16シード×120試合・同一ロスター反復）**

| | 段階3あり | なし | 段階3の寄与 |
|---|---|---|---|
| 防御率 | 3.455 → 3.177（**-0.277 ± 0.034**） | 3.331 → 3.228（-0.103 ± 0.041） | **-0.174 ± 0.053** |
| BB/9 | +0.004 ± 0.034 | +0.006 ± 0.039 | **±0（四球のコストなし）** |
| K/9 | **+0.568 ± 0.025** | +0.448 ± 0.032 | +0.120 ± 0.041 |
| 被安打/9 | **-0.425 ± 0.043** | -0.226 ± 0.043 | -0.199 ± 0.061 |

**実在する捕手の幅でリードの価値が2.7倍になった**（防御率 -0.103 → -0.277）。
リード0→100 では 防御率 -0.382 ± 0.030 / K/9 +0.779 / 被安打/9 -0.574 / BB/9 ±0。

**打者から見た効き（実際の分布・4リーグ×100試合）**

| 来た球 | 投球数 | 空振り率 | 被安打率(打球) | 塁打/打球 | 本塁打率 |
|---|---|---|---|---|---|
| 弱点側 | 40564 | **29.1%** | 29.7% | 0.383 | **1.19%** |
| 中間 | 236670 | 18.0% | 33.1% | 0.490 | 3.00% |
| 得意側 | 19565 | **13.7%** | 34.7% | 0.555 | **4.60%** |

投球数が弱点側40564対得意側19565と**2倍以上偏っている**のが段階3の働きそのもの。
苦手コースでは本塁打が4分の1になる。

**リーグ全体はやや投手寄りに動く（12リーグ×120試合・NPB相当）**

| | 段階3あり | なし |
|---|---|---|
| 打率 / 出塁 / 長打 | .2339 / .3033 / .3491 | .2397 / .3081 / .3597 |
| 四球率 / 三振率 | 9.07% / **22.4%** | 9.00% / 21.2% |
| BABIP / 失点 | .2904 / 3.605 | .2919 / 3.750 |

リーグ平均のリードが46前後あるため、**全捕手がある程度は弱点を突く**ぶん打者が不利になる。
これは仕様であってバグではない。**三振率が22.4%になり、段階1で開いた「実データより
1.1pt低い」乖離が閉じた**。打率は .240→.234 と実NPB(.247)から少し離れたが、
本作の歴代の測定値(.234〜.240)の範囲内で、season-check も全項目PASSしている。

## ⚠ 打撃側の再較正（`windowCoef` 0.40 → 0.42）
段階1〜8で捕手が段階的に賢くなった（弱点を突く・場面で目的を変える・
球種に合ったコースを要求する）。**どれも「捕手が良い仕事をする」方向**なので、
全捕手がそれを行うぶんリーグ全体が投手寄りに累積していた。

| NPB相当の設定 | 打率 | 三振率 | 失点 |
|---|---|---|---|
| 段階2の時点 | .2397 | 21.2% | 3.75 |
| 段階8の時点（0.40のまま） | .2334 | **23.5%** | 3.46 |
| **再較正後（0.42）** | **.2386** | **21.9%** | **3.69** |

三振率が実データ帯(19-22%)を超えていたのを戻した。失点3.69は実NPB 3.70 に一致。
**打者個々の能力ではなく物理の基準を動かしている**ので、能力の相対関係・
各機構の効果量（リードの価値、型ごとの差、コース別の傾向）はどれも不変。

自然ロスターの season-check: 打率.254 / 出塁.349 / 長打.324 / 防御率3.69 /
K/9 7.89 / BB/9 4.69 / 三振率19.5% / 四球率11.8% / 死球0.96% / 本塁打0.49。

⚠ **四球率は 10.8% ではなく 11.8% が正しい**。集計側のバグ（下記「死球」の節）で
「0打数2四球」のような試合の四球がシーズン成績から丸ごと落ちており、
打撃側だけ約8%低く出ていた。投手側の BB/9 4.7（＝打席の約12.4%）が常に正しく、
2つが食い違っていた。実NPBの8.5%より高いのは既知の課題で、これは変わらない。

**今後コース系の機構を足すときは、リーグが投手寄りに寄っていないか必ず確認すること。**
1つ1つは小さくても、捕手側にばかり足すと累積する。

## 球種ごとの「投げやすいコース」（`src/game/pitchShape.js` / 段階8）
投球位置は球種と無関係だった。捕手はどの球種でも同じセルを要求でき、
投手はどのセルにも同じ精度で投げられた。実際は球種ごとに自然な高さと横方向がある。

- **高さ**: ストレートは高め、変化球は低めが投げやすい（`NATURAL_LOW`）。
  `getHeightPitchEffect`（高めの速球・低めの変化球は空振りが取れる）と同じ向きなので、
  「効く高さ」と「投げやすい高さ」が一致し、配球の基本形が自然に出る
- **横**: 球種は腕側かグラブ側へ逃げる（`ARM_SIDE`）。
  スライダー/カッターはグラブ側、シュート/シンカー/ツーシームは腕側。
  グリッドは打者から見た向きなので `armSide × (同じ利き手 ? +1 : -1)` で変換できる
- **変化球レベル**: 高いほど σ が小さい（狙ったところに決まる）
- 捕手は球種に合ったセルを要求する（`shapeCallScore`）。σ も自然な側で小さくなる

**実測（3リーグ×80試合・制球60固定）**

| 球種 | 対同じ利き手 | 対逆の利き手 | 平均高さ |
|---|---|---|---|
| スライダー | **-0.224**（外角） | **+0.223**（内角） | +0.25 |
| シュート | **+0.204**（内角） | **-0.211**（外角） | +0.20 |
| シンカー | +0.146 | -0.175 | +0.33 |
| ツーシーム | +0.152 | -0.200 | +0.09 |
| フォーク | -0.01 | -0.01 | +0.31 |
| ストレート | -0.014 | -0.015 | **-0.10**（高め） |

左右で綺麗に反転し、縦系（フォーク・スプリッター・ナックル）は横に動かない。
**変化球レベル別の「狙ったセルにそのまま決まった率」**: レベル0-39 15.0% /
40-69 16.7% / 70+ 19.3%。

### ⚠ 踏んだ罠2つ（どちらも既知パターンの再発）
1. **誘い球(chase)にも球種の好みを掛けてしまった**。誘い球のセルはストライクに
   なる確率が揃っていないので、偏らせると四球が増える（段階3で同じ罠を踏んでいる）。
   実測で四球率 9.3%→**10.3%**。chase を除外して解決
2. **σ の平均が下がってリーグ全体の実質制球が上がった**。捕手が自然なコースを
   要求するぶん `fit` の平均が +0.115 に寄るため。防御率が 3.36→**3.18** に落ちた。
   `FIT_MEAN` を引いて「自然なコースへ要求できたぶんの差」だけが出るようにした

### 変化球の制球ペナルティを σ に一本化した
旧 `breakingControlPenalty` は制球値から引いていたが、
**自動シミュ (100-level)×0.20 / 采配モード (100-level)×0.30 と係数が違っていた**。
さらに采配モードだけ `callPitchTarget` と `swingProbability` にも影響していた。
`pitchShape.shapeSigma` の `LEVEL_SIGMA_W` に集約して両エンジンを揃えた。

## 場面ごとの投球目的（`src/game/pitchSituation.js` / 段階6）
段階1〜5で「どこに何を投げるか」「それがどう打たれるか」は繋がったが、
**捕手はいつでも同じ目的で配球していた**。実際は場面で欲しい結果が変わる。

| 場面 | 目的 | 要求 |
|---|---|---|
| 一塁に走者・2アウト未満 | 併殺狙い | 低め + ゴロ系の球種 + ゾーンで勝負 |
| 三塁に走者・2アウト未満 | 三振狙い | 速球=高め / 変化球=低め + 空振り系 + 誘い球増 |
| 満塁・0アウト | 三振狙い（押し出し回避） | 同上だが**誘い球を減らしてゾーンへ** |
| 2アウト | 通常 | 併殺も犠飛も関係ない |

- **三塁に走者でも、一塁が埋まっていて1アウトなら併殺狙いに戻る**（併殺でチェンジ）。
  「三塁走者＝常に三振狙い」にするとこの当たり前の判断が抜ける
- **3ボールでは場面補正を掛けない**。四球・押し出しの回避が最優先
- **リードで割り引かない**。「一塁に走者だから低め」は巧拙ではなく野球の基本。
  リードが効くのは「同じ低めの中でどのセルか（弱点・対角）」の部分

### ⚠ 三振狙いの「高め」は球種と噛み合わせること
最初の実装では三振狙いを一律に高めへ要求したが、**空振り率がむしろ下がった
（9.6% → 8.4%）**。高めの変化球はただの「抜け球」で最も打たれるため。
`getHeightPitchEffect`（`pitchCalling.js`）で高さと球種の相性を入れ、
`objectiveRowScore` が球種で高低を反転させるようにして解決した。

| | 高め | 低め |
|---|---|---|
| 速球 | **空振りが取れる** | 抜け球と同じで打たれる |
| 変化球 | 抜けたスライダー。長打 | **落ちる球。空振りが取れる** |

実効ミート `∓6` / パワー `∓4`。対称なので母集団の平均は0＝リーグ成績は動かない。

**実測（4リーグ×120試合。場面でバケツ分けし、補正の有無で比較）**

| 場面 | | ボール率 | 空振り率 | ゴロアウト/打球 |
|---|---|---|---|---|
| 一塁に走者 | あり | **34.8%** | 9.1% | **27.0%** |
| | なし | 35.9% | 9.5% | 25.1% |
| 三塁に走者 | あり | 36.4% | 9.4% | 26.9% |
| | なし | 36.7% | 9.2% | 25.7% |
| 走者なし | あり/なし | 35.3% / 35.3% | 10.5% / 10.5% | 25.4% / 25.4% |

走者なし・2アウトの列が完全に一致しているのが**変更が意図した場面だけに
閉じている証拠**。一塁に走者ではゴロが +1.9pt（3σ）増え、四球が減っている。
- **併殺数の増加は分離できなかった**。併殺は打球の2.3%しかなく、
  ゴロ+7.6%相当の増加(2.31→2.49%)はこの標本の誤差 ±0.22pt に埋もれる
- 目的別に切ると三振狙いは 空振り 9.6%→**10.3%** / インプレー率 17.7%→16.8%、
  満塁は ボール率 35.6%→**33.5%**（狙いどおり四球を避けている）

**リーグ全体（8シード×120試合の対比較）**

| | 場面補正あり | なし |
|---|---|---|
| 防御率 | 3.307 ± 0.035 | 3.418 ± 0.040 |
| 四球率 | **8.65%** | 9.02% |
| 三振率 | 22.39% | 22.20% |
| 打率 / 長打 | .2417 / .3531 | .2409 / .3542 |

**防御率 -0.111 ± 0.053。四球はむしろ減る**（併殺狙いの場面が最も多く、
そこでは勝負するため）。失点は 3.705 で実NPB 3.70 に一致した。

### 併殺の既知の問題
- **2アウトからの併殺で「4アウト目」が記録されていた**（`outs < 2` の判定漏れ）。
  投手の投球回がわずかに水増しされていた。修正済み
- 修正後の併殺は **0.12/試合**。実NPB は約0.7/試合で**明らかに少ない**。
  `dpBase = 15 + (内野守備-50)×0.35` と `distance < 40` の条件が厳しいため。
  別途の較正が必要（段階6とは独立の課題）

## 投球コース → 打球傾向（`src/simulation-logic.js` / 段階5）
段階1〜4で「どこに投げるか」は表現できたが、**その球がどう打たれるか**は
コースと無関係だった。内角も外角も同じ方向に飛び、高めも低めも同じ角度で上がっていた。

- **内角 → 引っ張り / 外角 → 流し打ち**: `direction += -colAxis × 8度 × batSide`
  - 引っ張りは右打者=三塁側(負) / 左打者=一塁側(正)。グリッドは打者から見た向き
    （col 4 = その打者の内角）なので、`batSide` を掛けるだけで左右が揃う
- **低め → ゴロ / 高め → フライ**: `launchAngle += -rowAxis × 7.5度`
- **中心は「打球になった投球」の平均コース**。実測すると両方ほぼ0だった
  （13048打球で colAxis -0.001 / rowAxis +0.009）。誘い球は外角低めに偏っているが
  そもそも打球になりにくいので効いてこない。**誘い球の配分や弱点狙いを変えたら測り直すこと**
  - 四隅バイアス（`MIDDLE_BIAS`）を入れた後に再測定: colAxis +0.0045 / rowAxis +0.0105。
    定数（0.00 / 0.01）のままで良い
- **スイッチヒッターの打席判定が壊れていた**。`pitcher.throwingArm` を見ていたが
  そのフィールドは誰も設定しておらず（実際は `throws`）、スイッチは常に右打者扱いだった。
  さらに左右も逆（左投手のとき左打席になっていた）

**⚠ 「引っ張り率」は定義で数字が変わる**。実データと比べるときは
**三分割**（引っ張り <-15度 / センター / 流し >+15度）で揃えること。
左右どちらの半面かで数えると同じ状態が 60% 対 84% のように見える。
CLAUDE.md には一時、左右半分で測った値（内角69.5%）と実データの三分割値（約60%）を
並べて「ほぼ同じ」と書いていた。

**実測（8チーム×110試合×4シード・打球78312個）**

| 来た球 | 引っ張り | センター | 流し | | 高さ | 平均打出し角 | ゴロ率 | フライ率 |
|---|---|---|---|---|---|---|---|---|
| 内角 | **53.8%** | 38.6% | 7.6% | | 高め | 19.7度 | **35.1%** | 40.9% |
| 真ん中 | 36.4% | 50.9% | 12.7% | | 真ん中 | 14.4度 | 46.4% | 33.6% |
| 外角 | **24.3%** | 51.4% | 24.3% | | 低め | 8.9度 | **56.8%** | 25.2% |
| リーグ全体 | 38.5% | 47.5% | 14.0% | | | | | |

内外角の差 **29.5pt**（実MLB 約30pt）/ 高低のゴロ率の差 **21.6pt**（実MLB 約22pt）。
リーグ全体の引っ張り率 38.5% は実MLB 38-42% に一致。

- ⚠ かつて「**打出し角の係数は5度が上限**。6度にすると打球種別が
  ゴロ42.9/ライナー27.1 まで崩れる」と記録していたが、**その後
  `calculateLaunchAngle` を書き直した（バレル帯を `BARREL_LO`/`BARREL_W` で持ち、
  各分岐の上端を50度台まで伸ばした）ので、もう当てはまらない**。
  7.5度での実測は ゴロ46.2 / ライナー21.2 / フライ26.6 / ポップ6.0 で、
  5度のとき（47.7/20.1/27.2/5.0）より**むしろ実データ(44/20/27/8)に近い**。
  本塁打も 0.624 → 0.654 と実NPB 0.70 へ寄る。
  **古い⚠を根拠に係数を戻さないこと。触る前に測ること**
- どちらの項も中心が0なので**リーグ総量は動かない**
  （同一ロスター反復10シード: 打率 +0.0013 ± 0.0012 / 防御率 +0.038 ± 0.043 /
  本塁打 -0.0016 ± 0.0088、すべて誤差内）
- **本塁打は 0.79 → 0.69 に減る**。打出し角に±7.5度の揺らぎが入ると
  バレル帯(26-34度)から外れる打球が増えるため。CLAUDE.mdの「帯を広げるとHRが2割減る」と
  同じ現象で、結果的に実NPBの0.70に一致した
- リーグ成績: 打率 .2366 / 出塁 .3055 / 長打 .3463 / 四球9.0% / 三振22.3% / 失点3.56。
  段階4から長打率が .357→.346、防御率が 3.34→3.23 と投手寄りに動いた

## 配球の3次元（`src/game/pitchSequence.js` / 段階4）
段階1〜3で1球ごとの位置と球種は表現できたが、**前の球との関係**が無かった。
実際の配球は「内角高め→外角低め」のように対角へ動かし、速球のあとに変化球を置いて
上下・左右・奥行きの3次元で揺さぶる。打者は前球からの動きの大きさに対応させられる。

### ⚠ 前提: 自動シミュレーションでは前球の情報が常に null だった
`simulateOnePitch` が `result.lastPitch` を一度も返しておらず、
**緩急ペナルティ(`speedDiffPenalty`)もトンネリング効果(`getTunnelingEffect`)も
一度も発火していなかった**。奥行きの次元は仕様として存在するだけで、
リーグ成績には無関係だった（`BALL_EFFECTS` が物理エンジンに繋がっていなかったのと同じ構図）。
`speedDiffPenalty` はさらに、前球ではなく**その投手のストレート**と比較していた。

- **打席ごとの配球メモリ**（`createSequence`）。直近**4球**の (col, row, 速球/変化球) を保持
  - 4球必要。3球だと内角高め↔外角低めの往復で直近が `[B,A,B]` になり、次のAの重複が
    1回にしか見えない。4球なら `[A,B,A,B]` で2回と数えられる
- **`sequenceShift(prev, cur)`** = 前球からどれだけ動いたか（0〜1）
  - 左右0.28 + 高低0.28 + 奥行き0.24 + **対角0.30**（左右と高低が*両方*動いた分）
  - 内角高め→外角低めは4項すべてが立って最大になる。同じ所へ2球続けると0
- **基準はリード0（完全ランダムに要求した場合）の平均シフト量 0.444**。
  ここを0点にすることで「でたらめに散らしただけでは何も得しない」形になる。
  実測: リード0で0.444 / リード46で0.478 / リード100で0.519
- **読まれる**（`locationReadChance`）: 直近4球で同じ引き出し(外中内×高中低×速球変化球=18通り)を
  使うほど打者が読む。的中の効果は球種の読みと同じ（タイミング窓1.3倍）
- 捕手側は `sequenceScore` を弱点狙いのスコアに足し込む。**リード0なら全部効かない**

### 「毎回対角に攻めるのも善し悪し」は内部最適として出る
リード81・制球85・同一ロスター反復10シードで、捕手の方針だけを変えた比較:

| 方針 | 防御率 | 現行との差 |
|---|---|---|
| 弱点のみ（対角を使わない） | 2.136 | **+0.040 ± 0.025** |
| 対角＋散らす（現行 MOVE_W=1.0） | 2.095 | — |
| 対角を最大化（MOVE_W=2.5・散らさない） | 2.126 | **+0.030 ± 0.028** |
| 散らしを重く（REPEAT_W=1.0） | 2.109 | +0.014 ± 0.029 |

**対角を使わないのも、対角に振り切るのも悪い。** 中間が最善という関係になっている。
- ⚠ この関係は `READ_MAX` で決まる。0.34 では「対角を最大化・散らさない」が最善のままで、
  読まれるコストが対角の利得を下回っていた。0.60 にして初めて内部最適が生まれる
- `REPEAT_W`（散らしを明示的に狙う項）は**単独では効果が測れない**（0にしても
  -0.030 ± 0.047）。対角へ動かせば自然に引き出しも変わるため仕事が重複している

### 効くのは奥行き。左右・高低の対角そのものは制球次第
制球60（リーグ相当）では配球方針を変えても防御率が動かない（全方針 3.19〜3.24）。
**目標セルからのばらつき(σ0.78)が設計した並びを洗い流す**ため。
制球85では上表のとおり方針差がはっきり出る。

**リード18→81 の推移（同一ロスター反復・16シード×120試合）**

| | 段階3前 | 段階3 | 段階4 |
|---|---|---|---|
| 防御率 | -0.103 ± 0.041 | -0.277 ± 0.034 | **-0.309 ± 0.048** |
| K/9 | +0.448 ± 0.032 | +0.568 ± 0.025 | **+0.771 ± 0.042** |
| 被安打/9 | -0.226 | -0.425 | **-0.490 ± 0.061** |
| BB/9 | +0.006 | +0.004 | +0.036 ± 0.037 |

段階4の防御率への上乗せ -0.032 ± 0.061 は**誤差内**。確実に言えるのは
**K/9 が +0.568 → +0.771 と増えた**ことと、四球のコストが依然ゼロであること。
リーグ成績はほぼ動かない（打率 .234→.237、三振率 22.4%→22.4%、防御率 3.30→3.34）。

## 配球と投球位置（`src/game/pitchCalling.js`）
**采配モードと自動シミュレーションで共有する**1球ごとのモデル。以前は2エンジンが
別式を持ち、ゾーン率が采配64%／自動48%、chase率が采配30%／自動12%と別世界だった。

- **制球は「ストライク率」ではなく「狙った所へ投げられる再現性」**
  1. `callPitchTarget()` — 捕手が狙いを決める（`zone`=勝負 / `edge`=際どく / `chase`=誘い球）
     - カウント別: 3ボールはzone66% / 2ストライクはchase38% / 平行カウントはedge48%
     - 捕手のリードが高いほどedgeを要求でき、打者の選球眼が高いとchaseを避ける
  2. `resolvePitchLocation()` — 制球で狙い通りに行くかが決まる
     - 狙い通り → ストライク or 有効な釣り球 / 外れる → 四球、あるいは`meatball`（失投）
     - 失投は打者能力+5/+3、際どいコースは-10/-8 で長打・打ち損じに直結する
     - フレーミングもここ（捕手の守備力で際どい球の判定が動く。基準はリーグ平均50）
  3. `swingProbability()` / `decideSwing()` — 打者が振るか
     - **ゾーン内スイングはカウントが進むほど増える**。旧実装は逆向き（2ストライクで減る）だった
     - `chase`（ボール球スイング）は制球にも依存する。**制球の良い投手のボール球は
       ストライクに見えるため振ってもらえる** → ストライク率70%超に到達する経路
- **ボール球でも打球になる**（`ballZoneContactChance` 約75%）。自動シミュレーションは
  従来「20%ファウル/80%空振り」で打球が一切発生せず、chase率を上げると三振だけが増えた
- **ファウルが打席を長引かせる**。実データでは全投球の17-18%がファウル。これが無いと
  コンタクトが即座に打席を終わらせ、四球が構造的に出ない
- **実測（制球60/ミート58/パワー55/選球眼52/守備60 = NPB相当）**
  | | 本作 | 実NPB |
  |---|---|---|
  | 打率 / 出塁 / 長打 | .240 / .309 / .370 | .247 / .316 / .366 |
  | 四球率 / 三振率 | 9.0% / 21.3% | 8.5% / 19-22% |
  | BABIP / 失点 | .290 / 3.51 | .290 / 3.70 |
  - 1球あたり: ボール35.5% 見逃し14.6% 空振り9.0% ファウル23% インプレー17.5%
    （実MLB 36 / 17 / 10 / 17 / 20。ファウルだけやや多いのが既知の乖離）
- **制球の効きは非線形**（`cc()`。リーグ平均の制球57を支点に、離れるほど効きを強める）
  - 四球は1球ごとのボール率に指数的に反応するため、線形にすると幅が出ない
  - 効き: 制球20→×1.34 / 40→×1.04 / 57→0 / 80→×1.87 / 100→×2.20
- **制球別の9イニングあたり四球（打者ミート55/選球眼50で実測）**
  | 制球 | 20 | 40 | 60 | 80 | 100 |
  |---|---|---|---|---|---|
  | BB/9 | 7.3 | 5.2 | 3.5 | 2.1 | 0.69 |
  | ストライク率 | 56.2% | 59.5% | 63.4% | 67.8% | 76.0% |
  | ゾーン率 | 42.1% | 44.4% | 47.2% | 50.4% | 57.2% |
  | chase | 24.4% | 27.1% | 30.7% | 35.1% | 43.8% |
  - **上限の基準は加藤貴之2022**（148回2/3で与四球11 ＝ BB/9 0.67）。制球100＝0.69がこれに当たる
  - ゾーン率は制球30〜90が実データの44-55%内。両端がわずかに外れる（制球20で42%・100で57%）
  - **既知の乖離**: 同じ四球数を出すのに実データより6ptほど高いストライク率が必要
    （加藤は約70%で0.67個、本作は76%で0.69個）。ファウル率が実データより高く
    打席が長い分、1球あたりのボール率が同じでも四球が出やすいことが原因
- **球種の選択も2エンジンで共有**（`selectPitchType`）。従来は采配モードだけが
  捕手のリードでスコア選択し、自動シミュレーションは変化球を完全ランダムに選んでいた
  - ストレートか変化球かは球種構成と投球方針で決め、**リードは「どの変化球か」に効く**。
    リードでストレートまで含めて最良の1球を選ばせると、ストレートはスコア上常に最下位
    （whiff 0 / gb 0 / weak -0.04）なので速球率が実データの45-55%から大きく外れる
  - `BALL_EFFECTS` を物理エンジンに繋いだ後は、この経路が実際に効く
    （制球70で「常に最良」と「ランダム」の防御率差 -0.253 ± 0.096、有意）
### ⚠ 捕手の球種スコアは物理と同じ物差しで測ること（`scoreBall`）
`whiff + gb + weak` の**単純合計**だった。三振とゴロを同じ価値と見なしており、
実測の防御率差との**順位相関が -0.500**——つまり**良い捕手ほど価値の低い球を
要求していた**。捕手の1位はシュート（実測7位）、カーブ（実測1位）は8位。

**type × level のグリッド実測に回帰して重みを決めてある**
（6球種 × Lv25/55/100 の18点。ストレート＋1球種で1シーズン回し、
ストレートのみとの防御率差を価値とする）。R²=0.974 / 順位相関 **+0.969**。
独立した9球種Lv50のデータでも **+0.967** と汎化する（旧実装は -0.500、
その次の版は +0.907）。

| 項 | 最初 | 途中 | **現在** | 根拠 |
|---|---|---|---|---|
| **レベル（球種によらない）** | **なし** | **なし** | **0.765** | **回帰で最大の項** |
| `whiffBonus` | 1.0 | 1.0 | 0.09 | 効果項は weak が主 |
| `groundballBonus` | 1.0 | 0.2 | 0.0 | weak と相関が強く単体の寄与がほぼ無い |
| `weakBonus` | 1.0 | 0.3 | 0.68 | 効果項では最大 |
| 実際の減速量/100 | なし | 1.5 | 1.89 | 緩急 |
| 制球ペナルティ | -(1-lv)×0.20 | 同左 | **レベル項に統合** | 定数差でしかない |

#### ⚠ 習熟度の価値を4分の1に見積もっていた
「シュートLv100 がカーブLv50 に負けるのはおかしい」という指摘から実測したところ、
**実測ではシュートLv100 の方が 0.31 勝っていた**（1.080 対 0.767）のに
スコアは逆（0.154 対 0.247）だった。原因は制球ペナルティの係数 0.20 で、
回帰値は **0.765**。

**持ち球の価値（ストレートのみとの防御率差・速球142/制球55・4シード×110試合）**

| 球種 | Lv25 | Lv55 | Lv100 |
|---|---|---|---|
| シュート | 0.203 | 0.578 | **1.080** |
| カッター | 0.319 | 0.572 | 0.933 |
| シンカー | 0.335 | 0.627 | 1.014 |
| スライダー | 0.453 | 0.534 | 1.021 |
| フォーク | 0.430 | 0.746 | 1.202 |
| カーブ | **0.575** | **0.767** | 1.159 |

**Lv100はどの球種でもLv55を上回る**（0.93〜1.20 対 0.53〜0.77）。
球種の性格が効くのは同じレベルで比べたときで、レベル差はそれを覆す。
低レベル帯では緩急の大きいカーブが強く、高レベル帯では差が縮む。

- レベルの type非依存項は `effectiveArsenalSize` がレベルで引き出しの重みを
  変える（0.55〜1.00）のと同じ話。**よく練った球は球種を問わず価値がある**
- 場面の重み（`objectiveBallWeight`）は別に乗るので、走者一塁ではゴロ系が
  さらに優先される
- **味方の内野守備でゴロ・凡打の値打ちが変わる**（`LEAD_DEF_W = 0.010`）。
  三振は確実にアウトが取れるが球数を食い、ゴロは省エネだが**守備に預ける**。
  実測（Lv100・4シード×110試合）で三振型の優位は守備が良いほど縮む:

  | 持ち球 | 守備30 | 守備75 |
  |---|---|---|
  | フォーク | 1.572 | 1.011 |
  | カーブ | 1.472 | 0.971 |
  | シンカー | 1.265 | **0.953** |
  | シュート | 1.291 | 0.925 |
  | ツーシーム | 0.969 | 0.716 |

  カーブ−シンカーの差は **0.207 → 0.018**（守備75でほぼ並ぶ）。
  内野4人の平均守備の 50 からの差で gb/weak の重みを動かす。
  実測: リーグ平均の内野守備はちょうど **50.0**（最小33〜最大72）なので
  centering は正しく、一律の下駄は乗らない
  - 実際に要求が変わる: シンカー対カーブは守備90で、ツーシーム対スライダーは
    守備75で逆転する（どちらもLv100・リード100）
  - ⚠ **ERAへの効果は測定できていない**。リーグ総量は誤差内
    （同一ロスター反復12シード: 防御率 -0.043 ± 0.034）で、守備を30/50/75に
    揃えた比較でも機構の有無で差が出なかった（±0.03〜0.09、いずれも標準誤差内）。
    持ち球に「ゴロ系と三振系が拮抗している」状況が要るので発火機会が少ない。
    **向きは実測どおりだが、効果量は未確認**
  - 球数の節約も方向は正しいが小さい: 1イニングあたり ストレートのみ14.4 /
    フォーク14.3 / カーブ14.2 / シュート14.1 / シンカー14.0。
    実MLBは高K型と contact型で約1球違うので**本作は1/3程度**
- リーグはほぼ動かない（同一ロスター反復10シード: 防御率 +0.017 ± 0.038 /
  打率 +0.0010 ± 0.0012 / BB/9 **-0.088 ± 0.040**）。よく練った球＝制球の
  良い球を選ぶぶん四球がわずかに減る

- ⚠ **緩急の項は生の `velocityMinus` ではなく実際の減速量 `pitchVelocityDrop`
  を使うこと**。後者はレベルで少し伸びる（×0.72〜1.00）ので、生の値だと
  **未熟な遅い球が満額の緩急として評価される**。実例: スライダー20/カーブ20/
  シュート100 の投手で、カーブLv20（実際の減速17.8km）がシュートLv100 と
  同格に選ばれていた。修正後は シュート100(0.154) > カーブ20(0.128) で、
  カーブがLv50まで上がると逆転する（0.247）
- **物理と同じ倍率を掛けること**。`calculatePhysicsContact` は whiff/gb/weak に
  `breakEfficiency(到達球速)` と `formPitchBonus(フォーム,球種)` を掛けている。
  捕手側が見ないと、サイド・アンダースローでシンカーを過小評価し、遅い投手で
  曲がりの落ちた球を過大評価する。そのため `selectPitchType` に `velocity` を渡す
- **`TYPE_SIGMA`（球種そのものの投げにくさ）も引くこと**。入れないと捕手が
  ナックルを無条件に最良と見なし、リード18→81 の BB/9 が +0.210 → +0.275 に
  悪化した。入れると +0.218 に収まる
- リーグ総量は動かない（同一ロスター反復10シード: 防御率 -0.035 ± 0.033 /
  打率 -0.0007 ± 0.0012）。リードの価値（18→81 で防御率 -0.35 前後）も
  従来と同等以上だが、**差の差は12シードでは有意に取れなかった**（要注意）

### 捕手に出来ることは3つだけ（設計方針）
1. **最も効果的な球種とコースを要求する**（`selectPitchType` / `callPitchTarget`）
2. **際どい球をストライクにする**（フレーミング。`fielding.defense` が担当）
3. **盗塁を刺す**（`physical.arm`）

**失投(meatball)は投手の制球の問題であって捕手には防げない。** 一時
「良い捕手は失投を減らす」処理を入れたが前提が誤っていたため撤去した。

## 打者の狙い球は「球種」と「コース」の2本立て（段階7）
従来の狙い球は球種だけだった。コースを張る要素を足し、
**球種とコースの両方を張り当てたときだけタイミング窓が ×1.50**（片方なら従来どおり×1.30）。
`calculatePhysicsContact` の `isGuessRight` は boolean から 0/1/2 の段階値になった
（boolean を渡しても「1つ的中」として動く後方互換あり）。

**コースを読む2つの信号**（`locationReadChance`）
1. **同じ引き出しの繰り返し**（段階4から）。直近4球で同じ 外中内×高中低×速球変化球 を使うほど
2. **要求そのものの偏り**（`readSignal`。ここが新規）。捕手が弱点を突きにいくほど
   `pickTargetCell` の softmax が尖り、次にどこへ来るかが読みやすくなる

- **これが無いと弱点狙い（段階3）に代償がまったく無い**。「常に最も効くセルへ要求する」が
  無条件に最善になってしまい、配球に駆け引きが生まれない
- **判定は帯（外/中/内 × 高/中/低）で行う**。打者は「外角低め」に張るのであって
  「(1,3)のセル」に張るわけではない。セル一致にすると発火率が1割程度にしかならず機構が死ぬ
- **投手が狙った帯に投げられた球だけが対象**。目標を外した球は張っていてもそこに来ない

**実測（1リーグ×60試合・NPB相当）**

| リード/制球 | 狙い通り率 | 平均偏り | コースを読まれた率 |
|---|---|---|---|
| 0 / 60 | 7.6% | 0.026 | **3.69%** |
| 46 / 60 | 28.6% | 0.055 | 4.85% |
| 100 / 60 | 31.1% | 0.097 | **7.24%** |
| 81 / 40 | 26.9% | 0.078 | 6.25% |
| 81 / 85 | **37.9%** | 0.101 | **7.56%** |

**捕手が読むほど読まれる**（リード0→100 で読まれる率が約2倍）。
**制球が良いほど読まれる**（狙った帯に来るため 26.9%→37.9%）——
狙い通りに投げられることの利得と、読まれるコストが同時に立つ形になっている。

- リードの価値は落ちていない（18→81 で防御率 **-0.297 ± 0.031**、四球のコストなし）。
  代償は付いたが、弱点を突く利得の方が依然として大きい
- リーグ成績は打者側にわずかに戻る（防御率 3.27→3.31 / 失点 3.58→3.64 / 打率 .2388→.2391）。
  実NPBの失点3.70に近づいた

## 振り方（フルスイング / 当てにいく）— `src/game/swingType.js`
「当てるだけ／フルスイング」に相当する判定は**チーム方針のレベルにしか無かった**。
自動シミュは `offenseStrategy.batting` で ミート±3/-5・パワー+8/-5 を打席の頭から
終わりまで一律に掛けるだけ、采配モードの「打撃方針」に至ってはスイング確率を
0.55/1.0/1.3 倍するだけで、**振り方そのものは1球も変わっていなかった**。

1球ごとに `swingPower`（-1=当てにいく 〜 +1=フルスイング）を決める。

| 要因 | 寄与 |
|---|---|
| コース適性 | `-weakness × 0.85`（**得意コースならフルスイング**） |
| 2ストライク | -0.60（追い込まれたら当てにいく） |
| **前球で崩された** | `-fooled × 0.50`（下記） |
| 打者有利カウント | 3-0/2-0 は +0.40、1-0/3-1 は +0.20、0-1 は -0.15 |
| 打者のタイプ | `((パワー-ミート) + 11) / 100 × 1.2`（長距離砲は振り抜く） |
| 打撃方針 | 積極/aggressive +0.35 / 待て/patient -0.30 |

### 崩された打者はゾーンを広げる（`FOOLED_CHASE`）
`fooled` は長らく**打者の振り方を決めるだけ**で、見極めには一切効いていなかった。
泳がされた次の球で、打者が平然といつもどおりの選球をしていたことになる。
守勢に入った打者はボール球に手を出す、を `swingProbability` に足す。

**スイング率（選球眼50・1ストライク）**

| コース | 通常 | 崩れ0.5 | 崩れ1.0（空振り直後） |
|---|---|---|---|
| ゾーン内 | 74.0% | 75.5% | 77.0% |
| 枠のすぐ外(edge) | 34.7% | 37.7% | **40.7%** |
| 明らかなボール | 10.5% | 11.6% | 12.7% |

- ⚠ **`aim` の配分（zone/edge/chase）は動かさないこと**。配分を動かす案は過去に
  7通り試して全て四球のコストが利得を上回った。**打者側の見極めを変える**なら
  捕手が余分にボール球を投げるわけではないので、四球のコストが出ない。
  「崩した直後に捕手の既存の誘い球がよく効く」という形で畳み掛けが成立する
- 崩れ状態は読み判定の **20.9%** で発生（空振り直後の最大崩れが6.9%）
- **係数は sublinear**。0.06 でも 0.16 の効果の7割が出る（四球は1球ごとの
  ボール率に指数的に反応するため）。リーグの動きが最小の 0.06 を採用

**リーグへの影響（同一ロスター反復8シード×120試合）**

| | 機構なし | あり(0.06) | 差 ± 標準誤差 |
|---|---|---|---|
| 打率 | .2396 | .2393 | -0.0003 ± 0.0018 |
| 出塁率 | .3277 | .3240 | **-0.0037 ± 0.0018** |
| BB/9 | 4.106 | 3.927 | **-0.179 ± 0.054** |
| K/9 | 7.810 | 7.699 | -0.111 ± 0.060 |
| 防御率 | 3.364 | 3.263 | -0.101 ± 0.063 |

**四球が減って打球が増える**（崩された打者が自分から凡打する）。
四球率は 10.9% → 10.5% と長年の課題（実NPB 8.5%）に対して**実データ側へ動く**。

#### ⚠ 先に試して捨てた2案（同じ轍を踏まないこと）
1. **崩された打者は読めない**（`guessSuccessRate` / `locationReadChance` を鈍らせる）
   ＋**崩されたらヤマを張らない**（`commitRateFor` を下げる）。
   → 崩れ後の空振り差が 6.2pt → 5.8pt と**動かない**。3つの変更が打ち消し合う:
   読み鈍りは空振り増だが、**張らなくなると外したときの -1（窓×0.84）も
   食らわなくなる**ため打者の得になる
2. **崩した直後は捕手が同じ引き出しで畳み掛ける**（`sequenceScore` の
   repeat ペナルティを割り引く）。→ 崩れ後の空振りが 15.4% → 13.3% と**悪化**。
   同じ所へ続けると `shiftMeetAdjust`（係数14）で打者にミート補正が入るため、
   読みを鈍らせた利得を上回る。**「repeat が不利」は読みモデルと物理の2箇所に
   書かれている**ので、片方だけ緩めても意味がない

### 「タイミングを崩されたら当てにいく」（`pitchSequence.js` の `fooled`）
物理エンジンは `powerTransferRate = meetQuality^0.6` で
**崩されたスイングにはパワーが乗らない**を既に表現していたが、それは *結果* の話で、
打者が **次の球で構え直す**（振り方を変える）という *判断* が無かった。

- 打席内の記憶として `seq.fooled`（0=芯で捉えた 〜 1=空振り）を持つ。
  `fooled = clamp(1 - meetQuality / 0.55, 0, 1)`、空振りは1.0
- 見送ると `× 0.5` で少し戻る（完全には戻らない）
- 実測: 前球で崩された打席は **59.8% が当てにいく**（前球を捉えていれば30.0%がフルスイング）

**この2つは別物として両方必要**。物理側だけだと「崩されたのにフルスイングし続ける」
打者になり、判断側だけだと「崩されてもパワーは満額乗る」ことになる。

効果は **ミート `-swingPower × 8` / パワー `+swingPower × 10`**。
既存の打撃方針（aggressive: ミート-5 / パワー+8）と同じ桁。
`combineBatterEffects` でコース適性・位置の質と合算するので、`contactChance`・
物理エンジン・打出し角まで追加の配線なしで通る。

- **フルスイングは無料の強化ではない**。パワーが乗るかわりにミートが落ち、
  空振り・ファウル・打ち損じが増える。どちらが得かは場面で変わる
- **`MEAN` は不動点として求めること**。下げると振り抜く球が増え → 空振りが増え →
  2ストライクのカウントが増えて生スコアの平均がまた下がる。
  -0.075 → -0.095 → -0.108 → -0.125（「崩された」項の追加後は -0.19 → **-0.215**）と
  反復して収束させた。1回で決めると centering が効かず、リーグ全体が「当てにいく」側に寄る

**振り方の分布（自然ロスター・スイングした球のみ 43188球）**

| 場面 | フルスイング | 通常 | 当てにいく |
|---|---|---|---|
| 得意コース | **58.9%** | 35.4% | 5.7% |
| 中間 | 23.4% | 51.5% | 25.1% |
| 苦手コース | 5.9% | 41.3% | **52.8%** |
| 2ストライク | 4.0% | 34.6% | **61.4%** |
| それ以外 | **39.9%** | 50.0% | 10.2% |
| 前球で崩された | 7.3% | 32.9% | **59.8%** |
| 前球を捉えた | **30.0%** | 46.1% | 23.9% |

### 「パワー80の打者が全てのスイングでパワー80」ではない
コース適性・振り方・投球位置の質を合算した **1球ごとの実効パワー**の実測
（自然ロスター1シーズン。素のパワー帯ごと）:

| 素のパワー | 5%点 | 25% | 中央 | 75% | 95%点 | σ |
|---|---|---|---|---|---|---|
| 20 | 1 | 8 | 17 | 24 | 32 | 9.9 |
| 50 | 28 | 41 | 48 | 53 | 60 | 9.8 |
| 60 | 39 | 52 | 60 | 67 | 75 | 11.0 |
| 80 | 54 | 64 | 78 | 85 | 92 | 12.1 |

**パワー80の打者は1球ごとに 54〜92 の間で振れる**（最小40・最大98）。
苦手コースを追い込まれてから振れば実効60台、得意コースを打者有利カウントで
振り抜けば90台。**さらにこの後 `powerTransferRate` が掛かる**ので、
崩されたスイングでは実際に打球へ乗るパワーはもっと落ちる。

**固定方針より場面で決める方が良い（同一ロスター反復・8シード×120試合）**

| 方針 | 打率 | 長打 | OPS | 防御率 | K/9 | HR/AB | 現行との差 |
|---|---|---|---|---|---|---|---|
| 場面で決める（現行） | .2515 | .3185 | .6536 | 3.606 | 7.93 | 0.0133 | — |
| 常にフルスイング | .2456 | .3165 | .6453 | 3.424 | **8.75** | **0.0140** | **-0.0083 ± 0.0030** |
| 常に当てにいく | **.2539** | .3118 | .6466 | 3.422 | **7.61** | **0.0107** | **-0.0070 ± 0.0032** |

**振り切るのも当てにいくのも、それ一辺倒は損**。配球の対角攻め（段階4）と同じく
中間＝適応が最善という内部最適が生まれている。性格も正しく出ていて、
フルスイングは本塁打+5%と三振増、当てにいくは打率最高で本塁打-20%。

**リーグ全体への影響（同一ロスター反復・12シード×120試合）**

| | あり | なし | 差 ± 標準誤差 |
|---|---|---|---|
| 打率 / 出塁 / 長打 | .2506 / .3339 / .3179 | .2492 / .3308 / .3161 | +0.0014 / **+0.0031** / +0.0017 |
| 防御率 | 3.515 | 3.446 | +0.070 ± 0.036 |
| K/9 | 8.076 | 8.175 | **-0.099 ± 0.038** |
| BB/9 | 4.825 | 4.700 | **+0.125 ± 0.022** |
| 三振率 / 四球率 | 20.29% / 11.12% | 20.65% / 10.87% | **-0.36pt / +0.25pt** |
| HR/AB | 0.0135 | 0.0129 | +5% |

OPSはほぼ動かず、**三振が減って四球と本塁打が増える**。四球が増えるのは
「2ストライクで当てにいく → ファウルで粘る → 打席が長くなる」ためで、
現実の counterpart がある挙動。打撃側の利得は防御率 +0.078（約2%）に収まっている。

- **UI**: 投球ログに `[フルスイング]`（橙）/`[当てにいく]`（青）を表示。
  采配モードの「打撃」方針（待て/おまかせ/積極）がそのまま振り方に乗る

## 球の出どころ（`src/game/deception.js`）
成瀬善久のように**球速も決め球も無いのに打たれない**投手がいる。球持ちが長く、
体の陰から腕が出るのでリリースが見えないタイプ。本作には「球速」「変化球」
「制球」しか無く、この軸が存在しなかった。

**球速を上げるのと同じではない**——ここが設計の要。

| 経路 | 効果 | 球速との違い |
|---|---|---|
| `calculatePhysicsContact` のタイミング窓 | 反応時間を削る（`DECEPTION_W = 0.18`） | 同じ |
| `guessSuccessRate` | **球種を見分けられない**（`DECEPTION_READ_W = 0.50`） | **速いだけの球は「何が来るか」までは隠せない** |

- **セーブに持たず名前+IDから決定的に導出**する（`batterZone.js` のコース適性と
  同じ方針。生成箇所が散在しているので、生成側を触らずに全投手が持てる）。
  `player.pitching.deception` を明示的に置けばそちらが優先される
- 実測の分布（投手1344人）: 平均50.0 / σ15.6 / 9〜91。
  「見えない」「丸見え」は各5%程度、6割は「普通」
- **UI**: 選手詳細の投球系に「出どころ」のバーと短い説明

**投手像ごとの実測（6シード×110試合・リーグ全員を同じ設定にして比較）**

| 投手像 | 防御率 | 被打率 | K/9 | BB/9 |
|---|---|---|---|---|
| 平均的（140/制球55/出55） | 2.84 | .2289 | 8.73 | 3.37 |
| 速球型（148/制球55/出55） | 2.45 | .2071 | 10.52 | 3.45 |
| **成瀬型（135/制球78/出82/チェンジ70）** | **1.99** | .2156 | 9.41 | 1.27 |
| 同じ投手の出どころを平均に（135/78/**出50**） | 2.33 | .2316 | 8.45 | 1.26 |
| 出どころ丸見え（140/制球55/**出22**） | 3.08 | .2402 | 7.78 | 3.29 |

**135km/hの成瀬型が148km/hの速球型を上回る**（1.99 対 2.45）。同じ投手から
出どころだけ抜くと 2.33 に落ちるので、この軸の寄与は -0.34。

- 出どころ 30→80 の効果は **-0.42 前後**（7シード）。捕手のリード(18→81 で -0.35)
  と同程度の重み。球速別では 132km -0.443 / 140km -0.425 / 150km -0.375 と
  **遅い投手ほど効く**（窓は 1/球速 に比例するので、同じ割合でも削れる ms が大きい）。
  ただし差は誤差内なので「傾向」まで
- リーグ総量は動かない（同一ロスター反復12シード: 防御率 -0.010 ± 0.040 /
  打率 -0.0012 ± 0.0011）。母集団の平均が50＝中立だから

### ⚠ 計測: `globalThis` フックはモジュール定数には効かない
`const DECEPTION_W = Number(globalThis.__DECW ?? 0.06)` を置いて計測スクリプト側で
`globalThis.__DECW = 0.18` としても**効かない**。ES の静的 import は巻き上げられて
先に評価されるので、代入時にはもう定数が読まれている。
これで「係数を3倍にしても効果が変わらない」という**存在しない結論**を一度出した
（実際には3回とも同じ 0.06 を測っていた）。関数の中で毎回読むか、
`getDeception` のような**関数側**にフックを置くこと。

## 打者の4分類（野村克也）— `src/game/batterType.js`
AI打者の狙い方を4つの型に分ける。**型は選ぶものではなく能力から決まる**。

| 型 | 張るもの | 当たり | 中立 | 外れ |
|---|---|---|---|---|
| **A型 直球対応**（理想型） | 直球（変化球にも対応） | 直球+1 / 読みも的中で+2 | 変化球 0 | **なし** |
| **B型 コース狙い** | 得意な側の縦列 | +2・振りにいく(×1.12) | 真ん中 0 | 逆の列 -1・**見送る(×0.55)** |
| **C型 方向決め** | 引っ張り／流し | +1 かつ**打球方向が寄る**(±9度) | 真ん中 0 | 逆 -1 |
| **D型 ヤマ張り** | 球種 | +2 | — | -1 |

- **型の判定**: ミート66以上→A（天才型）。以降はミートに対する相対的な強みで、
  選球眼が勝れば B、パワーが勝れば C、どれも平凡なら D
- **実測の分布（500人）**: A 16.4% / B 31.8% / C 24.4% / D 27.4%。
  平均能力は A=ミート71 / B=選球眼58 / C=パワー62 / D=平凡 と型と噛み合っている
- **A型に外し（負の値）が無いのが理想型の定義**。ミート66という条件自体が厳しいので
  能力の対価として素直に強くしてある
- **B型・C型は打席の頭で「どちら側を待つか」を決める**（`chooseWaitSide`）。
  得意な側で待つだけだと捕手（弱点を突く）と**構造的に常に逆を向く**ため、
  B型の効果が +0.007 と小さかった。選球眼が高いほど「来る側」を読み当てる
  （`readRate = 0.30 + (選球眼-50)/100 × 0.5`、上限0.70）。
  **打席ごとに1回だけ決める**（`sequence.waitSide` に保持）。1球ごとに引き直すと
  打者が毎球気を変えていることになって不自然
- 弱点側で待って来れば、打撃補正は弱点のままでも**準備は出来ている**
  （「分かっていれば打てる」）。ここで段階3と噛み合う
- カウント別に張る確率が変わる（打者有利45% / 平行30% / 投手有利18% / 2ストライク10%）。
  追い込まれたら当てにいく
- **B型だけは常時絞る**（2ストライクで18% / それ以外72%）。
  「あらかじめ打つコースを絞り込む」のが型の identity なので、
  他の型と同じ30%程度にすると +2 と -1 が相殺して個性が何も出ない

**型ごとの効果（同一ロスター反復・10シード×120試合・捕手リード46固定）**

| 型 | 打率 | 出塁率 | 四球率 | 三振率 | OPS差 |
|---|---|---|---|---|---|
| A型 | .272→**.288** | .338→.352 | 9.0→8.9% | 17.4%→**15.3%** | **+0.0413 ± 0.0074** |
| B型 | .177→.183 | .259→**.270** | **10.0→10.7%** | 33.6%→33.0% | **+0.0199 ± 0.0042** |
| C型 | .186→.184 | .256→.255 | 8.6→8.6% | 37.3%→36.6% | -0.0048 ± 0.0083 |
| D型 | .204→.209 | .273→.276 | 8.7→8.5% | 28.4%→28.5% | +0.0101 ± 0.0067 |

⚠ **D型は本来ぴったり0になるはず**（旧実装と同じロジック）。それが +0.010 ± 0.007
出ているのは、B型が見送るようになってゲーム展開が変わる**型間の干渉**。
他の型の数字もこの分（約+0.010）を差し引いて読むこと。
B型の実質は +0.010 前後、A型は +0.031 前後。

- **A型は三振が2.1pt減る**。対応力の高さがそのまま「三振しない打者」になる
- **B型は四球で稼ぐ**（+0.7pt、出塁率+.011）。打率もわずかに上がる。
  「絞る＝逆のコースには手を出さない」（見送り倍率×0.55）と
  「来る側を読む」（`chooseWaitSide`）の2つで、選球眼型らしい出塁の仕方になった
- **C型は本塁打が増える**（別測定で 9.3→10.4／500打数）。方向を決めて振り抜くため。
  ただしOPSへの寄与は測定のたびに +0.005〜+0.026 と振れる（B型の見送りが
  ゲーム展開を変えるため型間で干渉する）
- **D型は従来どおり**（±0）。旧実装がD型そのものだったので当然
- リーグ成績は許容内（打率.249 / 出塁.327 / 防御率3.26 / 四球率10.5% / 三振率21.9%）
- **UI**: 選手詳細のコース適性の上に型バッジ＋説明

### AI打者もヤマを張る（`batterCommitRate` → `commitRateFor`）
従来のAI打者は読みが外れても代償が無く、0 か +1 しか取らなかった。
つまり**良い捕手は「読ませない」ことしかできず、「読み違えさせる」ことができなかった**。
リードの価値が頭打ちだった原因はここ。

- カウント別に張る確率を持つ（打者有利40% / 平行25% / 投手有利15% / 2ストライク8%）
- 張って当たれば **+2**（タイミング窓×1.50）、外せば **-1**（×0.84）
- 張らなければ従来どおり（当たり+1 / 外れ0）
- **リーグ成績は動かない**（防御率 3.31→3.37 / 打率 .2391→.2393。誤差内）。
  +2 と -1 の組み合わせがリーグ平均のリードでちょうど釣り合う

**読みレベルの分布（1リーグ×80試合・NPB相当）**

| リード | 片方外し | 読みなし | 片方的中 | 両方的中 |
|---|---|---|---|---|
| 0 | 13.7% | 49.8% | **27.8%** | **8.7%** |
| 46 | 15.3% | 58.1% | 20.9% | 5.7% |
| 100 | **18.5%** | 69.9% | 9.8% | 1.8% |

良い捕手は打者の的中を 36.5%→11.6% に減らし、読み違えを 13.7%→18.5% に増やす。
（AI打者は球種にしか張らないので -2 には到達しない）

**リードの価値（同一ロスター反復）**

| | 導入前 | 導入後 |
|---|---|---|
| 18→81 防御率 | -0.297 ± 0.031 | **-0.319 ± 0.043** |
| 18→81 K/9 | +0.727 | **+0.829 ± 0.046** |
| 0→100 防御率 | -0.382 ± 0.030 | **-0.498 ± 0.050** |
| 0→100 K/9 | +0.779 | **+1.248 ± 0.030** |

実在する幅(18→81)の上積み +0.022 ± 0.053 は誤差内だが、
**全幅(0→100)では -0.382 → -0.498 と明確に伸びた**（+0.116 ± 0.058）。
K/9 は 0→100 で +0.779 → **+1.248** と大きく増えている。

### 采配モード: プレイヤーが「ヤマを張る」（`resolveBatterGuess`）
攻撃中に **狙い球（直球/変化球）** と **コース（内角/外角/高め/低め）** を張れる。
どちらも `おまかせ` なら従来どおり読み合いはAIに任せる。

- 当てれば +1（`COMMIT_HIT = 0.86` の確率で成立）／外せば **-1**
- コースは**半面**で張る。5×5のセルを指定させるのは操作として重すぎるし、
  打者の意識も「外角を待つ」という粒度
- **真ん中に来た球は中立**（当たりでも外れでもない）。ここを外れ扱いにすると
  3分割なので正解の側に張っても期待値が負になり、機能を使う理由が消える
  （実測: 捕手が内角を狙ってくる場面で「内角に張る」が OPS **-0.013**）

**実測（全員「内角が弱点」・リード70・10シード×120試合・同一ロスター反復）**

| 方針 | 打率 | OPS | 三振率 | おまかせとの差 |
|---|---|---|---|---|
| おまかせ | .2819 | .7273 | 17.0% | — |
| 直球に張る | .2804 | .7300 | 18.0% | +0.0028 ± 0.0032 |
| 変化球に張る | .2734 | .7093 | 18.6% | **-0.0180 ± 0.0022** |
| 内角に張る（捕手が来る側） | .2843 | .7328 | 16.3% | **+0.0055 ± 0.0031** |
| 外角に張る（逆） | .2750 | .7164 | 19.3% | **-0.0108 ± 0.0028** |

**当てずっぽうに張ると損**（内外をランダムに張ると平均 -0.003）、
**読めていれば得**、**外すと明確に損**。`COMMIT_HIT` を 0.92 まで上げると
外しても -0.006 にしかならず賭けが成立しなくなる（測定済み）。0.86 が境目。

- 情報源は揃っている: 試合画面のコース表示（この打席どこに来ているか）と
  選手詳細のヒートマップ（自分の弱点＝捕手が狙ってくる場所）
- 張った次元は**AIの読み合いを置き換える**（両方張れば `guessSuccessRate` も
  `locationReadChance` も使われない）

### リードが効く経路は2つ
1. **打者の狙い球を外す**（`guessSuccessRate`。下記）
2. **打者の弱点コースを要求する**（`pickTargetCell`。25分割グリッド段階3の節を参照）

実在する幅（リード18→81）での防御率は 1 だけなら -0.103、2 を足すと **-0.277**。
どちらも**四球のコストがゼロ**なのが重要で、それ以外の経路（際どいコースを多く要求する、
失投を減らす等）は測るたびに四球のコストが利得を上回った。

### 打者の狙い球を外す（`guessSuccessRate`）
打者の球種予測が的中すると `calculatePhysicsContact` でタイミング窓が **1.3倍** に
広がる。球種固有の空振り効果(whiffBonus 最大0.10)より桁が大きい。

  的中率 = (1 / 持ち球数) × (1 - 0.8 × lead/100) × 選球眼補正

- **リード0 = 配球が完全ランダム**。打者は持ち球数ぶんの1で当てられる
- **リード100 = その8割を外す**

| 持ち球 | リード0 | リード50 | リード100 |
|---|---|---|---|
| 2種類(40%) | 50.0% | 30.0% | 10.0% |
| 3種類(24%) | 33.3% | 20.0% | 6.7% |
| 4種類(29%) | 25.0% | 15.0% | 5.0% |

「的中率20%」という絶対値で定義してはいけない。持ち球5種類以上の投手では
完全ランダム(20%)と同じになり、リードが無意味になる。必ず割合で定義すること。

**実測（同一リーグ内の対比較・30シード×120試合）**

| 指標 | リード0 | リード100 | 差 |
|---|---|---|---|
| 防御率 | 3.392 | 3.163 | **-0.229 ± 0.080（有意）** |
| K/9 | 7.731 | 8.284 | **+0.553 ± 0.084（有意）** |
| 被安打/9 | 8.445 | 8.039 | **-0.406 ± 0.116（有意）** |
| BB/9 | 4.067 | 4.072 | +0.005 ± 0.068（**四球のコストなし**） |

良い捕手は打者に読ませないので**三振が増え、安打が減る**。これが主効果。
- **実在する範囲（リード18対81）でも 防御率 -0.220 ± 0.052（40シード、有意）**。
  K/9 +0.28。24シードでは -0.083 ± 0.081 とノイズに埋もれたので、
  この規模の効果は40シード程度必要
- **内訳（24シード×120試合、リード0対100）** — 三振の増加が失点減に繋がる経路:

  | 指標 | リード0 | リード100 | 差 |
  |---|---|---|---|
  | K/9 | 7.960 | 8.350 | +0.390 |
  | 被安打/9 | 8.474 | 8.043 | **-0.431** |
  | インプレー率 | 68.7% | 67.2% | -1.5pt |
  | BABIP | .2966 | .2908 | -0.0058 |
  | BB/9 | 3.916 | 4.086 | +0.170 |
  | 防御率 | 3.380 | 3.112 | **-0.268** |

  線形ウェイト（単打0.47/本塁打1.40/四球0.33）による検算では -0.192 が期待値で、
  実測 -0.268 と整合する。**三振が増えるだけでなく、当たっても弱い打球になる**
  （インプレー率とBABIPが同時に下がる）のが効いている。
  なお「三振増で四球も増える(+0.170)」と一度記録したが、これは0対100・24シードの
  結果で再現しなかった。18対81・40シードでは **-0.085 ± 0.057** と、むしろ減る

### 捕手3能力の寄与（同一ロスター反復・12〜16シード×120試合）
リード・守備・肩は別能力だが、実際に評価するのは「捕手1人」なので合わせて測る。
**段階3の導入後に、低ノイズの「同一ロスター反復」設計で測り直した値**（下の⚠参照）。

| 比較 | 防御率 | BB/9 | K/9 |
|---|---|---|---|
| リードのみ（18→81） | **-0.277 ± 0.034** | +0.004 ± 0.034 | **+0.568 ± 0.025** |
| 守備のみ（30→75） | **-0.185 ± 0.030** | **-0.272 ± 0.052** | +0.091 ± 0.056 |
| 肩のみ（20→80） | -0.019 ± 0.043 | +0.030 ± 0.048 | +0.076 ± 0.034 |
| **三拍子（18/30/肩20 → 81/75/肩80）** | **-0.497 ± 0.064** | **-0.404 ± 0.063** | **+0.499 ± 0.040** |

- **段階3以降はリードが最大の寄与**（-0.277）。守備(フレーミング)の -0.185 を上回る。
  段階3の前はリード -0.103 で守備の方が主役だった
- **リードは三振と被安打で稼ぐ**（K/9 +0.57 / 被安打-0.43）。**四球のコストはゼロ**
- **守備（フレーミング）は四球で稼ぐ**（BB/9 -0.27）
- **肩の防御率への寄与はノイズ内**。盗塁は確実に抑える（0.743→0.453/試合、肩20→80）が、
  盗塁1つの得点価値が小さいため防御率にはほとんど出ない。
  以前 -0.135 ± 0.061 と記録したが、これはロスター分割設計のノイズだった
- **3つは加算的**。単純合算 -0.481 に対し実測 -0.497（標準誤差0.064）

**捕手1人で防御率0.50・四球0.40・三振0.50。** ロスター編成で明確に意味のある差になる。

- ⚠ **計測設計がノイズの主成分を決める**。「良い捕手3チーム対悪い捕手3チーム」は
  同一リーグ内の対比較でも**ロスターの引きの差**が残り、30シードでも標準誤差0.065止まりで
  符号が run ごとに反転した。`structuredClone` でロスターを退避し、
  **同じロスター・同じ日程のリーグを能力だけ変えて2回走らせる**と16シードで標準誤差0.034。
  シードを増やす前に設計を見直すこと
- ⚠ **範囲を狭めると検出できなくなる**。効果の有無を判定する時は能力の全幅を使うこと

- 従来この経路は死んでいた。自動シミュレーションは的中率が `0.3/0.2` の固定値で
  **リードも球種数も見ていなかった**。采配モードは見ていたが係数0.02で、
  3球種の投手だとリード100でも 0.313→0.273 と13%しか下がらなかった
- **⚠ 小標本の偽陽性に注意**。かつてこの項に「リード20対75で -0.193 ± 0.090、有意」
  「球種選択の分離測定で -0.253 ± 0.096、有意」と記録していたが、どちらも8シードで、
  30〜36シードで再測定すると +0.040 ± 0.068 / -0.018 ± 0.063 とゼロだった。
  1シーズンの防御率のσは約0.4あり、8シードでは標準誤差0.14にしかならない。
  **0.1〜0.2の効果を8シードで判定してはいけない**
- **球種の「選択」は効かない**（上記の再測定どおり）。持ち変化球が平均2個で
  質も近いため、どちらを選んでも平均的な球種構成が動かないため
- **投球位置(zone/edge)の「配分」にリードの基礎項を持たせてはいけない。** 測るたびに
  四球のコストが利得を上回った。`canExecute`（制球依存）だけに掛け、
  リーグ平均の制球50では配分が動かないようにしてある。
  **配分ではなく「同じ狙いの中でどのセルか」を変える経路（段階3）なら四球のコストが出ない**
- **球種スコアには制球コストを入れること**（`scoreBall` の `-(1-levelFactor)*0.20`）。
  効果だけで選ぶと「効くが投げられない未熟な球種」を要求してしまい、
  BB/9 が 3.90→4.12 に増えて良い捕手ほど成績が悪化する

### リード能力の分布（`generateCatcherLead`）
生成箇所ごとに狭い一様分布（トライアウト35-70／大学25-55／補充30-50）だったため、
実在する捕手が25〜70・中央45に収まり、仕組みを整えても差が出しようがなかった。
`N(48, 18)` を 5〜95 で切った共有関数に統一し、実測の分布は **18〜81（中央46）** に。
平均は据え置きなのでリーグ成績は変わらない。

- **リードの edge 要求は投手の制球に条件づける**（`callPitchTarget` の `canExecute`）
  - 条件なしで zone→edge の配分だけを強めると**良い捕手ほど成績が悪化する**。
    際どい要求は四球のコストが打たせて取る利得を上回るため。
    実測（係数0.20→0.80）: リード30で防御3.07/四球7.8% → リード90で防御3.39/四球12.3%
  - `canExecute = max(0, (制球-50)/50)` を掛け、**投手が投げ切れる時だけ際どく要求する**
    形にすると符号が正しくなる（8シード実測でリード30→80 の防御率 制球40で-0.11 /
    制球70で-0.07）。ただし**効果量は標準誤差内**で、リードは依然として弱い能力
  - 実在の捕手のリードは25〜70（中央45）。90は事実上存在しない
- **采配モードのUI**: 守備中に「配球」（球種）と「狙い」（勝負/際どく/誘い）を選べる。
  どちらも `おまかせ` なら捕手AIが決める（`useGameStrategy` の `pitchTypeIndex`/`pitchAim`）

## 走者の進塁（`src/game/baserunning.js`）
**采配モードと自動シミュレーションで共有する**進塁判定。以前は両エンジンとも
外野フライのタッチアップしか無く、走者が散発の試合で得点が構造的に作れなかった。

- **症状**: BaseRuns（得点推定式）と実失点の比が、走者が多いほど正しく少ないほど外れる
  | 制球 | 10 | 30 | 52 | 65 | 85 |
  |---|---|---|---|---|---|
  | 修正前 実/予測 | 97% | 92% | 88% | 84% | 81% |
  | 修正後 実/予測 | 101% | 105% | 102% | 99% | 100% |
- **原因は3つ**
  1. **内野ゴロで走者が一切動かなかった**（`resolveGroundOutAdvance`で追加）
     三塁走者の生還（ゴロGO）約50%・二塁走者の三塁進塁（進塁打）約50%。内野の守備力で増減
  2. **2アウトで積極進塁を禁止していた**。実際は逆で、2アウトの走者は打球を確認せず
     スタートを切るため最も積極的（単打での2塁走者生還は0アウト約5割→2アウト約8.5割）
  3. **二塁打に積極進塁が無く**、1塁走者は必ず3塁で停止していた（実際は約45%が生還）
- **一塁走者は対象外**: 「打者アウト＋走者は一塁のまま」は「二塁でフォースアウト＋
  打者は一塁で生存」と同じ盤面・同じアウト数なので、野手選択の表現としてすでに正しい
- **采配モードには積極進塁も捕殺も無かった**（固定進塁のみ）。`tryExtraAdvance` で共通化
- 結果: NPB相当の戦力での失点 3.29 → 3.74（実NPB 3.70）、走者1人あたり得点 0.26 → 0.31（実0.327）

## 打球初速と飛距離を実データ基準にする（`EV_*` / `NPB_CARRY`）
飛距離の式は実測に合わせて較正されている（EV153→114m / 161→123m は
実際の 95mph→113m / 100mph→122m と一致）。つまり **EVは実物の km/h 単位**なのに、
旧係数では中央値が128km/hしか出ておらず（実MLB 145）、ハードヒット率(153km+)が
**1.8%**（実35-40%）だった。

**それでも本塁打率が合っていたのは2つの誤差が打ち消し合っていたから**。
バレル帯(26-34度)に入る打球が18.7%（実13%）と多く、
「そこまで強くない当たりが完璧な角度で入る」ことで帳尻が合っていた
（本作の本塁打の平均初速147km/h。実MLB 166km/h）。

| | 修正前 | **修正後** | 実データ |
|---|---|---|---|
| EV 5%/25%/中央/75%/95% | 103/117/128/138/149 | **105/126/145/162/179** | 103/126/145/161/175 |
| ハードヒット率(153km+) | 1.8% | **38%** | 35-40% |
| 打球種別ごとの平均EV | — | **139/149/150/110** | 138/151/148/116 |
| バレル(EV158+ & 26-34度) | — | **6.0%** | 6-8% |

- 係数は `EV_PTR/EV_SWEET/EV_BASE/EV_POWER/EV_QUALITY/EV_QUALITY_POWER`。
  実測の meetQuality 分布に対して分位が実データと一致するよう当てはめた
- 上限は 175 → **190km/h**（実MLBの最大）

### ⚠ 打球方向は一様分布ではない
`Math.random() * 90 - 45` で **±45度の一様分布**だった。そのため
フライの |方向| の中央値が **25.0度**（実データ 約15度）になり、
半分が両翼寄り＝フェンスが100mと短い側へ飛んでいた。
一様2つの和（三角分布・SD18.4度・範囲は±45度ちょうど）にして
**中央値14.9度 / 75%点25.2度**（実 15 / 26）に一致させた。

### ⚠ 打出し角の上端が32度で頭打ちだった
`calculateLaunchAngle` の各分岐の上端が 32〜47度で、
**「飛距離の出ない高いフライ(40-50度)」がほとんど存在しなかった**。
フライが全部最適角に密集し、本塁打が実データの3倍出ていた最大の原因。
上端を50度台まで伸ばし、ポップフライ(50度以上)の割合も実データに寄せた。
打球種別は **ゴロ41.9 / ライナー19.5 / フライ31.4 / ポップ7.2%**（実 44/20/27/8）。

⚠ **ポップフライは「上限が50度」ではない**。低品質コンタクト(meetQuality≤0.4)の
分岐が 50〜85度を出すので最大95度まで出る（打球方向の補正込み）。
ただしこの分岐は**ファウルで大半が消える**（`foulProb = 0.72 - meetQuality×0.40`
なので芯を外した打球ほどファウルになる）ため、素の発生率を実データの倍近く
持たせないと 8% に届かない。実測: 50度以上 7.2% / うち捕手が処理 20.7%。
捕手の処理は全打球の **1.5%**（実データの捕手刺殺は約1%）、投手 2.8%（実 約3%）。

### `NPB_CARRY` — NPB球はMLB球より飛ばない
飛距離の式はMLBのStatcast実測に合わせてあるので、そのまま使うと
**NPBの2倍近い本塁打が出る**（HR/フライ MLB 12-14% 対 NPB 8-10%、
本塁打 1.2 対 0.70/試合）。公認球の反発係数の差を `NPB_CARRY = 0.94` で吸収する。
実測: HR/フライ **9.2%** / 本塁打 **0.55-0.75/試合**。

⚠ **EV・バレル帯の割合・打出し角の分布・NPB_CARRY の4つは必ずセットで較正する**。
EVを上げると本塁打が激増するので、角度分布と飛距離係数で戻す関係にある。

## 盗塁（`src/game/stealing.js`）
**采配モードと自動シミュレーションで共有する**。以前は2エンジンが完全に別のモデルで、
同じ選手・同じ場面でも結果が別世界だった。

- 自動シミュ … `成功率 = (走力-25)×1.8 - 捕手肩×0.3 - 制球×0.1 + 乱数±10`。
  **盗塁スキル(`batting.steal`)を一切見ていなかった**（走力だけ）
- 采配モード … 試行率も成功率も別式。捕手の肩を二乗で引く独自スケール、
  左投手の牽制ボーナス、三塁盗塁の左投げペナルティも独自

`stealSuccessRate` / `stealAttemptRate` に集約。走力・盗塁スキル・捕手の肩・
投手のクイック（制球で近似）・左投手の牽制・三塁盗塁の補正を1箇所に持つ。

- **自動シミュは盗塁死を記録していなかった**（成功率が常に100%と出ていた）。
  盗塁刺の守備機会（捕手の補殺＋塁カバーの刺殺）も記録するようにした
- 実測: 盗塁 0.4〜0.6/チーム/試合（ロスターの引きで振れる）・成功率 73〜77%
  （実NPB 0.55・72-75%）
- ⚠ 采配モードの `bases` は boolean で走者を識別できないため、走者の走力・
  盗塁スキルは**打者のもので近似**している（積極進塁と同じ構造的制約）

## 球速・緩急と打球方向（`VEL_DIR` / `SEQ_DIR` / `PULL_BASE`）
打球方向に効く要素を3つに分けて持つ。旧モデルは全部を `velShift` の係数に
混ぜ込んでいたため、球速の効きを実データに合わせて下げるとリーグ全体の
引っ張り率まで一緒に落ちてしまった。

| 要素 | 定数 | 内容 |
|---|---|---|
| そもそも引っ張り気味 | `PULL_BASE = 6.0` | バットは前で最速になる。方向の中心を引っ張り側に置く |
| **球速そのもの** | `VEL_DIR = 0.15` | 速い球は差し込まれて流し、遅い球は引っ張り（142km基準） |
| **緩急（前球との差）** | `SEQ_DIR = 0.22` | 速球→遅球は早く振り出して引っ張り、遅球→速球は振り遅れて流し |

### ⚠ 球速の係数は 0.67 → 0.15 に下げた
旧値は**実データより3〜4倍効きすぎ**ていた。実測で球種間の引っ張り率が20pt動き
（実MLBは速球37%対カーブ43%で6pt程度）、165km/hに対して右打者がほぼ全打球を
右方向へ打つ状態だった。

### 緩急による方向は新規（従来は存在しなかった）
`velShift` は**その球の絶対速度**しか見ておらず、前球との関係は打球方向に
一切効いていなかった。`calculateBattedBallPhysics` に `lastPitch` を渡して
`seqDiff = 今の球速 - 前球の球速` を効かせる。母集団の平均は0（球速は上下に
振れるだけ）なのでリーグ成績は動かない。

**実測（自然ロスター・全打球）**

| 緩急 | 引っ張り率 |
|---|---|
| 速球→遅球（8km以上減） | **45.4%** |
| ほぼ同速 | 36.7% |
| 遅球→速球（8km以上増） | **30.5%** |

球種別は ストレート33.5% 〜 カッター42.3%（差 8.8pt。実MLB 約6pt）。
リーグ全体の引っ張り率 37-40%（実 38-42%）。

⚠ **3つはセットで較正すること**。`VEL_DIR` を下げると全体の引っ張り率も
一緒に落ちるので `PULL_BASE` で戻す。引っ張り率は担当野手の分布と
本塁打（ポール際はフェンスが近い）に直結する。

## ⚠ 2エンジンの突き合わせ（スキップ＝自動シミュ / 采配モード）
物理と判定は `simulation-logic.js` と `game/*.js` に集約されているので、
**打球・守備の較正は自動的に両方へ効く**。共有されているのは
`calculatePhysicsContact`（EV_*）/ `calculateBattedBallPhysics`（打出し角・方向・
NPB_CARRY）/ `judgeFielderReach`（CATCH・フェンス・pickOutfielder）/
`getErrorRate` / `getThrowErrorRate` / `tryExtraAdvance` /
`resolveGroundOutAdvance` / `DP_BASE`。

**共有されていなかったもの（修正済み）**
1. `fieldingPosition` が采配モードの `resultMap` では `'out'` にしか載っておらず、
   安打で「積極進塁に使う外野手」と「守備成績の記録先」が分からなかった
2. 采配モードの積極進塁が `avgArm`（外野3人の平均肩）で判定されており、
   **実際に打球を処理した野手の肩が効いていなかった**
3. **采配モードには選手個別の守備成績（守備機会・失策・捕殺）の記録が一切なかった**。
   自分で采配した試合だけ守備成績が付かないという状態だった
   （`recordFielding` を追加し、`gameSetup` で seasonStats へ転記）

**まだ揃っていないもの（既知）**
- 走者の足: 采配モードの `bases` は boolean で走者を識別できないため、
  積極進塁の走力は打線9人の平均で近似している（構造的制約）
- 盗塁阻止: 采配モードは独自式（`baseThrowout - speedReduction + …`）で、
  自動シミュの捕手肩＋送球エラー判定とは別物

## 打球を処理した野手（`fieldingPosition` / `pickOutfielder`）
`judgeFielderReach` の戻り値の `fieldingPosition` は
**「積極進塁の駆け引きで肩を使う野手」と「守備機会の記録先」**の2役を持つ。
安打の分岐の多くがこれを返しておらず、**単打の72.4%で野手が不明**だった。

| 分岐 | 記録率(修正前) |
|---|---|
| ヒット！（ライナーが抜けた） | 17% |
| 外野への安打（ゴロが内野を抜けた） | 0% |
| エラー（ヒット扱い/落球/悪送球） | 0% |
| アウト全般 | 100% |

- **積極進塁の判定で単打の72.9%が `throwerArm: null`** になっており、
  「強肩の外野手ほど走者が自重する」という設計が単打の1/4にしか効いていなかった
- 捕殺も `assistBy = fieldingPosition` なので同時に落ちていた（0.121 → 0.308/試合）
- ⚠ **ゴロ・ライナーが内野を抜けたら、担当は抜かれた内野手ではなく外野手**。
  `pickOutfielder(direction, defense)` を切り出して共有する。ここを内野手のままに
  すると内野手の肩で積極進塁を判定することになり、意味が逆になる
- 本塁打だけは野手が関与しないので `fieldingPosition` を持たない（正しい）

### 守備機会は「送球を受けた側」にも付く
アウトにした野手1人にしか守備機会を付けていなかったため、
**一塁手と捕手の守備機会が実データの1/6以下**で、守備率が事実上測定できなかった。

| | 修正前 | **修正後** | 実NPB |
|---|---|---|---|
| 一塁手 | **1.14** | **7.65** | 9.5 |
| 捕手 | **0.52** | **5.99** | 6.5 |
| 合計 | 17.1 | **31.8** | 約27 |

- 内野ゴロのアウト → 捕った野手の補殺 ＋ **一塁手の刺殺**
- 三振 → **捕手の刺殺**
- 併殺 → 二塁のピボット ＋ 一塁手の刺殺
- 残る乖離は外野手が多め（中堅4.0 対 実2.3）・遊撃が少なめ（1.6 対 4.3）で、
  打球の担当野手分布そのものに由来する（別途の課題）

## 打球種別ごとの安打率（`CATCH` / `simulation-logic.js`）
実データの安打率は打球種別で桁違いに違う。**ライナーが最も安打になる打球**で、
ゴロの3倍近い。ここを揃えないと「強い当たりが正面を突かれた」と
「ボテボテが抜けた」が同じ価値になり、打者の質が打球の質に反映されない。

| 打球 | 修正前 | **修正後** | 実データ |
|---|---|---|---|
| ゴロ | .368 | **.235** | .240 |
| ライナー | **.332** | **.644** | **.660** |
| フライ（本塁打込） | .343 | **.229** | .210 |
| ポップ | .020 | .019 | .020 |

修正前は3種とも .33〜.37 とほぼフラットで、**ライナーがゴロより低い**という
逆転が起きていた。リーグ全体のBABIPが合っていたのは、ゴロが抜けすぎる分と
ライナーが捕られすぎる分が相殺していただけ。

- 捕球率は `CATCH` に名前付きでまとめてある
  （groundFront .915 / groundSide .85 / linerInfield .60 / linerOutfield .44 /
  flyInfield .97 / popup .95 / flyShallow .985 / flyMedium .96 / flyDeep .93）
- ⚠ **4つは必ずセットで較正する**。リーグのBABIPは Σ(種別の割合 × 安打率) で
  決まるので、1つだけ動かすと打率が壊れる
- 較正は**打球コーパスを1度ダンプして judgeFielderReach に通し直す**のが速い。
  毎回シーズンを回すと1試行30秒かかるうえ、打球の引きがノイズになる
- 副次的に **内野安打が単打の28.7% → 18.5%**（実NPB 約13%）に改善。
  ゴロが内野を抜けすぎていた

### 併殺は「内野ゴロのアウト」だけを条件にする（`DP_BASE`）
`bases[0] && outs < 2 && launchAngle < 10 && !isOutfieldFly` で判定する。
以前は `distance < 40` も条件にしていたが、**この分岐に来た時点で内野手が
捕球している**（外野へ抜けた打球は 'out' にならない）ので二重の門番だった。

- 実測: **0.160 → 0.647 /チーム/試合**（実NPB 約0.70）
- `DP_BASE = 34`（内野守備50・走者の足55のとき34%）。守備で ±、走者の足で ∓
- `constants.js` に置いて自動シミュと采配モードで共有する
- ゴロの捕球率を変えると併殺の母数が変わるので、`CATCH` を触ったら測り直すこと

## 長打の物理（`src/simulation-logic.js` judgeFielderReach）
- **球場**: ポール際96m / 中間112m / センター119m。`96 + 23 × cos(方向)` で補間する
  - 以前は `cos²` 補間で 99m/**110m**/122m。中間（パワーアレイ）が浅すぎて引っ張った
    打球が容易にスタンドインし、逆に中堅方向は遠すぎた
- **フェンスを越える飛距離があり打出し角度が20-45度なら、原則そのまま本塁打**
  - 旧実装は「越えていても球速とパワーで20〜40%しかHRにしない」確率判定だった。
    実測で**塀を越える打球は全打球の2.69%（=0.72本/試合）あるのにHRは0.24本/試合**しか
    出ておらず、6割以上が外野フライに落とされていた
  - 能力差は飛距離側（`carryBase` × 角度補正 × パワー補正）で既に表現されている。
    **ここに能力係数を足すと二重評価になる**
  - 塀際の好捕・向かい風で稀に阻まれる（余裕5m未満で最大22%、それ以上は4%）
- **二塁打・三塁打は閾値ではなく確率**。旧実装は「95m超かつEV142+」の階段状で、
  外野の間を抜ける当たりの大半が単打になっていた（二塁打が安打の12%。実際は18%）
  - 二塁打: `(飛距離-78)/25×1.10 + (初速-130)/20×0.55 + (走力-60)/100×0.30`
  - 三塁打: コーナー(|方向|>26度)かつ88m超で `0.28 + (走力-55)/100×1.4`
- **実測（NPB相当=ミート58/パワー55/制球60/守備60）**: 安打8.2 二塁打1.3 三塁打0.15 本塁打0.71
  （実NPB 8.3 / 1.50 / 0.15 / 0.70）。長打率.351 / 失点3.9
  - 実際の独立リーグ戦力（ミート46/パワー34/制球52）では 本塁打0.43 二塁打0.91 失点4.8
- **四球率は実データより高い（NPB相当で14〜15%。実NPB 8.5%）**。長打とは独立の
  既知の課題で、投手の制球〜ストライク判定側の較正が必要

## 打球の担当野手分布（`src/simulation-logic.js` judgeFielderReach）
実データ（ゴロ44% / ライナー21% / フライ30% / ポップ5%、内野45-48%）に合わせて較正した。
以前は**フライ55%・ゴロ23%・内野31%**で、内野の守備力がほぼ結果に効いていなかった。

### 打球種別（`calculateLaunchAngle`）
- 打出し角度は「バットの上下どこに当たったか」で決まり、タイミングの良さとは半ば独立。
  **どの meetQuality 帯でも広い分布**を持たせること
  - 旧実装は `meetQuality > 0.8` を全てバレルゾーン(25-35度)にしていたためフライが55%に膨張した
- **バレル帯は飛距離が最大化する30度を中心に狭く取る（26-34度・芯を捉えた打球の58%）**
  - 帯を24-36度に広げると下端がライナーに落ち、**長打種別の比率はほぼ変えずに本塁打が2割減る**。
    幅は打球種別より長打に効くパラメータなので、HRが減ったらここを疑うこと
- 実測: ゴロ43.6% / ライナー23.2% / フライ30.1% / ポップ3.1%

### 担当野手
- **ゴロは飛距離に関わらず内野手が最初に処理する**。抜けて初めて外野への安打になる
  - 旧実装は `distance < 40` のみを内野とし、40m以上のゴロを外野手の担当にしていた
- **ゴロの飛距離は `2 + 初速×0.17 + 乱数20`**。旧式 `15 + 初速×0.15 + 乱数20` は必ず15m以上に
  なり、投手ゴロの条件が構造的に成立しなかった
- **投手・捕手も守備に参加する**
  - 投手: `distance < 29 && |direction| < 14`（マウンドは本塁から18.4m）
  - 捕手: `distance < 13`（バント性）または `launchAngle >= 70 && distance < 25`（真上のポップ）
  - ポップフライ(60度以上)は以前**無条件に「捕手」で記録**していたが、実際は打球位置で担当が変わる
- **中堅の担当範囲は左右両翼より広い**（基準±17度＋足で拡張）。旧実装は±15度で両翼の方が広かった
- 内野の方向帯: 三塁 `< -28` / 遊撃 `< 3` / 二塁 `< 28` / 一塁 `>= 28`（+野手の足で境界が拡張）
  - 三塁は線寄りだけを守り、三遊間は遊撃がカバーする。境界を-24にすると三塁13.0%>遊撃12.9%と
    逆転してしまう（実際は遊撃が三塁の1.5倍捕る）
- 実測分布: 左翼19.6% / 中堅16.8% / 右翼17.0% / 遊撃13.6% / 二塁9.9% / 三塁8.8% /
  一塁7.0% / 投手3.4% / 捕手1.7%
  - 打席の左右比率（右56%/左41%）が打球方向を決めるため、比率を変えたら必ず再測定すること
  - ⚠ **打球方向の分布を変えたら守備範囲の境界も必ず測り直す**。段階②で方向を
    一様分布から三角分布にしたとき、中堅の担当が **16.4% → 24.8%** に膨らんだ。
    中堅は±17度を守る設定だが、ベル型では大半の打球がその範囲に入るため。
    中堅±11度 / 三塁 -23度 / 一塁 +23度 に詰め直して元の分布に戻した
- **ゴロの捕球率は「正面0.85 / 横0.74−難易度×0.30」**。ゴロが全打球の45%を占めるため、
  旧値(0.97/0.88)からは引き下げ、ゴロ23%時代の較正値(0.83/0.60)からは引き上げてある
  - 打率への感度が非常に高い（横の値0.60→0.78で打率.273→.235）。ゴロ比率を変えたら必ず再較正すること
- 効果（守備・肩を揃えた場合／1チームあたり）: 守備35→4.90失点/試合, 50→4.63, 60→2.74, 75→2.56, 90→1.50
  - 失策/試合: 35→4.26, 50→2.81, 60→1.78, 75→1.18, 90→0.86

## 捕手の守備力（`fielding.defense`）
**このゲームで捕手の価値はリードではなく守備力にある。** 実測（8シード×100試合、
捕手守備30→90）で防御率が最大 -0.53 改善するのに対し、リード（30→80）は -0.11 と
標準誤差内に留まる。桁が違うので、捕手を評価する時はまず守備力を見ること。

### フレーミングは制球の低い投手ほど効く（実測で確認済み）
制球が低いほど「枠のすぐ外」に外れる球が増え、フレーミングの対象が増えるため。

| 投手の制球 | 20 | 40 | 60 | 80 | 100 |
|---|---|---|---|---|---|
| ゾーン外・枠すぐ外(フレーミング対象) | 41.9% | 41.1% | 40.2% | 36.7% | 24.4% |
| ゾーン率 | 44.0% | 48.1% | 52.1% | 60.5% | 75.6% |

捕手守備 30→90 による改善（8シード×100試合）:

| 投手の制球 | 四球率 | 防御率 |
|---|---|---|
| 40 | 11.52% → 10.24% (-1.29 ± 0.17) | 3.87 → 3.34 (**-0.53 ± 0.19**) |
| 60 | 8.21% → 6.99% (-1.21 ± 0.16) | 3.11 → 2.79 (**-0.31 ± 0.11**) |
| 80 | 5.04% → 4.31% (-0.73 ± 0.12) | 2.45 → 2.27 (-0.17 ± 0.11 / ノイズ内) |

**ノーコン投手ほど良い捕手を付ける価値がある**という関係が、意図せず正しく出ている。
フレーミング係数を触る時はこの勾配を壊していないか必ず再測定すること。

従来、捕手は「肩とリード（cLead）が良ければ良い捕手」で守備力が無意味だった。以下の2点で効かせる。
- **フレーミング**: 際どい球のストライク率が上がる。**基準はリーグ平均の捕手(=50)**
  - 絶対基準(60等)にすると平均以下の捕手が大半になり、リーグ全体の四球・防御率を押し上げてしまう
  - 効果は控えめ（守備100で+3.0pt、守備20で-1.8pt）
- **暴投・捕逸の抑制**: 走者がいる時のみ1球ごとに判定。投手の制球と捕手の守備で決まる
  - 発生時は全走者が1つ進塁（三塁走者は生還）。自動試合には従来この仕組み自体が無かった
- 実測（捕手守備30→90）: 四球 14.5→12.9 / 暴投 1.08→0.87 / 失点 10.2→9.6（両チーム計・1試合）

## スタミナによるパフォーマンス低下（`src/utils/physics.js` getStaminaPenalty）
- **50%以上**: ペナルティなし
- **50%未満**: 2次曲線 `deficit = 1 - staminaRate * 2` で急激に悪化
  - 球速: `-Math.round(deficit² * 20)` → 最大-20
  - 制球: `-Math.round(deficit² * 30)` → 最大-30
- 目安: 40%→球速-1/制球-1、30%→-3/-5、20%→-7/-11、10%→-13/-19、0%→-20/-30
- 降板閾値(25%)時点で既に球速-9、制球-14程度

## 疲労と成長率変動（`src/game/autoSimulation.js`, `src/game/gameSetup.js`）

シーズン中の出場と疲労が成長率（`growthModifier`）にリアルタイムで影響する。

### 成長率変動ルール
- **野手**: 10試合出場ごとに +0.01
- **先発投手**: 15イニング(45アウト)ごとに +0.01
- **リリーフ(closer/setup/ace_relief)**: 4登板ごとに +0.01（緊張感のある場面で成長）
- **リリーフ(その他)**: 5登板ごとに +0.01
- **疲労50超で出場/登板**: -0.01（毎試合判定）
- **優勝チーム**: シーズン終了時 +0.05
- **年度引き継ぎ**: 前年の変動 ×0.5 で持ち越し

### 野手疲労蓄積（スタメン出場時）
- `baseFatigue = Math.round(15 - (bodyStamina / 100) * 8)` → 体力100で+7、体力50で+11、体力30で+13/試合
- 休養日の回復: `Math.round(7 × (0.7 + recovery/100 × 0.6))` → 回復力50で-7/日
- 体力50の選手が週6試合+休み1日 → 1週間で疲労59（50超え）
- bodyStaminaが高い選手ほど疲労が溜まりにくく、成長率ペナルティを受けにくい
- 詳細は `仕様書.md` §12.5 参照

## キャンプ練習メニュー（`src/season/yearProgressionSystem.js`）

キャンプは4クール。毎クール、メイン1つ+サブ1つを選択して実行。
成長量は年齢・経験値・ポジション/打順補正あり。

### 高能力値の成長減衰（重要）
- **技術系(meet/power/eye/control/defense/steal)**: 70(プロ級)以上で減衰開始、超過1ごとに5%減衰（下限10%）
- **フィジカル系(speed/arm/stamina/bodyStamina/recovery)**: 80以上で減衰、超過1ごとに3%
- **球速**: 155km以上で減衰、超過1ごとに20%
- 技術系は69までは減衰なし、70以降急速に減衰（75で75%, 80で50%, 85で25%, 88以降は下限10%）
- メイン練習の基礎成長係数は0.11（元0.167から約34%減）
- 覚醒発生率は `experience/15`（元 `experience/10` から33%減）
- **覚醒は減衰を受けない**: 発生時は +3〜8 の大幅成長（基礎成長に加算）。"何かのきっかけで飛躍する選手"を再現

### メイン練習（TRAINING_MENUS）
| メニュー | 対象能力 | 成長量目安 | 備考 |
|---|---|---|---|
| 打撃練習 | ミート, パワー, 選球眼 | ミート+0〜2、パワー+0〜1、選球眼+0〜1/クール | パワーは才能依存のため抑制 |
| 走塁練習 | 走力, 盗塁 | 各+0〜2/クール | 野手向け |
| 守備練習 | 守備, 肩力 | 各+0〜2/クール | 投手野手共通 |
| 選球眼練習 | 選球眼 | +0〜2/クール | 野手向け |
| スタミナ練習 | スタミナ | +0〜2/クール | 投手向け |
| 制球練習 | 制球 | +0〜2/クール | 投手向け |
| 球速練習 | 球速 | +0〜2km/クール | 155km以上は減衰 |
| 新球種習得 | 新変化球 | 覚醒(10%):Lv61-80 / 大成功(15%):Lv41-60 / 成功(20%):Lv21-40 / 習得(25%):Lv1-20 / 失敗(30%):習得不可 | 投手のみ、球種選択可 |

### サブ練習（SUB_TRAINING_MENUS）
メインより効果小。基本は40%の確率で+1、そのうち30%で+2。
| メニュー | 効果 | 備考 |
|---|---|---|
| ランニング | 走力+0〜2、体力+1〜4(確定)、投手スタミナ+1(20%) | 安定枠 |
| 筋トレ | パワー/肩力/走力のいずれか1つ+0〜2 | ランダム選択でパワー特化を緩和 |
| ストレッチ | 各能力+1(各10%)、回復+1(30%) | 広く薄く |
| 守備補強 | 守備+0〜2、弱ポジ適正+3(30%) | |
| 変化球練習 | 分配: 全変化球に合計4〜8pt / **1球種を指定すると集中練習**（+2 と適性+1が乗る） | 投手のみ、年齢補正あり |
| 新球種習得 | 成功率12%、成功時Lv20-39 | 投手のみ、球種ランダム |
| フォーム改造 | 成功20%: フォーム変更+制球+3〜5 / 失敗: 制球-1〜3 | 投手ハイリスク |
| 打席変更 | switch化15% / 反対20% / switch→片30%。失敗: ミート-1〜2 | ハイリスク |
| サブポジ練習 | 指定ポジ適正+9〜15 | ポジション選択可 |
| Cリード学習 | Cリード+1〜3 | 捕手向け |

### 派遣（Year2以降、キャンプ全期間）
派遣した選手は通常練習不参加。結果はキャンプ終了時に判明。
各チーム各派遣先1人ずつ、大学派遣はチーム全体で4人まで、プロ研修はリーグ全体で合計8人まで。**失敗なし**だが3段階のリスク制（飛躍1.5倍 / 成長1.0倍 / 微成長0.5倍）。
- 飛躍率: 25% + min(15%, experience/250) → 25〜40%
- 微成長率: max(5%, 15% - experience/300) → 5〜15%（経験豊富な選手ほどリスク減）
- 成長率: 残り（約45〜60%）
- **派遣は成長減衰を受けない**。

**大学野球留学**（26歳以下 / 総合力60以下 / **フィジカル系**メイン）
| 対象 | 微成長 (0.5倍) | 成長 (1.0倍) | 飛躍 (1.5倍) |
|---|---|---|---|
| 投手 | 球速+1〜2(≤158), スタミナ+5〜12, 制球+1〜2 | 球速+2〜4(≤158), スタミナ+10〜24, 制球+2〜5 | 球速+3〜6(≤158), スタミナ+15〜36, 制球+3〜7 |
| 野手 | パワー+4〜8, 走力+3〜6, 肩力+2〜4, ミート+1〜2 | パワー+8〜17, 走力+6〜13, 肩力+4〜9, ミート+2〜5 | パワー+12〜25, 走力+9〜19, 肩力+6〜13, ミート+3〜7 |

**プロ研修**（24歳以下 / 総合力55以下 / **技術系**メイン）
| 対象 | 微成長 (0.5倍) | 成長 (1.0倍) | 飛躍 (1.5倍) |
|---|---|---|---|
| 投手 | 制球+4〜8, 球速+0〜1(≤155), 変化球各+2〜6, スタミナ+2〜7 | 制球+8〜17, 球速+1〜3(≤155), 変化球各+5〜12, スタミナ+5〜14 | 制球+12〜25, 球速+1〜4(≤155), 変化球各+7〜18, スタミナ+7〜21 |
| 野手 | ミート+4〜8, 選球眼+3〜6, 守備+2〜5, パワー+1〜2 | ミート+8〜17, 選球眼+6〜13, 守備+5〜10, パワー+2〜5 | ミート+12〜25, 選球眼+9〜19, 守備+7〜15, パワー+3〜7 |

- **追加覚醒**: 微成長時10% / 成長時20% / 飛躍時30%の確率で、投手は球速/制球、野手はミート/パワー/走力のうちランダム1つが +5〜12（倍率適用後）。**ただし球速が選ばれた場合は +2〜4（倍率適用後 最大6km）に抑制**
- **球速成長キャップ**: 1回の派遣で球速の総成長量は **最大13km** に制限（リアリズム維持のため）
- 詳細は `仕様書.md` §12「シーズン成長システム（キャンプ & 派遣）」参照
